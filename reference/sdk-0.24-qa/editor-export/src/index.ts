// @ts-nocheck
"use strict";
/* Generated by the HackHub Quest Mod Editor (build 2026-09-19.r203). Edit the project, not this file. */
var sdk = require("@hotbunny/hackhub-content-sdk");
var PROJECT = {"mod":{"id":"qe-sdk-024-editor-qa","name":"QE SDK 0.24 Editor QA Scaffold","version":"1.0.28","author":"HackHub Quest Editor","description":"Evidence-only project for SDK 0.24 in-game verification: native Wi-Fi, HTTP/curl events, the Timer node delay and calendar modes, the Twotter node (accounts, backdated series, cleanup), and the pack extras (start-menu entry, desktop widget, right-click entries, translated words). Nothing auto-starts - claim one quest at a time with the harness command qe24 run - so a tester sees one row's noise, not five.","tags":["qa","sdk-0.24","editor"],"dependencies":[],"minSdkVersion":"0.24.0","apiVersion":1},"quests":[{"id":"q-sdk-024-editor-qa","name":"QESdk024EditorQa","title":"QE SDK 0.24 editor QA","description":"Install on a throwaway save to verify editor-generated native Wi-Fi and SDK 0.24 HTTP/browser events. When this checklist says an objective ticks, look at the active quest/objective tracker: the line should become checked/completed, often with a debug toast.","icon":null,"group":"sandbox","rewards":{"money":0,"xp":0},"employer":{"firstName":"QE","lastName":"Bot","email":"qa@qe24.test"},"autoStart":false,"autoComplete":false,"abandonable":true,"hasCompleteButton":false,"hideObjectivesWhenDone":false,"closingObjectiveText":"QE24 editor QA objectives recorded.","questsToComplete":[],"maxClaim":null,"maxClaimPerDay":null,"hackhubPost":null,"dialog":[],"graph":{"nodes":[{"id":"n-start","type":"entry.start","position":{"x":0,"y":0},"data":{}},{"id":"n-wifi","type":"world.wifi","position":{"x":280,"y":0},"data":{"ssid":"QE24-LAB-5G","password":"correct-horse-battery","signal":3,"bssid":"02:24:00:00:24:02","channel":44,"wps":true,"ipMode":"random","ip":"","model":"TP-Link Archer C6","users":[{"id":"u-admin","username":"admin","password":"qe24-admin","firstName":"QE","lastName":"Router"}],"ports":[{"id":"p-http","external":80,"internal":80,"active":true,"locked":true,"service":"http","version":"RouterUI 1.2.3"},{"id":"p-ssh","external":22,"internal":22,"active":true,"locked":false,"service":"ssh","version":"OpenSSH 8.9.0"}],"children":[{"id":"d-client","ip":"10.24.0.2","name":"qe24-lab-client","type":"DEVICE","lanIp":"10.24.0.2","vulnerabilities":[],"users":[{"id":"u-qa","username":"qa","password":"wifi-child","firstName":"Wi","lastName":"Fi"}],"ports":[{"id":"p-child-ssh","external":22,"internal":22,"active":true,"locked":false,"service":"ssh","version":"OpenSSH 8.9.0"}],"rules":[],"rootFiles":[{"id":"f-logs","name":"logs","isFolder":true,"children":[{"id":"f-qa","name":"qa","isFolder":false,"data":"wifi child reached","extension":"txt"}]}],"children":[]}],"destroyOnComplete":false}},{"id":"n-mail","type":"comms.dialogue","position":{"x":560,"y":0},"data":{"kind":"mail","phone":{"branch":"default","startIndex":0,"continueMode":"onEnd"},"kisscord":{"contactId":"","messages":[]},"mail":{"from":"qa@qe24.test","to":"player@gomail.com","subject":"QE24 editor QA scaffold","content":"Editor QA scaffold is active. Tick means the active quest/objective tracker line becomes checked/completed; the webpage loading by itself is not enough. Open http://qe24-website.test/ and /echo, then record which of http-request and http-response checked off. Connect to Wi-Fi QE24-LAB-5G with passphrase correct-horse-battery, then disconnect.","replyable":false},"weechat":{"host":"","password":"","registerServer":true,"messages":[]},"postLive":false}},{"id":"n-start-note","type":"flow.debug","position":{"x":840,"y":0},"data":{"label":"QE24 editor scaffold started; native Wi-Fi node emitted with bssid/channel/wps.","labelAuto":false,"toast":false,"includeData":true,"includePayload":true}},{"id":"n-load","type":"entry.load","position":{"x":0,"y":220},"data":{}},{"id":"n-load-note","type":"flow.debug","position":{"x":280,"y":220},"data":{"label":"QE24 editor scaffold OnObjectivesStart/listeners registered or re-registered after load.","labelAuto":false,"toast":false,"includeData":true,"includePayload":true}},{"id":"t-http-response","type":"trigger.event","position":{"x":0,"y":460},"data":{"event":"Http.Response","conditions":[{"id":"c-http-host","join":"and","field":"request.host","op":"equals","value":"qe24-website.test"}]}},{"id":"t-http-request","type":"trigger.event","position":{"x":0,"y":620},"data":{"event":"Http.Request","conditions":[{"id":"c-req-host","join":"and","field":"host","op":"equals","value":"qe24-website.test"}]}},{"id":"t-http-intercept","type":"trigger.event","position":{"x":0,"y":780},"data":{"event":"Http.Intercepted","conditions":[{"id":"c-int-host","join":"and","field":"host","op":"equals","value":"qe24-website.test"}]}},{"id":"t-wifi-connect","type":"trigger.event","position":{"x":0,"y":940},"data":{"event":"Network.WifiConnected","conditions":[{"id":"c-wifi-name","join":"and","field":"wifiNetwork.name","op":"equals","value":"QE24-LAB-5G"}]}},{"id":"t-wifi-disconnect","type":"trigger.event","position":{"x":0,"y":1100},"data":{"event":"Network.WifiDisconnected","conditions":[]}},{"id":"o-http-response","type":"objective","position":{"x":360,"y":460},"data":{"name":"http-response","description":"Open http://qe24-website.test/ and /echo in Browser. If this objective gets a checkmark/completed state, Http.Response fired for the editor website.","hint":"Report: page loaded yes/no, http-response checked yes/no, and any debug toast/log text. If the page loads but this stays unchecked, record Response as not fired.","info":"r166 evidence-only SDK 0.24 QA scaffold; no feature fence should be lifted from this project alone.","terminalCommand":"","hidden":false}},{"id":"o-http-request","type":"objective","position":{"x":360,"y":620},"data":{"name":"http-request","description":"Open http://qe24-website.test/ and /echo in Browser. If this objective gets a checkmark/completed state, Http.Request fired before the response.","hint":"Report: page loaded yes/no, http-request checked yes/no, and any debug toast/log text. It may differ from http-response; that is the point of this check.","info":"r166 evidence-only SDK 0.24 QA scaffold; no feature fence should be lifted from this project alone.","terminalCommand":"","hidden":false}},{"id":"o-http-intercepted","type":"objective","position":{"x":360,"y":780},"data":{"name":"http-intercepted","description":"Optional: with the raw qe24 harness also installed, run qe24 intercept on, open http://qe24-website.test/, then qe24 intercept queue and qe24 intercept forward. Check whether this objective completes.","hint":"Report held request yes/no and http-intercepted checked yes/no. Run qe24 intercept off if anything appears stuck.","info":"r166 evidence-only SDK 0.24 QA scaffold; no feature fence should be lifted from this project alone.","terminalCommand":"","hidden":false}},{"id":"o-wifi-connect","type":"objective","position":{"x":360,"y":940},"data":{"name":"wifi-connect","description":"Connect to QE24-LAB-5G using correct-horse-battery. A checkmark here means the editor-generated Wi-Fi connect event fired.","hint":"Report whether the objective checked and whether scan/status shows BSSID 02:24:00:00:24:02, channel 44, WPS true.","info":"r166 evidence-only SDK 0.24 QA scaffold; no feature fence should be lifted from this project alone.","terminalCommand":"","hidden":false}},{"id":"o-wifi-disconnect","type":"objective","position":{"x":360,"y":1100},"data":{"name":"wifi-disconnect","description":"Disconnect from QE24-LAB-5G. A checkmark here means Network.WifiDisconnected fired for the editor scaffold.","hint":"Report whether this objective checked after disconnecting and whether reload avoids duplicate QE24-LAB-5G networks.","info":"r166 evidence-only SDK 0.24 QA scaffold; no feature fence should be lifted from this project alone.","terminalCommand":"","hidden":false}},{"id":"d-http-response","type":"flow.debug","position":{"x":720,"y":460},"data":{"label":"Editor QA ticked: http-response","labelAuto":false,"toast":false,"includeData":true,"includePayload":true}},{"id":"d-http-request","type":"flow.debug","position":{"x":720,"y":620},"data":{"label":"Editor QA ticked: http-request","labelAuto":false,"toast":false,"includeData":true,"includePayload":true}},{"id":"d-http-intercepted","type":"flow.debug","position":{"x":720,"y":780},"data":{"label":"Editor QA ticked: http-intercepted","labelAuto":false,"toast":false,"includeData":true,"includePayload":true}},{"id":"d-wifi-connect","type":"flow.debug","position":{"x":720,"y":940},"data":{"label":"Editor QA ticked: wifi-connect","labelAuto":false,"toast":false,"includeData":true,"includePayload":true}},{"id":"d-wifi-disconnect","type":"flow.debug","position":{"x":720,"y":1100},"data":{"label":"Editor QA ticked: wifi-disconnect","labelAuto":false,"toast":false,"includeData":true,"includePayload":true}}],"edges":[{"id":"e-start-wifi","source":"n-start","sourceHandle":"out","target":"n-wifi","targetHandle":"in","kind":"flow"},{"id":"e-wifi-mail","source":"n-wifi","sourceHandle":"out","target":"n-mail","targetHandle":"in","kind":"flow"},{"id":"e-mail-note","source":"n-mail","sourceHandle":"out","target":"n-start-note","targetHandle":"in","kind":"flow"},{"id":"e-load-note","source":"n-load","sourceHandle":"out","target":"n-load-note","targetHandle":"in","kind":"flow"},{"id":"e-t-http-response-o-http-response","source":"t-http-response","sourceHandle":"when","target":"o-http-response","targetHandle":"trigger","kind":"condition"},{"id":"e-t-http-request-o-http-request","source":"t-http-request","sourceHandle":"when","target":"o-http-request","targetHandle":"trigger","kind":"condition"},{"id":"e-t-http-intercept-o-http-intercepted","source":"t-http-intercept","sourceHandle":"when","target":"o-http-intercepted","targetHandle":"trigger","kind":"condition"},{"id":"e-t-wifi-connect-o-wifi-connect","source":"t-wifi-connect","sourceHandle":"when","target":"o-wifi-connect","targetHandle":"trigger","kind":"condition"},{"id":"e-t-wifi-disconnect-o-wifi-disconnect","source":"t-wifi-disconnect","sourceHandle":"when","target":"o-wifi-disconnect","targetHandle":"trigger","kind":"condition"},{"id":"e-o-http-response-debug","source":"o-http-response","sourceHandle":"done","target":"d-http-response","targetHandle":"in","kind":"flow"},{"id":"e-o-http-request-debug","source":"o-http-request","sourceHandle":"done","target":"d-http-request","targetHandle":"in","kind":"flow"},{"id":"e-o-http-intercepted-debug","source":"o-http-intercepted","sourceHandle":"done","target":"d-http-intercepted","targetHandle":"in","kind":"flow"},{"id":"e-o-wifi-connect-debug","source":"o-wifi-connect","sourceHandle":"done","target":"d-wifi-connect","targetHandle":"in","kind":"flow"},{"id":"e-o-wifi-disconnect-debug","source":"o-wifi-disconnect","sourceHandle":"done","target":"d-wifi-disconnect","targetHandle":"in","kind":"flow"}]}},{"id":"qe-siv3sx5g","name":"QESdk024TimerQa","title":"Timer QA (S-01/S-02/S-03)","description":"Auto-starts. Timer A (2 in-game minutes) must fire mail + toast. Timer B (2 in-game hours) must fire on reload survival, and must be cancelled if the quest is completed or abandoned first.","icon":null,"group":"sandbox","rewards":{"money":0,"xp":0},"employer":{},"autoStart":false,"autoComplete":false,"abandonable":true,"hasCompleteButton":false,"hideObjectivesWhenDone":false,"closingObjectiveText":"","questsToComplete":[],"maxClaim":null,"maxClaimPerDay":null,"hackhubPost":null,"dialog":[],"graph":{"nodes":[{"id":"qe-5r4w5o8j","type":"entry.start","position":{"x":0,"y":0},"data":{}},{"id":"qe-j45yw3g4","type":"flow.debug","position":{"x":260,"y":0},"data":{"label":"timer quest started","labelAuto":false,"toast":false,"includeData":false,"includePayload":false}},{"id":"qe-tmrf7pia","type":"flow.timer","position":{"x":520,"y":0},"data":{"mode":"after","years":0,"months":0,"weeks":0,"days":0,"hours":0,"minutes":2,"offsetYears":0,"offsetMonths":0,"offsetWeeks":0,"offsetDays":0,"hour":0,"minute":0,"dateYear":0,"dateMonth":0,"dateDay":0}},{"id":"qe-vycm8q8p","type":"comms.dialogue","position":{"x":780,"y":0},"data":{"kind":"mail","phone":{"branch":"default","startIndex":0,"continueMode":"onEnd"},"kisscord":{"contactId":"","messages":[]},"mail":{"from":"qe24-timer@test.net","to":"player@gomail.com","subject":"Timer A arrived (S-01 green)","content":"<p>The 2 in-game minute timer fired. S-01 (fire) is green if you read this within a few seconds of loading the save.</p>","replyable":false},"weechat":{"host":"","password":"","registerServer":true,"messages":[]},"postLive":false}},{"id":"qe-i5q1pcr0","type":"fx.notify","position":{"x":1040,"y":0},"data":{"message":"Timer A fired","variant":"toast","tone":"info"}},{"id":"qe-9b919r74","type":"flow.timer","position":{"x":1300,"y":0},"data":{"mode":"after","years":0,"months":0,"weeks":0,"days":0,"hours":2,"minutes":0,"offsetYears":0,"offsetMonths":0,"offsetWeeks":0,"offsetDays":0,"hour":0,"minute":0,"dateYear":0,"dateMonth":0,"dateDay":0}},{"id":"qe-i0duex84","type":"fx.notify","position":{"x":1560,"y":0},"data":{"message":"Timer B fired (do NOT see this if the quest ended first)","variant":"toast","tone":"info"}}],"edges":[{"id":"qe-hwwxxklz","source":"qe-5r4w5o8j","sourceHandle":"out","target":"qe-j45yw3g4","targetHandle":"in","kind":"flow"},{"id":"qe-9vctmhbs","source":"qe-j45yw3g4","sourceHandle":"out","target":"qe-tmrf7pia","targetHandle":"in","kind":"flow"},{"id":"qe-weelqqhd","source":"qe-tmrf7pia","sourceHandle":"out","target":"qe-vycm8q8p","targetHandle":"in","kind":"flow"},{"id":"qe-zqr85tzu","source":"qe-vycm8q8p","sourceHandle":"out","target":"qe-i5q1pcr0","targetHandle":"in","kind":"flow"},{"id":"qe-mkt997g6","source":"qe-i5q1pcr0","sourceHandle":"out","target":"qe-9b919r74","targetHandle":"in","kind":"flow"},{"id":"qe-hx2c4we3","source":"qe-9b919r74","sourceHandle":"out","target":"qe-i0duex84","targetHandle":"in","kind":"flow"}]}},{"id":"qe-cal1","name":"QESdk024TimerCalQa","title":"Timer QA (calendar: S-05/S-06/S-07/S-13)","description":"Auto-starts. Two rows fire within seconds (an exact date already past, and a coming day already past today - both must fire immediately). The third arms a mixed '1 month 2 weeks 2 days at 18:23' row that resolves at arm time: read it with 'qe24 timers' instead of waiting. Save and reload before it is due - it must still be listed.","icon":null,"group":"sandbox","rewards":{"money":0,"xp":0},"employer":{},"autoStart":false,"autoComplete":false,"abandonable":true,"hasCompleteButton":false,"hideObjectivesWhenDone":false,"closingObjectiveText":"","questsToComplete":[],"maxClaim":null,"maxClaimPerDay":null,"hackhubPost":null,"dialog":[],"graph":{"nodes":[{"id":"qe-cal1-start","type":"entry.start","position":{"x":0,"y":0},"data":{}},{"id":"qe-cal1-n0","type":"flow.debug","position":{"x":260,"y":0},"data":{"label":"Timer calendar QA armed - S-06 next (an exact date already past)","labelAuto":false,"toast":false,"includeData":false,"includePayload":false}},{"id":"qe-cal1-t1","type":"flow.timer","position":{"x":520,"y":0},"data":{"mode":"at","years":0,"months":0,"weeks":0,"days":0,"hours":0,"minutes":0,"offsetYears":0,"offsetMonths":0,"offsetWeeks":0,"offsetDays":0,"hour":0,"minute":0,"dateYear":2020,"dateMonth":1,"dateDay":1}},{"id":"qe-cal1-m1","type":"comms.dialogue","position":{"x":780,"y":0},"data":{"kind":"mail","phone":{"branch":"default","startIndex":0,"continueMode":"onEnd"},"kisscord":{"contactId":"","messages":[]},"mail":{"from":"qe24-timer@test.net","to":"player@gomail.com","subject":"S-06 green: an exact date in the past fired immediately","content":"<p>The Timer was set to 2020-01-01 00:00 in-game - a moment long past. It fired the first mail within seconds of the quest starting. If you are reading this within a few seconds of loading, S-06 is green.</p>","replyable":false},"weechat":{"host":"","password":"","registerServer":true,"messages":[]},"postLive":false}},{"id":"qe-cal1-n1","type":"flow.debug","position":{"x":1040,"y":0},"data":{"label":"S-06 done. S-05 next: a coming day, 0 days at 00:00 (already past today)","labelAuto":false,"toast":false,"includeData":false,"includePayload":false}},{"id":"qe-cal1-t2","type":"flow.timer","position":{"x":1300,"y":0},"data":{"mode":"daytime","years":0,"months":0,"weeks":0,"days":0,"hours":0,"minutes":0,"offsetYears":0,"offsetMonths":0,"offsetWeeks":0,"offsetDays":0,"hour":0,"minute":0,"dateYear":0,"dateMonth":0,"dateDay":0}},{"id":"qe-cal1-m2","type":"comms.dialogue","position":{"x":1560,"y":0},"data":{"kind":"mail","phone":{"branch":"default","startIndex":0,"continueMode":"onEnd"},"kisscord":{"contactId":"","messages":[]},"mail":{"from":"qe24-timer@test.net","to":"player@gomail.com","subject":"S-05 green: 'a coming day, 0 days at 00:00' fired immediately","content":"<p>A coming day with 0 days and a clock time already past must fire at once, not wait a day. This mail arriving within seconds is that row's green light.</p>","replyable":false},"weechat":{"host":"","password":"","registerServer":true,"messages":[]},"postLive":false}},{"id":"qe-cal1-n2","type":"flow.debug","position":{"x":1820,"y":0},"data":{"label":"S-13 next: 1 month 2 weeks 2 days at 18:23 - read 'qe24 timers'","labelAuto":false,"toast":false,"includeData":false,"includePayload":false}},{"id":"qe-cal1-t3","type":"flow.timer","position":{"x":2080,"y":0},"data":{"mode":"daytime","years":0,"months":0,"weeks":0,"days":0,"hours":0,"minutes":0,"offsetYears":0,"offsetMonths":1,"offsetWeeks":2,"offsetDays":2,"hour":18,"minute":23,"dateYear":0,"dateMonth":0,"dateDay":0}},{"id":"qe-cal1-m3","type":"comms.dialogue","position":{"x":2340,"y":0},"data":{"kind":"mail","phone":{"branch":"default","startIndex":0,"continueMode":"onEnd"},"kisscord":{"contactId":"","messages":[]},"mail":{"from":"qe24-timer@test.net","to":"player@gomail.com","subject":"S-13 row fired: 1 month 2 weeks 2 days at 18:23","content":"<p>If you are reading this, the mixed-offset row was reached in real time. Usually you check it with 'qe24 timers' right after arming - the day and clock time it resolved to are printed there.</p>","replyable":false},"weechat":{"host":"","password":"","registerServer":true,"messages":[]},"postLive":false}}],"edges":[{"id":"qe-cal1-e0","source":"qe-cal1-start","sourceHandle":"out","target":"qe-cal1-n0","targetHandle":"in","kind":"flow"},{"id":"qe-cal1-e1","source":"qe-cal1-n0","sourceHandle":"out","target":"qe-cal1-t1","targetHandle":"in","kind":"flow"},{"id":"qe-cal1-e2","source":"qe-cal1-t1","sourceHandle":"out","target":"qe-cal1-m1","targetHandle":"in","kind":"flow"},{"id":"qe-cal1-e3","source":"qe-cal1-m1","sourceHandle":"out","target":"qe-cal1-n1","targetHandle":"in","kind":"flow"},{"id":"qe-cal1-e4","source":"qe-cal1-n1","sourceHandle":"out","target":"qe-cal1-t2","targetHandle":"in","kind":"flow"},{"id":"qe-cal1-e5","source":"qe-cal1-t2","sourceHandle":"out","target":"qe-cal1-m2","targetHandle":"in","kind":"flow"},{"id":"qe-cal1-e6","source":"qe-cal1-m2","sourceHandle":"out","target":"qe-cal1-n2","targetHandle":"in","kind":"flow"},{"id":"qe-cal1-e7","source":"qe-cal1-n2","sourceHandle":"out","target":"qe-cal1-t3","targetHandle":"in","kind":"flow"},{"id":"qe-cal1-e8","source":"qe-cal1-t3","sourceHandle":"out","target":"qe-cal1-m3","targetHandle":"in","kind":"flow"}]}},{"id":"qe-cal2","name":"QESdk024WaitMonthQa","title":"Timer QA (Wait in months: S-14, S-09)","description":"Auto-starts. First row: Wait 1 minute - fires a second later, so you know the chain ran. Second row: Wait 1 month - months have no field in the engine's duration form, so this is the scheduleAt path. Read it with 'qe24 timers'; on a day the next month has, it must land on the same day number one month on (that is S-09's claim as well).","icon":null,"group":"sandbox","rewards":{"money":0,"xp":0},"employer":{},"autoStart":false,"autoComplete":false,"abandonable":true,"hasCompleteButton":false,"hideObjectivesWhenDone":false,"closingObjectiveText":"","questsToComplete":[],"maxClaim":null,"maxClaimPerDay":null,"hackhubPost":null,"dialog":[],"graph":{"nodes":[{"id":"qe-cal2-start","type":"entry.start","position":{"x":0,"y":0},"data":{}},{"id":"qe-cal2-n0","type":"flow.debug","position":{"x":260,"y":0},"data":{"label":"Timer Wait QA armed - a 1 minute Wait first (S-01 path)","labelAuto":false,"toast":false,"includeData":false,"includePayload":false}},{"id":"qe-cal2-t1","type":"flow.timer","position":{"x":520,"y":0},"data":{"mode":"after","years":0,"months":0,"weeks":0,"days":0,"hours":0,"minutes":1,"offsetYears":0,"offsetMonths":0,"offsetWeeks":0,"offsetDays":0,"hour":0,"minute":0,"dateYear":0,"dateMonth":0,"dateDay":0}},{"id":"qe-cal2-m1","type":"comms.dialogue","position":{"x":780,"y":0},"data":{"kind":"mail","phone":{"branch":"default","startIndex":0,"continueMode":"onEnd"},"kisscord":{"contactId":"","messages":[]},"mail":{"from":"qe24-timer@test.net","to":"player@gomail.com","subject":"Wait 1 minute fired (S-01 path)","content":"<p>This is the engine's own duration form (schedule), the path S-01/S-02 verified. It arriving about a second after load is expected: one in-game minute is one real second at scale 60.</p>","replyable":false},"weechat":{"host":"","password":"","registerServer":true,"messages":[]},"postLive":false}},{"id":"qe-cal2-n1","type":"flow.debug","position":{"x":1040,"y":0},"data":{"label":"Now arming Wait 1 month (scheduleAt) - read 'qe24 timers'","labelAuto":false,"toast":false,"includeData":false,"includePayload":false}},{"id":"qe-cal2-t2","type":"flow.timer","position":{"x":1300,"y":0},"data":{"mode":"after","years":0,"months":1,"weeks":0,"days":0,"hours":0,"minutes":0,"offsetYears":0,"offsetMonths":0,"offsetWeeks":0,"offsetDays":0,"hour":0,"minute":0,"dateYear":0,"dateMonth":0,"dateDay":0}},{"id":"qe-cal2-m2","type":"comms.dialogue","position":{"x":1560,"y":0},"data":{"kind":"mail","phone":{"branch":"default","startIndex":0,"continueMode":"onEnd"},"kisscord":{"contactId":"","messages":[]},"mail":{"from":"qe24-timer@test.net","to":"player@gomail.com","subject":"Wait 1 month row fired now","content":"<p>If this arrived without a month passing, the month Wait behaved like an instant one - that is a finding, please report it.</p>","replyable":false},"weechat":{"host":"","password":"","registerServer":true,"messages":[]},"postLive":false}}],"edges":[{"id":"qe-cal2-e0","source":"qe-cal2-start","sourceHandle":"out","target":"qe-cal2-n0","targetHandle":"in","kind":"flow"},{"id":"qe-cal2-e1","source":"qe-cal2-n0","sourceHandle":"out","target":"qe-cal2-t1","targetHandle":"in","kind":"flow"},{"id":"qe-cal2-e2","source":"qe-cal2-t1","sourceHandle":"out","target":"qe-cal2-m1","targetHandle":"in","kind":"flow"},{"id":"qe-cal2-e3","source":"qe-cal2-m1","sourceHandle":"out","target":"qe-cal2-n1","targetHandle":"in","kind":"flow"},{"id":"qe-cal2-e4","source":"qe-cal2-n1","sourceHandle":"out","target":"qe-cal2-t2","targetHandle":"in","kind":"flow"},{"id":"qe-cal2-e5","source":"qe-cal2-t2","sourceHandle":"out","target":"qe-cal2-m2","targetHandle":"in","kind":"flow"}]}},{"id":"qe-tw1","name":"QESdk024TwotterQa","title":"Twotter QA (T-08/T-09/T-10/T-11/T-13)","description":"Claim it with 'qe24 run tw1'. The moment it starts, the editor's Twotter node posts a series from @qe24_editor: five tweets, four backdated (a year, three months, six weeks, twelve days) and one that posts as you watch. Open Twotter and search qe24_editor: T-08 is the face and the counts, T-09 is the order and the ages. Then save and reload (T-10), and finally open the profile and one post so both objectives tick — the Complete button appears then, and pressing it is T-11: the account and its posts must disappear.","icon":null,"group":"sandbox","rewards":{"money":0,"xp":0},"employer":{},"autoStart":false,"autoComplete":false,"abandonable":true,"hasCompleteButton":true,"hideObjectivesWhenDone":false,"closingObjectiveText":"Twotter QA recorded — the series was on @qe24_editor.","questsToComplete":[],"maxClaim":null,"maxClaimPerDay":null,"hackhubPost":null,"dialog":[],"graph":{"nodes":[{"id":"qe-tw1-start","type":"entry.start","position":{"x":0,"y":0},"data":{}},{"id":"qe-tw1-note","type":"flow.debug","position":{"x":240,"y":0},"data":{"label":"Twotter QA started: @qe24_editor now carries the series. Open Twotter and search for it.","labelAuto":false,"toast":false,"includeData":false,"includePayload":false}},{"id":"qe-tw1-post","type":"comms.tweet","position":{"x":480,"y":0},"data":{"accountId":"qe-tw-account","tweets":[{"id":"qe-tw1-row1","content":"A year ago today this account was the only thing that worked. T-09 wants this row reading 'a year ago'.","timeMode":"earlier","agoAmount":1,"agoUnit":"years","likes":96,"comments":7,"shares":4,"views":3120,"showInTimeline":true},{"id":"qe-tw1-row2","content":"Three months back: the manifest was sealed at 02:40 and nobody had signed for it.","timeMode":"earlier","agoAmount":3,"agoUnit":"months","likes":41,"comments":3,"shares":1,"views":1180,"showInTimeline":false},{"id":"qe-tw1-row3","content":"Six weeks back: an attaché case on a bench, and nobody came back for it. (Six weeks, not a month — the age should read 'a month ago' only if the game rounds it there.)","timeMode":"earlier","agoAmount":6,"agoUnit":"weeks","likes":64,"comments":2,"shares":3,"views":2040,"showInTimeline":false},{"id":"qe-tw1-row4","content":"Twelve days ago somebody asked the right question. We should not have ignored it.","timeMode":"earlier","agoAmount":12,"agoUnit":"days","likes":18,"comments":0,"shares":0,"views":430,"showInTimeline":false},{"id":"qe-tw1-row5","content":"This one posts the moment the quest runs, so it should read 'a few seconds ago'.","timeMode":"arrival","agoAmount":2,"agoUnit":"days","likes":2,"comments":0,"shares":0,"views":14,"showInTimeline":false}]}},{"id":"qe-tw1-seen-trigger","type":"trigger.event","position":{"x":760,"y":200},"data":{"event":"Twotter.ProfileSeen","conditions":[{"id":"qe-tw1-c-handle","join":"and","field":"username","op":"equals","value":"qe24_editor"}]}},{"id":"qe-tw1-obj-profile","type":"objective","position":{"x":1060,"y":200},"data":{"name":"profile-seen","description":"Open Twotter, search qe24_editor and open the profile. This row ticks here when Twotter.ProfileSeen reaches the quest.","hint":"On that screen: the bio, the BRIGHT VIOLET banner (#AA28FF), the AMBER avatar, followers 412 and following 96 must be the ones authored in the editor. Report anything missing or wrong, and whether the banner showed.","info":"r185 Twotter QA. Row meanings: reference/sdk-0.24-qa/STATUS.md.","terminalCommand":"","hidden":false}},{"id":"qe-tw1-postseen-trigger","type":"trigger.event","position":{"x":760,"y":380},"data":{"event":"Twotter.PostSeen","conditions":[{"id":"qe-tw1-c-user","join":"and","field":"userId","op":"equals","value":"qe-tw-account"}]}},{"id":"qe-tw1-obj-post","type":"objective","position":{"x":1060,"y":380},"data":{"name":"post-seen","description":"Open one of the series' posts. Ticks when Twotter.PostSeen fires for our account — T-13's yes. Both this and the profile row done means the game shows the Complete button (row T-11).","hint":"The post can be the newest one (posted as you arrived). Report whether this row ticked. Once both rows are ticked, the Complete button appears in the journal — press it for T-11.","info":"r185 Twotter QA. Row meanings: reference/sdk-0.24-qa/STATUS.md.","terminalCommand":"","hidden":false}}],"edges":[{"id":"qe-tw1-e1","source":"qe-tw1-start","sourceHandle":"out","target":"qe-tw1-note","targetHandle":"in","kind":"flow"},{"id":"qe-tw1-e2","source":"qe-tw1-note","sourceHandle":"out","target":"qe-tw1-post","targetHandle":"in","kind":"flow"},{"id":"qe-tw1-e3","source":"qe-tw1-seen-trigger","sourceHandle":"when","target":"qe-tw1-obj-profile","targetHandle":"trigger","kind":"condition"},{"id":"qe-tw1-e4","source":"qe-tw1-postseen-trigger","sourceHandle":"when","target":"qe-tw1-obj-post","targetHandle":"trigger","kind":"condition"}]}},{"id":"qe-tw2","name":"QESdk024TwotterShareQa","title":"Twotter QA (T-12: two quests, one account)","description":"T-12, the rule Zeis approved. This quest posts from the SAME account as 'Twotter QA (T-08...)': @qe24_editor. Claim both with 'qe24 run tw1' and 'qe24 run tw2', then watch what each completion does. Finishing the first quest while the other is still live must leave the account standing; finishing the second must remove it, whichever order you complete them in. Read the state after each step with 'qe24 twotter audit'.","icon":null,"group":"sandbox","rewards":{"money":0,"xp":0},"employer":{},"autoStart":false,"autoComplete":false,"abandonable":true,"hasCompleteButton":true,"hideObjectivesWhenDone":false,"closingObjectiveText":"Shared-account QA recorded.","questsToComplete":[],"maxClaim":null,"maxClaimPerDay":null,"hackhubPost":null,"dialog":[],"graph":{"nodes":[{"id":"qe-tw2-start","type":"entry.start","position":{"x":0,"y":0},"data":{}},{"id":"qe-tw2-note","type":"flow.debug","position":{"x":240,"y":0},"data":{"label":"Second quest posting from @qe24_editor (T-12's shared account).","labelAuto":false,"toast":false,"includeData":false,"includePayload":false}},{"id":"qe-tw2-post","type":"comms.tweet","position":{"x":480,"y":0},"data":{"accountId":"qe-tw-account","tweets":[{"id":"qe-tw2-row1","content":"Posted by the second quest that shares this account - T-12 wants it on the same profile.","timeMode":"arrival","agoAmount":2,"agoUnit":"days","likes":3,"comments":0,"shares":0,"views":9,"showInTimeline":false}]}},{"id":"qe-tw2-seen-trigger","type":"trigger.event","position":{"x":760,"y":200},"data":{"event":"Twotter.ProfileSeen","conditions":[{"id":"qe-tw2-c-handle","join":"and","field":"username","op":"equals","value":"qe24_editor"}]}},{"id":"qe-tw2-obj-profile","type":"objective","position":{"x":1060,"y":200},"data":{"name":"shared-profile-seen","description":"Open @qe24_editor's profile after this quest has posted: both quests' tweets must be on the one profile.","hint":"'qe24 twotter audit' lists @qe24_editor and whether its bio is still a string. Report what it says after each completion.","info":"r185 Twotter QA. Row meanings: reference/sdk-0.24-qa/STATUS.md.","terminalCommand":"","hidden":false}}],"edges":[{"id":"qe-tw2-e1","source":"qe-tw2-start","sourceHandle":"out","target":"qe-tw2-note","targetHandle":"in","kind":"flow"},{"id":"qe-tw2-e2","source":"qe-tw2-note","sourceHandle":"out","target":"qe-tw2-post","targetHandle":"in","kind":"flow"},{"id":"qe-tw2-e3","source":"qe-tw2-seen-trigger","sourceHandle":"when","target":"qe-tw2-obj-profile","targetHandle":"trigger","kind":"condition"}]}},{"id":"qe-tw3","name":"QESdk024TwotterPostEventQa","title":"Twotter QA (T-13: does Twotter.Post ever fire?)","description":"Optional, and it never completes anything. The plan says the game's Twotter.Post event is about a quest's own DECLARED tweets — the path the editor refuses to use — so opening one of our API-posted tweets should fire PostSeen and not Post. Claim it with 'qe24 run tw3' before browsing, open a post, and look at the journal: the row ticking by itself is the finding. The r185 run already answered this (it stayed unchecked), so this quest is here to keep the question testable.","icon":null,"group":"sandbox","rewards":{"money":0,"xp":0},"employer":{},"autoStart":false,"autoComplete":false,"abandonable":true,"hasCompleteButton":false,"hideObjectivesWhenDone":false,"closingObjectiveText":"","questsToComplete":[],"maxClaim":null,"maxClaimPerDay":null,"hackhubPost":null,"dialog":[],"graph":{"nodes":[{"id":"qe-tw3-start","type":"entry.start","position":{"x":0,"y":0},"data":{}},{"id":"qe-tw3-note","type":"flow.debug","position":{"x":240,"y":0},"data":{"label":"T-13 canary armed: open any Twotter post now and watch this quest's single objective.","labelAuto":false,"toast":false,"includeData":false,"includePayload":false}},{"id":"qe-tw3-post-trigger","type":"trigger.event","position":{"x":760,"y":0},"data":{"event":"Twotter.Post","conditions":[]}},{"id":"qe-tw3-obj-post-event","type":"objective","position":{"x":1060,"y":0},"data":{"name":"post-event-canary","description":"Expected to STAY unchecked: Twotter.Post is what the game fires for a quest's own declared tweets, and the editor does not emit those. If this row ever ticks by itself, say so — it changes what the manual may promise about When-event triggers.","hint":"Nothing to press: the whole point is that it cannot be completed by hand either. Report only if it ticks.","info":"r185/r186 Twotter QA. Row meaning: reference/sdk-0.24-qa/STATUS.md.","terminalCommand":"","hidden":false}}],"edges":[{"id":"qe-tw3-e1","source":"qe-tw3-start","sourceHandle":"out","target":"qe-tw3-note","targetHandle":"in","kind":"flow"},{"id":"qe-tw3-e2","source":"qe-tw3-post-trigger","sourceHandle":"when","target":"qe-tw3-obj-post-event","targetHandle":"trigger","kind":"condition"}]}},{"id":"qe-x1","name":"QESdk024ExtrasQa","title":"{{tr.qe24.quest.title}}","description":"Pack extras QA: the menu entry, the desktop widget, the right-click entries and the translated words.","icon":null,"group":"sandbox","rewards":{"money":0,"xp":0},"employer":{},"autoStart":false,"autoComplete":false,"abandonable":true,"hasCompleteButton":true,"hideObjectivesWhenDone":true,"closingObjectiveText":"","questsToComplete":[],"maxClaim":null,"maxClaimPerDay":null,"hackhubPost":null,"dialog":[],"graph":{"nodes":[{"id":"qe-x1-start","type":"entry.start","position":{"x":0,"y":0},"data":{}},{"id":"qe-x1-note","type":"fx.notify","position":{"x":320,"y":0},"data":{"message":"{{tr.qe24.quest.note}}","variant":"notify","tone":"info"}}],"edges":[{"id":"e-x1-1","source":"qe-x1-start","sourceHandle":"out","target":"qe-x1-note","targetHandle":"in","kind":"flow"}]}}],"websites":[{"id":"w-qe24-website","host":"qe24-website.test","name":"QE24 Website Harness","popular":false,"pages":[{"id":"p-home","path":"/","title":"QE24 HTTP event target","seo":true,"description":"Static editor website for SDK 0.24 HTTP/curl event QA.","search":["qe24","sdk","curl","http"],"template":"blank","content":"<h1>QE24 HTTP event target</h1><p>This page loading proves the static website exists. Now check the active quest tracker: did http-request and/or http-response get checkmarks?</p><ul><li><a href=\"/echo\">Static echo page</a></li><li><a href=\"/hidden/result\">Nested static page</a></li></ul>"},{"id":"p-echo","path":"/echo","title":"QE24 echo","seo":true,"description":"Second static page for repeated browser/curl probes.","search":["qe24","echo"],"template":"blank","content":"<h1>Echo</h1><p>After this page loads, check the quest tracker for http-request and http-response checkmarks.</p>"},{"id":"p-hidden","path":"/hidden/result","title":"QE24 nested result","seo":false,"description":"Hidden nested static page for path handling.","search":["qe24","nested"],"template":"blank","content":"<h1>Nested result</h1><p>If you can reach this with curl, static website path handling works.</p>"}]}],"twotterAccounts":[{"id":"qe-tw-account","handle":"qe24_editor","displayName":"QE24 Editor Account","bio":"Made by the editor's Twotter node (r185). Banner must be BRIGHT VIOLET (#AA28FF), avatar AMBER - both authored in the editor.","avatar":"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAADAAAAAwCAIAAADYYG7QAAAAN0lEQVR42u3OMQ0AAAgDsOnHNHNBOJpUQLOTVyIkJCQkJCQkJCQkJCQkJCQkJCQkJCQkJCQkdKcsm9HE2Fnb/QAAAABJRU5ErkJggg==","banner":"data:image/png;base64,","verified":true,"followers":412,"following":96,"removeWhenQuestEnds":true}],"extras":{"menuItems":[{"id":"qe24-menu-extras","label":"{{tr.qe24.menu.label}}","icon":"","action":{"kind":"notify","text":"{{tr.qe24.menu.message}} Plain text after the translated half.","questId":"","mailFrom":"","mailSubject":"","mailContent":"","handbookId":"","handbookCategory":""}}],"widgets":[{"id":"qe24-extras-widget","src":"widgets/qe24-extras-widget.html","width":320,"height":180,"x":40,"y":40,"transparent":false}],"contextItems":[{"id":"qe24-ctx-file","label":"{{tr.qe24.ctx.file}}","icon":"","target":"file","action":{"kind":"notify","text":"Right-click on a file works (T-25a).","questId":"","mailFrom":"","mailSubject":"","mailContent":"","handbookId":"","handbookCategory":""}},{"id":"qe24-ctx-desktop","label":"{{tr.qe24.ctx.desktop}}","icon":"","target":"desktop","action":{"kind":"notify","text":"Right-click on the desktop works (T-25b).","questId":"","mailFrom":"","mailSubject":"","mailContent":"","handbookId":"","handbookCategory":""}}]},"translations":{"languages":["en","de"],"strings":{"en":{"qe24.menu.label":"QE24: extras check","qe24.menu.message":"The pack's own menu entry works (T-23).","qe24.ctx.file":"QE24: inspect this file","qe24.ctx.desktop":"QE24: desktop action","qe24.quest.title":"QE24 extras QA","qe24.quest.note":"The extras QA quest started (T-27)."},"de":{"qe24.menu.label":"QE24: Extras-Prüfung","qe24.menu.message":"Der eigene Menüeintrag des Packs funktioniert (T-23).","qe24.ctx.file":"QE24: diese Datei prüfen","qe24.ctx.desktop":"QE24: Desktop-Aktion","qe24.quest.title":"QE24 Extras-Prüfung","qe24.quest.note":"Die Extras-Prüfquest wurde gestartet (T-27)."}}}};
var __QE_BUILD = "2026-09-19.r203";

var __QE = (function () {
    /* The token scope for anything OUTSIDE a quest (r203): pack extras are
       registered at mod level, so they have no quest Data. The getters are the
       same lazy ones the quest scope uses - computed only if a token asks, and
       never allowed to throw, because an eager SDK call once cost a mod its
       whole quest (see dataScope). */
    function tokenScope() {
        return {
            player: {
                get ip() { return __QE.safe(function () { return sdk.Network && sdk.Network.getPlayerIp ? sdk.Network.getPlayerIp() : ""; }); },
                get email() { return __QE.safe(function () { return sdk.Mail && sdk.Mail.getPlayerEmail ? sdk.Mail.getPlayerEmail() : ""; }); },
                get username() { return __QE.safe(function () { return sdk.Shell && sdk.Shell.getUsername ? sdk.Shell.getUsername() : ""; }); },
            },
            random: {
                get password() { return __QE.safe(function () { return sdk.Random && sdk.Random.password ? sdk.Random.password() : ""; }); },
                get ip() { return __QE.safe(function () { return sdk.Network && sdk.Network.randomIp ? sdk.Network.randomIp() : ""; }); },
                get username() { return __QE.safe(function () { return sdk.Random && sdk.Random.username ? sdk.Random.username() : ""; }); },
            },
        };
    }

    function getPath(obj, path) {
        return String(path).split(".").reduce(function (acc, k) {
            return acc == null ? acc : acc[k];
        }, obj);
    }
    function fill(tpl, scope) {
        return String(tpl).replace(/\{\{([^}]+)\}\}/g, function (_m, p) {
            var key = p.trim();
            /* {{tr.some.key}} - translated text, wherever the author typed it.
               The SDK's t() falls back to English, then to the key itself, so a
               missing line is visible on screen instead of silently blank; if
               the build has no Localization at all, showing the key is still
               more honest than showing nothing. */
            if (key.indexOf("tr.") === 0) {
                var tkey = key.slice(3);
                /* NOT __QE.safe here: it turns null into "", which would erase
                   the difference between "no translation" and "empty text" and
                   leave a blank on screen. A failure has to stay a failure so
                   the key can be shown instead. */
                var out = null;
                try {
                    out = sdk.Localization && sdk.Localization.t ? sdk.Localization.t(tkey) : null;
                } catch (eTr) {
                    out = null;
                }
                if (out == null || out === "") return tkey;
                return String(out);
            }
            var v = getPath(scope, key);
            return v == null ? "" : String(v);
        });
    }
    /* The same translation lookup as fill()'s {{tr.}} branch, exposed for the
       fields the game reads ONCE, at registration: a quest's Title and
       Description. Everything else is filled when it is used, but those two are
       taken off the definition while it is handed over - so the token has to be
       resolved before that, and there is no quest Data to offer at that point. */
    function fillTranslations(text) {
        return String(text == null ? "" : text).replace(/\{\{tr\.([^}]+)\}\}/g, function (_m, k) {
            var tkey = String(k).trim();
            var out = null;
            try {
                out = sdk.Localization && sdk.Localization.t ? sdk.Localization.t(tkey) : null;
            } catch (eTr) {
                out = null;
            }
            if (out == null || out === "") return tkey;
            return String(out);
        });
    }
    function asString(x) { return x == null ? "" : String(x); }
    /* GoMail renders a mail body as plain text, so any HTML in it is shown
       literally - QA got a briefing that read "<p>His name is <b>Anselm
       Ritter</b>." on screen. The editor's mail field is a rich-text box that
       produces HTML, so the two have to be reconciled here: turn the block
       tags into line breaks, drop the inline ones, and unescape the entities.

       Nemesis, whose mail displays correctly, sends plain text with blank
       lines between paragraphs. That is what this produces. */
    function htmlToText(html) {
        var s = String(html == null ? "" : html);
        if (s.indexOf("<") < 0 && s.indexOf("&") < 0) return s;
        s = s.replace(/<\s*(script|style)[^>]*>[\s\S]*?<\s*\/\s*\1\s*>/gi, "");
        s = s.replace(/<\s*br\s*\/?\s*>/gi, "\n");
        s = s.replace(/<\s*\/\s*(p|div|h[1-6]|li|tr|blockquote)\s*>/gi, "\n\n");
        s = s.replace(/<\s*li[^>]*>/gi, "\u2022 ");
        s = s.replace(/<\s*hr[^>]*\/?\s*>/gi, "\n----------\n");
        s = s.replace(/<[^>]+>/g, "");
        s = s.replace(/&nbsp;/gi, " ")
             .replace(/&lt;/gi, "<")
             .replace(/&gt;/gi, ">")
             .replace(/&quot;/gi, "\"")
             .replace(/&#39;/g, "'")
             .replace(/&apos;/gi, "'")
             .replace(/&amp;/gi, "&");
        /* Collapse the runs of blank lines the block rules leave behind, and
           trim the ends, without touching deliberate spacing inside. */
        s = s.replace(/[ \t]+\n/g, "\n").replace(/\n{3,}/g, "\n\n");
        return s.replace(/^\s+|\s+$/g, "");
    }
    /* Turn an ISO-ish date (yyyy-mm-dd) into an age phrase ("3 days",
       "2 months", "1 year"). Returns "" for a blank or unparseable date. */
    function ageStringFromDate(value) {
        if (!value) return "";
        var then = new Date(value);
        if (isNaN(then.getTime())) return "";
        var secs = Math.floor((Date.now() - then.getTime()) / 1000);
        if (secs < 60) return "just now";
        var units = [
            ["year", 31536000],
            ["month", 2592000],
            ["week", 604800],
            ["day", 86400],
            ["hour", 3600],
            ["minute", 60],
        ];
        for (var i = 0; i < units.length; i++) {
            var n = Math.floor(secs / units[i][1]);
            if (n >= 1) return n + " " + units[i][0] + (n === 1 ? "" : "s");
        }
        return "just now";
    }
    /* Text coming back from the game is whatever the player typed, and the
       value beside it is whatever the author typed. Matching those two byte
       for byte fails for reasons that have nothing to do with the story:
       lynx "Anselm Ritter" and lynx Anselm Ritter raise the same event with a
       different query string, and nobody expects a capital letter to decide
       whether an objective ticks. So text comparisons are case-insensitive,
       trimmed, and blind to surrounding quotes. Numeric and regex comparisons
       are untouched - a regex means the author wants exact control. */
    function loose(x) {
        var t = asString(x).trim();
        var first = t.charAt(0);
        var last = t.charAt(t.length - 1);
        if (t.length > 1 && (first === "\"" || first === "'") && last === first) t = t.slice(1, -1).trim();
        return t.toLowerCase();
    }

    /* Read the field a condition names off an event payload.

       Older SDKs typed Terminal.Lynx.Search as { query: string }, so the
       editor offered "query" and projects saved that field. The game emitted
       the search term as a BARE STRING, so asking the payload for .query gave
       undefined and the objective never ticked. SDK 0.24.0 corrected that
       declaration, but old projects still need to run.

       Primitive events are a legitimate shape the author still has to be able
       to match on. When the payload is a primitive, any field name resolves to
       the payload itself - which is the only value there is, and certainly what
       the author meant. */
    function fieldOf(payload, field) {
        if (payload != null && typeof payload !== "object") return payload;
        var direct = getPath(payload, field);
        if (direct !== undefined) return direct;
        /* An object with exactly one primitive value is unambiguous too: a
           mismatched field name should not be the difference between a quest
           that works and one that stops dead. */
        if (payload && typeof payload === "object" && !Array.isArray(payload)) {
            var keys = Object.keys(payload);
            if (keys.length === 1 && typeof payload[keys[0]] !== "object") return payload[keys[0]];
        }
        return direct;
    }

    function matchClause(c, payload, scope) {
        var raw = fieldOf(payload, c.field);
        var val = fill(c.value, scope);
        var a = loose(raw);
        var b = loose(val);
        switch (c.op) {
            case "equals": return a === b;
            case "notEquals": return a !== b;
            case "contains": return a.indexOf(b) >= 0;
            case "notContains": return a.indexOf(b) < 0;
            case "startsWith": return a.indexOf(b) === 0;
            case "endsWith": return a.slice(a.length - b.length) === b;
            case "matches": try { return new RegExp(val).test(asString(raw)); } catch (e) { return false; }
            case "exists": return raw !== undefined;
            case "notEmpty": return asString(raw).length > 0;
            case "greaterThan": return Number(raw) > Number(val);
            case "lessThan": return Number(raw) < Number(val);
            default: return false;
        }
    }
    function matchAll(list, payload, scope) {
        var acc = true;
        for (var i = 0; i < list.length; i++) {
            var r = matchClause(list[i], payload, scope);
            acc = i === 0 ? r : (list[i].join === "or" ? (acc || r) : (acc && r));
        }
        return acc;
    }
    function matchInput(input, answer) {
        var a = input.caseSensitive ? String(answer) : String(answer).toLowerCase();
        var e = input.caseSensitive ? input.expected : String(input.expected).toLowerCase();
        if (!e) return true;
        if (input.matchMode === "contains") return a.indexOf(e) >= 0;
        if (input.matchMode === "regex") {
            try { return new RegExp(e).test(a); } catch (err) { return false; }
        }
        return a === e;
    }
    function matchPrompt(input, answer, scope) {
        var mode = input.matchMode || "any";
        if (mode === "any") return true;
        var expected = fill(input.expected || "", scope);
        if (String(expected).trim().length === 0) return false;
        var a = input.caseSensitive ? String(answer) : String(answer).toLowerCase();
        var e = input.caseSensitive ? String(expected) : String(expected).toLowerCase();
        if (mode === "contains") return a.indexOf(e) >= 0;
        if (mode === "regex") {
            try { return new RegExp(String(expected), input.caseSensitive ? "" : "i").test(String(answer)); } catch (err) { return false; }
        }
        return a === e;
    }
    /* Never let an optional lookup take a quest down with it: a missing
       permission, a missing API or a throwing getter all become "". */
    function safe(fn) {
        try {
            var v = fn();
            return v == null ? "" : v;
        } catch (e) {
            return "";
        }
    }
    function log(msg) {
        try { if (typeof console !== "undefined" && console.log) console.log("[quest-editor] " + msg); } catch (e) { /* nothing to do */ }
    }
    /* A wait that never depends on the game's clock. sleep() prefers
       Random.sleep so story beats obey game time, but anything that has to
       happen for certain (checking whether a mail actually arrived) uses this
       instead: if Random.sleep were ever paused, stubbed or slow, a
       verify-and-repair step hanging on it would silently never run. */
    function wait(ms) {
        return new Promise(function (res) { setTimeout(res, ms); });
    }
    /* Run fn over list in order, staying SYNCHRONOUS until something
       actually returns a promise.

       This exists because of how the engine scopes a mod's permissions: a mod
       is only "current" while the engine is inside a call it made. Work pushed
       into a microtask happens after that call has returned, by which point
       the SDK no longer knows who is calling and refuses everything with
       Mod "null". A plain reduce over Promise.resolve() defers even purely
       synchronous work, so the entire quest graph was running too late. */
    /* A short, readable rendering of an event payload, for the log. Keeps the
       whole thing to one line and one screenful: the point is to show which
       fields exist and roughly what is in them, not to dump the object. */
    function describe(data) {
        if (data == null) return String(data);
        if (typeof data !== "object") return JSON.stringify(data);
        var parts = [];
        for (var k in data) {
            if (!Object.prototype.hasOwnProperty.call(data, k)) continue;
            var v = data[k];
            var shown;
            if (v == null) shown = String(v);
            else if (typeof v === "object") shown = Array.isArray(v) ? "[" + v.length + " items]" : "{object}";
            else {
                shown = String(v);
                if (shown.length > 60) shown = shown.slice(0, 57) + "...";
                shown = JSON.stringify(shown);
            }
            parts.push(k + "=" + shown);
        }
        return parts.length ? "{ " + parts.join(", ") + " }" : "{ no fields }";
    }

    function seq(list, fn, onError) {
        var i = 0;
        function step() {
            while (i < list.length) {
                var item = list[i++];
                var r;
                try {
                    r = fn(item);
                } catch (e) {
                    if (onError) onError(e);
                    continue;
                }
                /* Only pay for a promise when the node really is async. */
                if (r && typeof r.then === "function") {
                    return r.then(step, function (e) {
                        if (onError) onError(e);
                        return step();
                    });
                }
            }
            return undefined;
        }
        return step();
    }

    function sleep(ms) {
        /* Prefer the game's own timer (SDK 0.21.0 Random.sleep) so waits obey
           whatever the game does with time; fall back to a plain timeout when
           the API is not there. */
        try {
            if (typeof sdk !== "undefined" && sdk && sdk.Random && sdk.Random.sleep) {
                return Promise.resolve(sdk.Random.sleep(ms));
            }
        } catch (e) { /* fall through to the timeout below */ }
        return new Promise(function (res) { setTimeout(res, ms); });
    }
    /* Community packs (r137/r138): substitute a pack template's "{{field}}"
       holes with the author's form answers, then resolve story tokens, then
       parse. String holes splice with JSON escaping but NO surrounding quotes
       (the hole already sits inside a JSON string); a number/boolean hole
       fills raw when it is the whole value, else as text. */
    function packText(scope, fields, values, text) {
        var t = String(text == null ? "" : text);
        (fields || []).forEach(function (f) {
            var v = (values || {})[f.key];
            if (v === undefined || v === null) return;
            t = t.split("{{" + f.key + "}}").join(String(v));
        });
        return fill(t, scope);
    }
    function packFill(scope, fields, values, template) {
        var json = JSON.stringify(template == null ? {} : template);
        (fields || []).forEach(function (f) {
            var v = (values || {})[f.key];
            if (v === undefined || v === null) return;
            var out;
            if (f.type === "number") {
                out = String(Number(v));
                if (out === "NaN") out = "0";
            } else if (f.type === "boolean") {
                out = v === "true" ? "true" : "false";
            } else {
                out = JSON.stringify(String(v)).slice(1, -1);
            }
            var hole = "{{" + f.key + "}}";
            if (f.type !== "string" && f.type !== "text") {
                /* A number or boolean fills its hole raw - but only when the
                   hole IS the whole value; the template's quotes would turn
                   "8080" into a string. Embedded holes splice as text. */
                var whole = JSON.stringify(hole);
                if (json.indexOf(whole) !== -1) json = json.split(whole).join(out);
                else json = json.split(hole).join(out);
            } else {
                json = json.split(hole).join(out);
            }
        });
        return JSON.parse(fill(json, scope));
    }
    return { getPath: getPath, fill: fill, fillTranslations: fillTranslations, tokenScope: tokenScope, packText: packText, packFill: packFill, htmlToText: htmlToText, matchAll: matchAll, matchInput: matchInput, matchPrompt: matchPrompt, sleep: sleep, seq: seq, describe: describe, wait: wait, ageStringFromDate: ageStringFromDate, safe: safe, log: log };
})();

function __qeRegisterProject(sdk, PROJECT) {

    /* ── scheduled timers (SDK 0.24 Time/Scheduler) ──────────────────────
       A Timer arms a job on the in-game clock when the story reaches the
       node; when the clock hits the due time the job fires and the node's
       "Out" wire runs (r172, renamed r173).

       The kind registry is SHARED across every installed pack (SDK docs):
       two mods both using "timer" would silently answer each other's
       jobs, so the mod id goes into the kind. The handler must exist on
       every load, before anything schedules - hence here, at the top of
       mod load, before any quest starts. */
    var BEAT_KIND = "qe/" + String((PROJECT && PROJECT.mod && (PROJECT.mod.id || PROJECT.mod.name)) || "editor-mod") + "/timer";
    /* questId -> fire(nodeId). Rebound by each quest's OnStart /
       OnObjectivesStart to whichever instance the engine actually runs. */
    var liveBeats = {};
    /* questId -> job ids that quest armed and has not cancelled. Kept here,
       above the quest factory, because the job handler below runs outside any
       quest's closure and needs to drop a job the moment it fires. */
    var beatJobsByQuest = {};
    /* The job ids we armed, minus the ones that have already fired. Leaving a
       fired id in the list made the cancel line overstate itself — "cancelled 2
       pending timer(s)" with only one left, in the very log a tester is told to
       trust (S-03, 2026-09-18). */
    function forgetBeatJob(questId, id) {
        var list = beatJobsByQuest[questId];
        if (!list || !id) return;
        for (var i = 0; i < list.length; i++) {
            if (list[i] === id) {
                list.splice(i, 1);
                return;
            }
        }
    }
    /* How many times a due job may be re-armed while its quest is not
       live yet (the engine can fire due jobs at mod load, before quest
       start). A quest that auto-starts on load is live within a few
       ticks; anything still missing after this many re-arms is dropped
       with a log line instead of looping forever. */
    var BEAT_REARM_MAX = 20;
    if (sdk.Scheduler && sdk.Scheduler.register) {
        sdk.Scheduler.register(BEAT_KIND, function (payload, job) {
            var p = payload || {};
            if (job) forgetBeatJob(p.questId, job.id);
            var fire = liveBeats[p.questId];
            if (!fire) {
                if ((p.attempts || 0) < BEAT_REARM_MAX) {
                    /* Not live yet: re-arm for the next engine tick. */
                    sdk.Scheduler.schedule(BEAT_KIND, { questId: p.questId, nodeId: p.nodeId, attempts: (p.attempts || 0) + 1 }, { ms: 10 });
                    return;
                }
                __QE.log("timer missed: quest " + p.questId + " is not active in this session");
                return;
            }
            fire(p.nodeId);
        });
        __QE.log("timer handler registered (kind " + BEAT_KIND + ")");
    } else {
        __QE.log("no Scheduler API in this game build - timers will not fire");
    }

    /* ── Twotter (r185) ──────────────────────────────────────────────────
       Accounts are mod-level data (PROJECT.twotterAccounts); the Tweet node
       posts from one of them. Everything an account needs happens through the
       platform API here, never through the declarative TwotterAccounts /
       Tweets quest fields — those are the fields the engine writes with
       bio: undefined, which crashed the game's Twotter search for seven QA
       rounds and got the whole feature removed in r31.

       What the r179 probe established and this code relies on:

       - createUser() fills a COMPLETE record (name, surname, avatar,
         banner, joinedAt, password) for the fields we cannot express, so we
         hand it the ones we own and let it fill the rest.
       - A bio we send as "" stays a string. Nothing may ever send
         undefined - that is the whole bug.
       - removeUser() deletes accounts, quest-declared ones included.
       - postTweet() posts, and Twotter.PostSeen fires for the player.

       And what the P-01 probes added (2026-09-18):

       - A sendedAt we send is KEPT: all three spellings read back "a month
         ago", while a tweet with no time read "a few seconds ago". So a
         backdated series is possible, and the runtime sends ISO with
         milliseconds computed from the in-game clock.
       - A profile shows the newest tweet first, whatever order we posted in.
         We post oldest -> newest anyway (deterministic, and it matches the
         author's list), and the display sorts itself.

       Ownership, in the words of the rule the author approved: a quest removes
       only what it created, and only when no other live quest declares the
       same account. An account we merely ADOPTED (it already existed) is never
       ours to remove. The SDK asks a mod to take its accounts with it when the
       package unloads, and the unload hook below does that. Measured in game on
       2026-09-19: when the player DISABLES this mod in the Mods list, the game
       applies that at the next start, calls the hook, and the accounts go (the
       removal stuck across the reload). A plain quit calls no hook - nothing
       needs cleaning then - and a mod deleted from disk while the game is closed
       can never run any code of ours, so that one case still leaves the accounts
       in the save (question 11). */
    var TWOTTER_ACCOUNTS = PROJECT.twotterAccounts || [];
    var twotterReady = !!(sdk.Twotter && sdk.Twotter.createUser && sdk.Twotter.addUser);
    /* accountId -> questId that created it in this session. */
    var twotterCreatedBy = {};
    /* accountId -> true when the account already existed and we only refreshed
       it. Adopted accounts belong to someone else (another mod, or the player)
       and are never removed by us. */
    var twotterAdopted = {};
    /* questId -> true once the quest has been seen to start (OnStart or
       OnObjectivesStart) and -> true once it has completed or been abandoned.
       A quest only counts as LIVE while it is started and not ended, which is
       the rule the author approved. An account a quest declares but has never
       needed does not hold it: a mod whose act I is abandoned leaves an empty
       profile behind otherwise, and act II brings the account back itself when
       it starts (see ensureDeclaredTwotterAccounts). */
    var twotterQuestLive = {};
    var twotterQuestEnded = {};
    /* accountId -> [questId] for every quest whose Tweet nodes post from it.
       Read from PROJECT's graphs, so it covers quests that have not started. */
    var twotterDeclaredBy = {};
    (PROJECT.quests || []).forEach(function (q) {
        ((q.graph && q.graph.nodes) || []).forEach(function (n) {
            if (n.type !== "comms.tweet" || !n.data || !n.data.accountId) return;
            var list = twotterDeclaredBy[n.data.accountId] || (twotterDeclaredBy[n.data.accountId] = []);
            if (list.indexOf(q.id) < 0) list.push(q.id);
        });
    });

    function twotterAccount(id) {
        for (var i = 0; i < TWOTTER_ACCOUNTS.length; i++) {
            if (TWOTTER_ACCOUNTS[i].id === id) return TWOTTER_ACCOUNTS[i];
        }
        return null;
    }

    function twotterHandle(account) {
        var handle = String(account.handle == null ? "" : account.handle).replace(/^@+/, "");
        return handle.replace(/ +/g, "_");
    }

    /* A display name is one line to the author; the platform stores a first
       name and a surname, so the first word is the first name and the rest is
       the surname ("Justyna Kuznetsova" -> "Justyna" / "Kuznetsova"). */
    function twotterSplitName(displayName) {
        var parts = String(displayName == null ? "" : displayName).trim().split(" ");
        var words = [];
        for (var i = 0; i < parts.length; i++) {
            if (parts[i]) words.push(parts[i]);
        }
        if (!words.length) return { first: "", last: "" };
        return { first: words[0], last: words.slice(1).join(" ") };
    }

    /* Create the account, or adopt the one already carrying this handle.

       The id is the editor's own account id, which makes creation idempotent
       across reloads: a second call finds the account and refreshes it instead
       of making a twin. */
    function ensureTwotterAccount(account, questId) {
        if (!twotterReady || !account) return null;
        var handle = twotterHandle(account);
        if (!handle) {
            __QE.log("twotter: account " + account.id + " has no handle; skipping it");
            return null;
        }
        var existing = null;
        try {
            if (sdk.Twotter.getUserByUsername) existing = sdk.Twotter.getUserByUsername(handle);
        } catch (e) {
            __QE.log("twotter: looking up @" + handle + " failed (continuing): " + e);
        }
        var name = twotterSplitName(account.displayName);
        var fields = {
            username: handle,
            firstName: name.first,
            lastName: name.last,
            name: name.first,
            surname: name.last,
            /* ALWAYS a string. A blank bio is "", never undefined — the exact
               shape that crashed the game's search. */
            bio: account.bio == null ? "" : String(account.bio),
            verified: !!account.verified,
            followers: Math.max(0, Math.round(Number(account.followers) || 0)),
            following: Math.max(0, Math.round(Number(account.following) || 0)),
        };
        /* Author-supplied pictures are passed through when they exist, and left
           out entirely when they do not, so the engine's own default is used
           rather than an empty string (r20: an empty avatar was what first
           broke the profile screen). */
        if (account.avatar) fields.avatar = account.avatar;
        if (account.banner) fields.banner = account.banner;

        if (existing) {
            /* Ours or somebody else's? A record carrying the id we create with
               is the account this mod registered — in this session, or in one
               before the last reload — and it stays ours to clean up. Anything
               else is a genuine adoption: the player, or another mod, had that
               handle first, and it is not ours to delete. */
            var ours = String(existing.id) === String(account.id);
            if (ours) {
                if (!twotterCreatedBy[account.id]) twotterCreatedBy[account.id] = questId;
            } else {
                twotterAdopted[account.id] = true;
            }
            try {
                if (sdk.Twotter.updateUser) sdk.Twotter.updateUser(existing.id, fields);
                __QE.log(ours
                    ? "twotter: refreshed @" + handle + " (our account, already in the save)"
                    : "twotter: adopted @" + handle + " (already existed); author fields refreshed");
            } catch (e) {
                __QE.log("twotter: refreshing @" + handle + " failed (continuing): " + e);
            }
            return existing.id;
        }
        try {
            var user = sdk.Twotter.createUser({
                id: account.id,
                username: fields.username,
                firstName: fields.firstName,
                lastName: fields.lastName,
                bio: fields.bio,
                verified: fields.verified,
                followers: fields.followers,
                following: fields.following,
                avatar: fields.avatar,
                banner: fields.banner,
            });
            sdk.Twotter.addUser(user);
            twotterCreatedBy[account.id] = questId;
            __QE.log("twotter: created @" + handle + " (" + account.id + ") for quest " + questId);
            return account.id;
        } catch (e) {
            __QE.log("twotter: creating @" + handle + " failed (the story continues): " + e);
            return null;
        }
    }

    /* Every account the mod declares, ensured once. Called from the quest's
       start hooks: the first quest that needs an account brings it into the
       world, and a later quest that needs the same one simply finds it. */
    function ensureDeclaredTwotterAccounts(questId) {
        if (!twotterReady) {
            __QE.log("twotter: no Twotter API in this game build - accounts are not registered");
            return;
        }
        var accountIds = Object.keys(twotterDeclaredBy);
        for (var i = 0; i < accountIds.length; i++) {
            /* The map is accountId -> [questId]. Until r186 this loop walked the
               account ids and searched THEM for the quest id, so it never
               matched and never ran: the only thing that ever created an
               account was a tweet node, which meant a quest whose posting was
               skipped (a re-run after the account had been removed) came back
               with no account at all. */
            var declarers = twotterDeclaredBy[accountIds[i]] || [];
            if (declarers.indexOf(questId) < 0) continue;
            ensureTwotterAccount(twotterAccount(accountIds[i]), questId);
        }
    }

    function removeTwotterAccount(accountId, why) {
        if (!twotterReady || !sdk.Twotter.removeUser) return;
        try {
            var gone = sdk.Twotter.removeUser(accountId);
            __QE.log("twotter: removeUser(" + accountId + ") -> " + gone + (why ? " (" + why + ")" : ""));
            if (gone) {
                delete twotterCreatedBy[accountId];
                delete twotterAdopted[accountId];
            }
        } catch (e) {
            __QE.log("twotter: removing " + accountId + " failed (continuing): " + e);
        }
    }

    /* A quest has finished (complete or abandon): the accounts that quest
       declared may have become unneeded — take back the ones this mod created
       and no live quest still declares.

       Note which accounts are looked at: the ones THIS quest declares, not the
       ones it created. The creator and the last declarer are usually different
       quests (quest A brings a character into the world, quest B still posts
       from it), and an account is only due for removal when the LAST quest that
       needs it ends — whoever that turns out to be. Three things keep an
       account: the author asked for it to outlive the story, another live quest
       still declares it, or it was not ours (we adopted it). */
    function releaseTwotterAccounts(questId) {
        twotterQuestEnded[questId] = true;
        if (!twotterReady) return;
        var ids = Object.keys(twotterDeclaredBy);
        for (var i = 0; i < ids.length; i++) {
            var accountId = ids[i];
            var declared = twotterDeclaredBy[accountId] || [];
            var mine = false;
            for (var k = 0; k < declared.length; k++) {
                if (declared[k] === questId) { mine = true; break; }
            }
            if (!mine) continue;
            if (twotterAdopted[accountId] || !twotterCreatedBy[accountId]) continue;
            var account = twotterAccount(accountId);
            if (!account) continue;
            if (account.removeWhenQuestEnds === false) {
                __QE.log("twotter: keeping @" + twotterHandle(account) + " - the author asked for it to outlive the quest");
                continue;
            }
            var stillNeeded = false;
            var keptBy = "";
            for (var j = 0; j < declared.length; j++) {
                var other = declared[j];
                if (other === questId) continue;
                if (twotterQuestLive[other] && !twotterQuestEnded[other]) {
                    stillNeeded = true;
                    keptBy = other;
                    break;
                }
            }
            if (stillNeeded) {
                __QE.log("twotter: keeping @" + twotterHandle(account) + " - quest " + keptBy + " is still live and declares it");
                continue;
            }
            removeTwotterAccount(accountId, "the last quest that needs it ended");
        }
    }

    /* The game unloading this package (the SDK's own example for it is
       "disabled by user"): accounts a mod adds live in the player's save and are
       NOT removed when the mod goes away, so this is where the mod takes its
       characters with it. Adopted accounts are left alone - they were never
       ours - and everything gets a log line, because the alternative is an
       account nobody can explain.

       Measured 2026-09-19 (game 1.3.1): disabling the mod in the Mods list is
       applied by the game at the next start, and the hook fires there -
       "unloading: removing the Twotter accounts this mod declared" then
       "twotter: removeUser(qe-tw-account) -> true (mod unloaded)", with the
       account gone from the save at the following load. A plain quit to desktop
       calls no hook at all, and the account survives that (nothing needed
       cleaning, so nothing was wrong). Removing the mod from disk while the game
       is closed is the one case no mod code can ever reach: the game prunes the
       orphaned quest by itself ("[PruneOrphanQuests] ... no installed content
       defines it") but leaves the accounts. */
    function removeAllTwotterAccounts() {
        if (!twotterReady || !sdk.Twotter.removeUser) return;
        for (var i = 0; i < TWOTTER_ACCOUNTS.length; i++) {
            var account = TWOTTER_ACCOUNTS[i];
            if (twotterAdopted[account.id]) {
                __QE.log("twotter: leaving @" + twotterHandle(account) + " - we adopted it, it was not ours");
                continue;
            }
            var exists = null;
            try {
                exists = sdk.Twotter.getUserByUsername ? sdk.Twotter.getUserByUsername(twotterHandle(account)) : null;
            } catch (e) {
                exists = null;
            }
            /* Only the record we registered: if somebody else is carrying the
               handle now, that account is not ours to delete. */
            if (!exists || String(exists.id) !== String(account.id)) continue;
            removeTwotterAccount(exists.id, "mod unloaded");
        }
    }

    /* ── one quest ─────────────────────────────────────────────────────── */
    function registerQuest(qd) {
        var g = qd.graph;
        var byId = {};
        g.nodes.forEach(function (n) { byId[n.id] = n; });
        function outs(id) {
            return g.edges.filter(function (e) { return e.source === id; });
        }
        function flowOuts(id) {
            return outs(id).filter(function (e) { return e.kind === "flow"; });
        }

        var mailNodes = g.nodes.filter(function (n) { return n.type === "comms.dialogue" && n.data.kind === "mail"; });
        var mailIndex = {};
        var mailFrom = {};
        mailNodes.forEach(function (n, i) {
            mailIndex[n.id] = i;
            if (n.data.mail.from) mailFrom[n.id] = n.data.mail.from;
        });

        var Mails = mailNodes.map(function (n) {
            var m = n.data.mail;
            var out = { title: m.subject, content: __QE.htmlToText(m.content) };
            if (m.replyable) out.replyable = true;
            if (m.attachment && m.attachment.name) out.attachment = m.attachment;
            return out;
        });

        function mapDialogOption(o, scope, continueFlow, markCallback) {
            var oo = { label: scope ? __QE.fill(o.label || "", scope) : (o.label || "") };
            if (o.text) oo.text = scope ? __QE.fill(o.text, scope) : o.text;
            if (o.switchBranch) oo.switchBranch = o.switchBranch;
            if (o.nextIndex != null) oo.nextIndex = Number(o.nextIndex);
            if (o.timeout != null) oo.timeout = Number(o.timeout);
            if (o.isEnd) oo.isEnd = true;
            if (continueFlow && o.isEnd) {
                oo.onSelect = continueFlow;
                if (markCallback) markCallback();
            }
            return oo;
        }

        function dialogLineEndsCall(l, index, lines) {
            if (l.options && l.options.length) return false;
            return !!l.isEnd || index >= lines.length - 1;
        }

        function mapDialogLine(l, scope, continueFlow, index, lines, markCallback) {
            var out = {
                speaker: l.speaker,
                text: scope ? __QE.fill(l.text || "", scope) : (l.text || ""),
            };
            if (l.audio) out.audio = l.audio;
            if (l.timeout != null) out.timeout = Number(l.timeout);
            if (l.isEnd) out.isEnd = true;
            if (continueFlow && dialogLineEndsCall(l, index, lines)) {
                out.onEnd = continueFlow;
                if (markCallback) markCallback();
            }
            if (l.options && l.options.length) {
                out.options = l.options.map(function (o) {
                    return mapDialogOption(o, scope, continueFlow, markCallback);
                });
            }
            return out;
        }

        function buildDialog(scope, continueFlow, markCallback) {
            var out = {};
            (qd.dialog || []).forEach(function (b) {
                var lines = b.lines || [];
                out[b.name] = lines.map(function (l, i) {
                    return mapDialogLine(l, scope, continueFlow, i, lines, markCallback);
                });
            });
            return out;
        }

        var Dialog = buildDialog(null, null, null);

        function installDialog(scope, continueFlow) {
            var callbacks = 0;
            if (!questRef) return callbacks;
            var filled = buildDialog(scope, continueFlow, function () { callbacks++; });
            if (Object.keys(filled).length) questRef.Dialog = filled;
            return callbacks;
        }

        var kisscordNodes = g.nodes
            .filter(function (n) { return n.type === "comms.dialogue" && n.data.kind === "kisscord"; });
        var KisscordChats = kisscordNodes
            .map(function (n) {
                return {
                    contactId: n.data.kisscord.contactId,
                    messages: (n.data.kisscord.messages || []).map(function (m) {
                        var out;
                        if (m.playerAction === "send") {
                            out = { content: m.playerText, isMine: true, delayMs: m.delayMs };
                        } else if (m.playerAction === "upload") {
                            out = { content: "[uploaded file: " + ((m.upload && m.upload.name) || "file") + "." + ((m.upload && m.upload.extension) || "txt") + "]", isMine: true, delayMs: m.delayMs };
                        } else if (m.playerAction === "input") {
                            out = { content: m.content || "(waiting for your answer)", delayMs: m.delayMs };
                        } else {
                            out = { content: m.content, isMine: m.isMine, delayMs: m.delayMs };
                        }
                        if (m.unlocksAfter && m.unlocksAfter.length) out.unlocksAfter = m.unlocksAfter;
                        return out;
                    }),
                };
            });

        /* WeeChatChatDefinition (the declarative, quest-scoped chat script) only
           has host/messages in the SDK — there is no password or
           registerServer field on it. Making the server something the
           player can actually connect to (weechat HOST PASSWORD) is a
           separate, imperative call: WeeChat.createServer(host, password).
           That registration (and its matching removeServer cleanup) is done
           in OnObjectivesStart/OnComplete/OnAbandon below — see
           weechatServers. */
        var weechatServers = g.nodes
            .filter(function (n) { return n.type === "comms.dialogue" && n.data.kind === "weechat" && n.data.weechat.registerServer; })
            .map(function (n) { return { host: n.data.weechat.host, password: n.data.weechat.password }; });

        var weechatNodes = g.nodes
            .filter(function (n) { return n.type === "comms.dialogue" && n.data.kind === "weechat"; });
        var WeeChatChats = weechatNodes
            .map(function (n) {
                return {
                    host: n.data.weechat.host,
                    messages: (n.data.weechat.messages || []).map(function (m) {
                        /* isMine is what marks a line as the PLAYER's. The SDK
                           says of username: "Player username auto-filled if
                           isMine" — so a player line needs the flag and no
                           username, not a hardcoded one. We were sending
                           username:"you" with no flag, which the engine reads
                           as an NPC who happens to be called "you" (audit,
                           r69). */
                        var out;
                        if (m.playerAction === "send" || m.playerAction === "upload") {
                            out = { content: m.playerAction === "upload"
                                ? "[uploaded file: " + ((m.upload && m.upload.name) || "file") + "." + ((m.upload && m.upload.extension) || "txt") + "]"
                                : m.playerText, isMine: true };
                        } else if (m.playerAction === "input") {
                            out = { content: m.content || "(waiting for your answer)", username: m.username };
                        } else if (m.isMine) {
                            out = { content: m.content, isMine: true };
                        } else {
                            out = { content: m.content, username: m.username };
                        }
                        if (m.delayMs) out.delayMs = m.delayMs;
                        return out;
                    }),
                };
            });

        /* Timed chats — OPT IN, per node ("Play when the story reaches this
           node"). A quest-level KisscordChats /
           WeeChatChats script is handed to the engine when the quest starts, so
           a conversation that has to land on a Sequence beat cannot use that
           path. Those nodes are played through the platform API instead
           (SDK 0.21.0: Kisscord.sendMessage(channelUserId, content, isMine),
           WeeChat.sendMessage({host, username, message})), one message at a
           time, honouring each message's own delay. Everything else stays
           declarative, which is what the engine scopes and cleans up for us. */
        var chatOfNode = {};
        kisscordNodes.forEach(function (n, i) { chatOfNode[n.id] = KisscordChats[i]; });
        weechatNodes.forEach(function (n, i) { chatOfNode[n.id] = WeeChatChats[i]; });
        var liveChat = {};
        function optedInAndWired(n) {
            return n.data.postLive === true && g.edges.some(function (e) {
                return e.kind === "flow" && e.target === n.id;
            });
        }
        kisscordNodes.forEach(function (n) {
            if (optedInAndWired(n) && sdk.Kisscord && sdk.Kisscord.sendMessage) liveChat[n.id] = "kisscord";
        });
        weechatNodes.forEach(function (n) {
            if (optedInAndWired(n) && sdk.WeeChat && sdk.WeeChat.sendMessage) liveChat[n.id] = "weechat";
        });
        var DeclaredKisscordChats = KisscordChats.filter(function (_c, i) { return !liveChat[kisscordNodes[i].id]; });
        var DeclaredWeeChatChats = WeeChatChats.filter(function (_c, i) { return !liveChat[weechatNodes[i].id]; });
        var playedChats = {};

        function sendChatNow(node, scope) {
            var mode = liveChat[node.id];
            var chat = chatOfNode[node.id];
            if (!mode || !chat || playedChats[node.id]) return Promise.resolve();
            /* Once per playthrough: a conversation replayed by a loop would
               stack duplicates in a window the player may still be reading. */
            playedChats[node.id] = true;
            return (chat.messages || []).reduce(function (chain, m) {
                return chain.then(function () {
                    var wait = Math.max(0, Number(m.delayMs || 0));
                    var send = function () {
                        var content = __QE.fill(m.content || "", scope);
                        if (mode === "kisscord") sdk.Kisscord.sendMessage(chat.contactId, content, !!m.isMine);
                        else sdk.WeeChat.sendMessage({ host: chat.host, username: m.username || "", message: content });
                    };
                    return wait > 0 ? __QE.sleep(wait).then(send) : send();
                });
            }, Promise.resolve());
        }

        /* ── Twotter posts (r185) ──────────────────────────────────────── */

        /* Node ids that have already posted in this playthrough. The flow
           re-runs on a save reload (entry.load chains replayed by
           OnObjectivesStart), and a node that posted twice would stack
           duplicates in a feed the player may still be reading. */
        var postedTweetNodes = {};

        /* The moment an "already on the profile" tweet was posted, as ISO with
           milliseconds — the one spelling the game kept in all three P-01a
           tests, and what its relative ages are rendered from. */
        function twotterEarlierIso(row) {
            var base = (sdk.Time && sdk.Time.date) ? sdk.Time.date() : new Date();
            var when = new Date(base.getTime());
            var amount = Math.max(1, Math.round(Number(row.agoAmount) || 1));
            var unit = String(row.agoUnit || "days");
            if (unit === "minutes") when.setMinutes(when.getMinutes() - amount);
            else if (unit === "hours") when.setHours(when.getHours() - amount);
            else if (unit === "days") when.setDate(when.getDate() - amount);
            else if (unit === "weeks") when.setDate(when.getDate() - 7 * amount);
            else if (unit === "months") when.setMonth(when.getMonth() - amount);
            else if (unit === "years") when.setFullYear(when.getFullYear() - amount);
            return when.toISOString();
        }

        /* Post this node's tweets, once per playthrough. Rows are posted oldest
           -> newest: the author writes a history the way it happened, and while
           the profile sorts by time anyway (P-01b), posting in order keeps two
           tweets at the same age reading in the order they were written. */
        function postTweets(node, scope) {
            if (!twotterReady || !sdk.Twotter.postTweet) {
                __QE.log("twotter node " + node.id + ": no Twotter API in this build - nothing posted");
                return;
            }
            if (postedTweetNodes[node.id]) {
                __QE.log("twotter node " + node.id + ": already posted in this playthrough; skipping");
                return;
            }
            postedTweetNodes[node.id] = true;
            var account = twotterAccount(node.data.accountId);
            if (!account) {
                __QE.log("twotter node " + node.id + ": no account set (or the account was deleted) - nothing posted");
                return;
            }
            var userId = ensureTwotterAccount(account, qd.id);
            if (!userId) return;
            var rows = node.data.tweets || [];
            if (!rows.length) {
                __QE.log("twotter node " + node.id + ": no tweets to post");
                return;
            }
            for (var i = 0; i < rows.length; i++) {
                var row = rows[i];
                /* Deterministic id: a reload cannot post a second copy under a
                   different name, and cleanup has something stable to remove. */
                var tweetId = "qe-" + qd.id + "-" + node.id + "-" + i;
                var tweet = {
                    id: tweetId,
                    userId: userId,
                    content: __QE.fill(row.content || "", scope),
                    interaction: {
                        comments: Math.max(0, Math.round(Number(row.comments) || 0)),
                        share: Math.max(0, Math.round(Number(row.shares) || 0)),
                        likes: Math.max(0, Math.round(Number(row.likes) || 0)),
                        views: Math.max(0, Math.round(Number(row.views) || 0)),
                    },
                    showInTimeline: !!row.showInTimeline,
                };
                if (row.timeMode === "earlier") tweet.sendedAt = twotterEarlierIso(row);
                /* The posting API's record has no picture field (SDK 0.24
                   TwotterTweet), and the r185 QA run confirmed it: a tweet with
                   an attached picture showed no picture in the timeline or on
                   the detail page, so the editor no longer offers one (r187).
                   The key is still sent when an older project carries one:
                   unknown keys cost the game nothing, and if a future SDK
                   starts reading either spelling, the pictures authors already
                   attached come back with it. */
                if (row.image) {
                    tweet.image = row.image;
                    tweet.media = row.image;
                }
                try {
                    sdk.Twotter.postTweet(tweet);
                    __QE.log("twotter node " + node.id + ": posted " + tweetId +
                        (tweet.sendedAt ? " (backdated to " + tweet.sendedAt + ")" : " (now)"));
                    questCleanup.push({ kind: "tweet", id: tweetId });
                } catch (e) {
                    __QE.log("twotter node " + node.id + ": posting failed (the story continues): " + e);
                }
            }
        }

        /* Everything this quest added to the world that should disappear with
           it. Filled as the flow runs (a node the story never reaches added
           nothing), drained in OnComplete/OnAbandon. */
        var questCleanup = [];

        /* Job ids armed by flow.timer nodes, cancelled when the quest ends: a
           timer for a finished quest must not fire. The same array is reachable
           as beatJobsByQuest[qd.id], which is how the job handler (outside this
           closure) drops an id once it has fired. */
        var beatJobs = beatJobsByQuest[qd.id] || (beatJobsByQuest[qd.id] = []);

        /* Has the quest already been torn down once?

           OnComplete can be called more than once - the engine may re-run it,
           and AutoComplete plus a "completed (in-memory)" objective can bring
           it round again on a save that has finished the quest before. Tearing
           the same world down twice means destroying a network that is already
           gone, which is exactly the call the loader once hung on (r72). */
        var cleanedUp = false;

        /* The last non-empty quest Data we saw. See dataScope(). */
        var lastKnownData = null;

        /* reason is "complete" or "abandon".

           Networks are NOT destroyed when a quest completes (r81). Three
           reasons, in order of how much they cost:

           1. It hangs the game. The engine retires the quest and calls
              OnComplete while the player may still be sitting in a meterpreter
              session on the machine we are about to delete. In that state
              destroyNetwork's promise never settles - not resolved, not
              rejected - and the renderer freezes. QA saw exactly this: every
              cleanup trace printed except "network <ip> destroyed" (r80).
           2. It reads wrong. A company you just hacked should still be there
              afterwards; having it evaporate the moment the mail is sent is
              strange.
           3. Nobody else does it. Nemesis, the known-working reference mod,
              creates seven networks at random addresses and its OnComplete and
              OnAbandon are empty.

           Abandoning still tears down, because that means the player walked
           away and is not connected to anything. An author who really wants
           the world to vanish on completion can still opt in per network.

           Leftovers are cheap: these are data structures, not disk images, and
           the addresses are random, so a stale one cannot collide with a fresh
           run. The r75/r76 ownership ledger reclaims anything we created on a
           later load, when no session can be live in it. */
        function runQuestCleanup(reason) {
            if (cleanedUp) {
                __QE.log("cleanup already ran for this quest; skipping");
                return;
            }
            cleanedUp = true;
            var skipped = 0;
            var todo = questCleanup.filter(function (item) {
                /* Keep networks standing when the quest is simply finished. */
                if (item.kind === "network" && reason === "complete" && !item.onComplete) {
                    skipped++;
                    return false;
                }
                return true;
            });
            questCleanup.length = 0;
            __QE.log("cleanup starting (" + reason + "): " + todo.length + " item(s) to undo" +
                (skipped ? ", leaving " + skipped + " network(s) standing" : ""));
            /* A quest that ends may be lived through again: the engine lets a
               finished quest be claimed afresh, and the tweets this node
               posted were just removed with everything else. Forgetting the
               posting guard here is what lets the next run post them again —
               without it a re-claimed quest shows a profile with nothing on it
               and objectives waiting for a post that can never arrive (seen in
               the r185 QA run). Reloading mid-story is unaffected: the guard is
               what stops a replayed flow from stacking duplicates then. */
            postedTweetNodes = {};
            /* The quest's Twotter characters go with it (r185). Done here
               rather than in the loop below because it asks a question the
               other kinds do not: whether another live quest still needs the
               account. */
            releaseTwotterAccounts(qd.id);

            while (todo.length) {
                var item = todo.pop();
                __QE.log("cleanup: " + item.kind + " " + (item.ip || item.domain || item.command || item.id || ""));
                try {
                    if (item.kind === "network" && sdk.Network.destroyNetwork) {
                        /* destroyNetwork is the ONLY cleanup call that returns
                           a promise, and an unhandled rejection from it takes
                           the renderer with it. r75 guarded the copy in
                           reclaimDomain and missed this one (r79). */
                        var dp = sdk.Network.destroyNetwork(item.ip);
                        if (dp && typeof dp.then === "function") {
                            (function (ip) {
                                dp.then(
                                    function () { __QE.log("cleanup: network " + ip + " destroyed"); },
                                    function (err) { __QE.log("cleanup: destroying network " + ip + " failed: " + err); }
                                );
                            })(item.ip);
                        }
                    }
                    if (item.kind === "domain" && sdk.Network.removeDomain) sdk.Network.removeDomain(item.domain);
                    if (item.kind === "commandData" && sdk.Shell && sdk.Shell.removeCommandData) sdk.Shell.removeCommandData(item.command, item.input);
                    if (item.kind === "tweet" && sdk.Twotter && sdk.Twotter.removeTweet) {
                        sdk.Twotter.removeTweet(item.id);
                        __QE.log("cleanup: tweet " + item.id + " removed");
                    }
                    if (item.kind === "firewall" && sdk.Network.removeFirewallRule) sdk.Network.removeFirewallRule(item.ip, item.port);
                    if (item.kind === "database" && sdk.Database && sdk.Database.remove) sdk.Database.remove(item.id);
                    if (item.kind === "port") {
                        if (item.action === "open" && sdk.Network.closePort) sdk.Network.closePort(item.ip, item.port);
                        if (item.action === "close" && sdk.Network.openPort) sdk.Network.openPort(item.ip, item.port);
                        if (item.action === "add" && sdk.Network.removePort) sdk.Network.removePort(item.ip, item.port);
                    }
                } catch (e) {
                    /* the world may already be gone; never block cleanup */
                    __QE.log("cleanup: " + item.kind + " failed (continuing): " + e);
                }
            }
            __QE.log("cleanup finished");
        }

        var objectiveNodes = g.nodes.filter(function (n) { return n.type === "objective"; });
        var questRef = null;
        var needsTargetIp = g.nodes.some(function (n) {
            return (n.type === "world.network" || n.type === "world.wifi") && n.data.ipMode === "random";
        });
        /* Any network whose top-level machine is not a router will be wrapped
           in one (r77), and that router needs its own allocated address. */
        var needsGateway = g.nodes.some(function (n) {
            return n.type === "world.network" &&
                String((n.data.device && n.data.device.type) || "").toUpperCase() !== "ROUTER";
        });

        var Objectives = objectiveNodes.map(function (n) {
            var unlocks = g.edges
                .filter(function (e) { return e.kind === "unlock" && e.target === n.id; })
                .map(function (e) { var s = byId[e.source]; return s && s.type === "objective" ? s.data.name : null; })
                .filter(Boolean);
            var trig = g.edges
                .filter(function (e) { return e.kind === "condition" && e.target === n.id; })
                .map(function (e) { return byId[e.source]; })
                .filter(function (s) { return s && s.type === "trigger.event"; })[0];
            /* Objective text carries {{tokens}} like everything else an author
               writes - and since r73 the target address is one, because the
               game allocates it. This list is built when the class is defined,
               before CreateData() has run, so the tokens cannot be resolved
               yet; refillObjectives() below does it once the quest starts.
               QA saw the raw "nmap {{data.targetIp}} -sV" in the quest panel. */
            var o = { name: n.data.name, description: __QE.fill(n.data.description || "", dataScope()) };
            if (unlocks.length) o.unlocksAfter = unlocks;
            if (n.data.hidden) o.hidden = true;
            if (n.data.hint) o.hint = __QE.fill(n.data.hint, dataScope());
            if (n.data.info) o.info = __QE.fill(n.data.info, dataScope());
            if (n.data.terminalCommand) o.terminalCommand = __QE.fill(n.data.terminalCommand, dataScope());
            /* The declarative trigger is still declared, even though
               OnObjectivesStart now completes the objective imperatively as
               well. Whichever one the build honours, the objective ticks off,
               and completing an already-completed objective is a no-op. */
            if (trig) {
                var clauses = trig.data.conditions;
                o.trigger = {
                    event: trig.data.event,
                    condition: function (data) {
                        return __QE.matchAll(clauses, data, dataScope());
                    },
                };
            }
            return o;
        });

        /* How many objectives have ticked, and whether the author asked for
           the panel to be emptied once they all have.

           Current builds can formally finish a quest through the Complete quest
           node. This legacy cleanup option remains for projects that choose to
           leave a story in the active list: when the last objective ticks we
           flip rows to hidden and ask the panel to redraw. */
        var objectivesDone = 0;
        function hideAllObjectives() {
            if (!questRef || !questRef.Objectives) return;
            var rows = questRef.Objectives;
            /* Hiding EVERY row works - QA confirmed the list empties - but the
               header then reads "0/0 completed", because the counter counts
               visible rows and there are none. Leaving the last row visible
               and rewriting it into a closing line reads as finished instead:
               "1/1 completed", one ticked item saying the contract is closed.

               The author supplies the text; if they leave it empty we hide
               everything and accept 0/0. */
            var closing = qd.closingObjectiveText;
            rows.forEach(function (o, i) {
                if (closing && i === rows.length - 1) {
                    o.hidden = false;
                    o.description = __QE.fill(closing, dataScope());
                    /* The hint and the terminal command belonged to the step
                       the player just finished; leaving them would show stale
                       instructions under a line that says the job is done. */
                    delete o.hint;
                    delete o.info;
                    delete o.terminalCommand;
                } else {
                    o.hidden = true;
                }
            });
            /* Nudge the engine to re-read. Assigning the array back is the
               same shape as the Dialog rewrite that does work in-game. */
            try { questRef.Objectives = rows.slice(); } catch (e) { /* frozen? leave it */ }
            __QE.log(closing
                ? "all objectives done; left a closing line and hid " + (rows.length - 1) + " row(s)"
                : "all objectives done; asked the panel to hide them (" + rows.length + " row(s))");
        }

        /* Fill the objectives' {{tokens}} again now that the quest has its
           Data. Called at the top of OnStart, which is the first moment
           CreateData() has run and {{data.targetIp}} resolves to a real
           address. Writes onto the array the engine is holding. */
        function refillObjectives() {
            if (!questRef || !questRef.Objectives) return;
            objectiveNodes.forEach(function (n, i) {
                var o = questRef.Objectives[i];
                if (!o) return;
                o.description = __QE.fill(n.data.description || "", dataScope());
                if (n.data.hint) o.hint = __QE.fill(n.data.hint, dataScope());
                if (n.data.info) o.info = __QE.fill(n.data.info, dataScope());
                if (n.data.terminalCommand) o.terminalCommand = __QE.fill(n.data.terminalCommand, dataScope());
            });
        }

        /* Builds the scope object {{token}} fields resolve against.
           data/Data both point at the quest's own persisted Data (set via
           SetData / CreateData) — NOT the current event payload, which is
           matched separately via each condition's own field selector. This
           matches what every field hint in the editor promises ("insert a
           value you saved earlier"). player and random fields are computed
           fresh each call so repeated use of e.g. {{random.password}}
           yields independent values. */
        function dataScope(extra) {
            /* questRef.Data can go UNDEFINED while the story is still running.
               QA r79 saw it on the last three nodes of a finished quest:

                   reached "Out-PayThePlayer"   | saved: undefined
                   reached "Out-Dialogue-Received" | saved: undefined

               ...where every earlier node had printed the real values. The
               engine had already begun retiring the completed quest, so its
               Data was gone while our flow was still walking the wires behind
               it. Any {{data.x}} in those nodes would have filled with "" -
               a payment description or a mail body quietly losing its values.

               Remember the last Data we saw and fall back to it, so the tail
               of a quest reads the same as the rest of it. */
            if (questRef && questRef.Data) lastKnownData = questRef.Data;
            var d = (questRef && questRef.Data) || lastKnownData || {};
            /* Every player/random field is a GETTER, computed only if a token
               actually asks for it, and never allowed to throw. Reading them
               eagerly cost a mod its whole quest once: a project with no
               {{player.ip}} anywhere still called Network.getPlayerIp on every
               scope, the loader refused it for want of the "network"
               permission, and the exception escaped OnStart so the quest never
               started. A value the author never mentioned must not be able to
               do that. */
            /* __QE.tokenScope(), not a bare call: tokenScope lives in the IIFE
               above, and this function is a sibling of it, not a child. A bare
               call threw "tokenScope is not defined" on EVERY quest node that
               filled a token - which is how the r203 gates caught it (208 tests
               in compile.test.ts). */
            var base = __QE.tokenScope();
            base.data = d;
            base.Data = d;
            if (extra) { for (var k in extra) base[k] = extra[k]; }
            return base;
        }

        function scopeOf(ctx) {
            return dataScope({ vars: ctx && ctx.vars });
        }

        /* Kisscord/WeeChat content is registered declaratively
           on the instance (the SDK drives when each message actually shows,
           not the flow graph), so there's no single "send" call site to fill
           tokens at like there is for mail/dialog. Best effort: re-render
           with whatever Data exists once CreateData has populated it (called
           from OnObjectivesStart, which always runs after CreateData). This
           correctly resolves anything set in CreateData (including
           {{data.targetIp}}), but NOT values a flow node sets later via
           fx.setData mid-playthrough — those can't retroactively update
           content the SDK already registered. */
        function refillComms() {
            var scope = dataScope();
            if (DeclaredKisscordChats.length) {
                questRef.KisscordChats = DeclaredKisscordChats.map(function (c) {
                    return Object.assign({}, c, {
                        messages: c.messages.map(function (m) {
                            return m.content ? Object.assign({}, m, { content: __QE.fill(m.content, scope) }) : m;
                        }),
                    });
                });
            }
            if (DeclaredWeeChatChats.length) {
                questRef.WeeChatChats = DeclaredWeeChatChats.map(function (c) {
                    return Object.assign({}, c, {
                        messages: c.messages.map(function (m) {
                            return m.content ? Object.assign({}, m, { content: __QE.fill(m.content, scope) }) : m;
                        }),
                    });
                });
            }
        }

        /* One node must never be able to end the story.
           The Ledger template proved why: a single bad SDK call in a Tool
           response node threw, the promise chain died, and the briefing mail
           two nodes later was never sent - the player got a quest with an
           objective and nothing else. Now a node that throws is logged and the
           flow carries on to the next one. */
        /* Sending a mail from the story.

           Two rounds of QA said the same thing: quests built here declared
           their mail in Quest.Mails and sent it with this.sendMail(index), the
           engine took the call without complaint, and nothing ever appeared in
           GoMail. A working quest mod from another author (Nemesis) was read
           side by side with ours and it never touches this.sendMail or
           Quest.Mails at all - every mail it sends, including its opening
           briefing, goes out through the global Mail.send({ from, subject,
           content }). That is the path the game demonstrably delivers, so it is
           the path used first here.

           this.sendMail is still tried afterwards, but only if Mail.send was
           not available, and the inbox is still checked so a report can say
           which path carried the mail. */
        function sendQuestMail(node, scope) {
            var mi = mailIndex[node.id];
            var baseMail = Mails[mi];
            if (!baseMail) return;
            var subject = __QE.fill(baseMail.title || "", scope);
            /* baseMail.content was already converted to plain text when Mails
               was built - converting again here would strip a literal "<tag>"
               the author actually wrote. Only the {{tokens}} are filled. */
            var content = __QE.fill(baseMail.content || "", scope);
            var from = mailFrom[node.id] || "";

            /* Keep the declared copy in step with what was actually sent, so a
               quest whose mail carries {{tokens}} still reads correctly if the
               engine ever surfaces Quest.Mails itself.

               Note what this does NOT do: it only refreshes an entry that is
               already there. Writing into an array the engine never took would
               make the fallback below believe it has a usable mail when the
               engine has nothing. */
            if (questRef && questRef.Mails && questRef.Mails[mi] &&
                String(questRef.Mails[mi].title || "").length > 0) {
                var filledMail = { title: subject, content: content };
                if (baseMail.replyable) filledMail.replyable = true;
                if (baseMail.attachment) filledMail.attachment = baseMail.attachment;
                questRef.Mails[mi] = filledMail;
            }

            /* Which path can express what this mail needs?

               Mail.send takes a MailDefinition: subject, content, from, to,
               metadata, attachments — and NO replyable. Quest.sendMail sends
               Quest.Mails[i], a QuestMailDefinition, which is the only shape
               that carries a reply flag.

               Mail.send stays the default because r37 measured it as the
               reliable path. But a mail the author marked replyable cannot say
               so through it, so that one mail goes the other way - provided the
               engine has actually taken our Mails array, which is the same
               check the fallback below makes. If it has not, send through
               Mail.send anyway and say why: a mail that arrives without its
               Reply button beats a mail that never arrives. */
            var engineHasMail = !!(questRef && questRef.Mails && questRef.Mails[mi] &&
                String(questRef.Mails[mi].title || "").length > 0);
            var wantsReply = !!baseMail.replyable;
            var how = "";

            if (wantsReply && engineHasMail && questRef.sendMail) {
                try {
                    questRef.sendMail(mi, from || undefined);
                    how = "Quest.sendMail(" + mi + ") [replyable]";
                } catch (eR) {
                    __QE.log("Quest.sendMail(" + mi + ") threw while sending a replyable mail: " +
                        (eR && eR.message ? eR.message : eR));
                }
            }
            if (!how && wantsReply) {
                __QE.log("mail \"" + subject + "\" is marked replyable, but the engine has no usable copy " +
                    "to send that way - going out through Mail.send instead, which has no reply flag, " +
                    "so no Reply button will appear.");
            }

            if (!how && sdk.Mail && sdk.Mail.send) {
                var direct = { subject: subject, content: content };
                if (from) direct.from = from;
                var to = __QE.safe(function () { return sdk.Mail.getPlayerEmail ? sdk.Mail.getPlayerEmail() : ""; });
                if (to) direct.to = to;
                if (baseMail.attachment && baseMail.attachment.name) {
                    direct.attachments = [{
                        name: baseMail.attachment.name,
                        extension: baseMail.attachment.extension || "txt",
                        data: baseMail.attachment.content || "",
                    }];
                }
                /* MailDefinition — what Mail.send takes — has no "replyable"
                   field. Only QuestMailDefinition does, and that is the array
                   the engine ignores on this build (r37). So a mail sent this
                   way never gets a Reply button, whatever the author ticked.
                   Say so once rather than leaving them looking for it. */
                try {
                    sdk.Mail.send(direct);
                    how = "Mail.send";
                } catch (e) {
                    __QE.log("Mail.send failed for \"" + subject + "\": " + (e && e.message ? e.message : e));
                }
            }

            /* Only if the global API is missing or refused the mail: the old
               quest-scoped path, so nothing regresses on a build where it is
               the only one that exists. */
            if (!how) {
                /* Quest.sendMail(index) sends whatever the ENGINE holds in
                   this.Mails at that index - not the text we just filled in.
                   When the engine has not taken our Mails array (which happens
                   when the quest was registered without permissions), it sends
                   a mail with an empty subject and empty body. QA got exactly
                   that: a briefing that arrived blank, and a Mail.Read trigger
                   that could never match a subject of "".

                   So check what the engine actually has before trusting it. */
                var engineMail = questRef && questRef.Mails ? questRef.Mails[mi] : null;
                var usable = engineMail && String(engineMail.title || "").length > 0;
                if (!usable) {
                    __QE.log("Quest.sendMail(" + mi + ") skipped: the engine has no usable copy of \"" +
                        subject + "\" (its Mails[" + mi + "] is " +
                        (engineMail ? "empty" : "missing") + "), so it would deliver a blank mail");
                } else {
                    try {
                        if (questRef.sendMail) {
                            questRef.sendMail(mi, from || undefined);
                            how = "Quest.sendMail(" + mi + ")";
                        }
                    } catch (e2) {
                        __QE.log("Quest.sendMail(" + mi + ") threw: " + (e2 && e2.message ? e2.message : e2));
                    }
                }
            }

            if (!how) {
                __QE.log("mail \"" + subject + "\" could not be sent: this build offers neither Mail.send nor a usable Quest.sendMail");
                return;
            }
            __QE.log("mail \"" + subject + "\" sent via " + how);

            /* Confirm it landed. This wait is a plain timer on purpose - the
               check must not be able to hang on the game's own clock, which is
               what stopped the previous build from ever reporting anything. */
            __QE.wait(1500).then(function () {
                var inbox = __qeInbox();
                if (inbox === null) return; /* engine does not let us look */
                if (__qeInboxHas(inbox, subject)) {
                    __QE.log("mail \"" + subject + "\" delivered");
                } else {
                    __QE.log("mail \"" + subject + "\" was accepted by " + how + " but is not in the inbox");
                }
            });
        }

        /* The player's inbox, or null when this build will not show it - null
           and "empty" mean different things to the delivery check. */
        function __qeInbox() {
            try {
                if (!sdk.Mail || !sdk.Mail.getInbox) return null;
                var list = sdk.Mail.getInbox();
                return list && list.length != null ? list : null;
            } catch (e) {
                return null;
            }
        }

        function __qeInboxHas(inbox, subject) {
            for (var i = 0; i < inbox.length; i++) {
                if (inbox[i] && inbox[i].subject === subject) return true;
            }
            return false;
        }

        var objectiveDoneDepth = 0;
        var pendingQuestEnd = null;

        function performQuestEnd(req) {
            if (!req) return;
            try {
                if (req.kind === "complete") {
                    if (questRef && typeof questRef.complete === "function") {
                        questRef.complete();
                        __QE.log("quest completed by Complete quest node");
                    } else {
                        __QE.log("Complete quest node skipped: Quest.complete is unavailable");
                    }
                } else if (req.kind === "retire") {
                    if (questRef && typeof questRef.retire === "function") {
                        questRef.retire();
                        __QE.log("quest retired by Retire quest node");
                    } else {
                        __QE.log("Retire quest node skipped: Quest.retire is unavailable");
                    }
                } else if (req.kind === "unclaim") {
                    if (sdk.Quest && typeof sdk.Quest.unclaim === "function") {
                        sdk.Quest.unclaim(req.name || qd.name);
                        __QE.log("quest unclaimed by Unclaim quest node: " + (req.name || qd.name));
                    } else {
                        __QE.log("Unclaim quest node skipped: Quest.unclaim is unavailable");
                    }
                }
            } catch (e) {
                __QE.log("quest ending failed: " + (e && e.message ? e.message : e));
            }
        }

        function requestQuestEnd(kind, name) {
            var req = { kind: kind, name: name };
            if (objectiveDoneDepth > 0) {
                pendingQuestEnd = req;
                return;
            }
            performQuestEnd(req);
        }

        function flushQuestEnd() {
            if (!pendingQuestEnd) return;
            var req = pendingQuestEnd;
            pendingQuestEnd = null;
            performQuestEnd(req);
        }

        function runFlow(nodeId, ctx, depth) {
            try {
                return runFlowStep(nodeId, ctx, depth);
            } catch (e) {
                var failed = byId[nodeId];
                __QE.log("node " + nodeId + (failed ? " (" + failed.type + ")" : "") +
                    " failed and was skipped: " + (e && e.message ? e.message : e));
                return continueFrom(nodeId, ctx, depth);
            }
        }

        /* The wires out of a node, followed without running the node itself. */
        function continueFrom(nodeId, ctx, depth) {
            var node = byId[nodeId];
            if (!node) return undefined;
            /* Synchronous for the same reason as next() above: a skipped node
               must not push the rest of the quest into a microtask, or
               everything after it loses the mod's permissions. */
            return __QE.seq(flowOuts(nodeId), function (e) {
                return runFlow(e.target, ctx, depth + 1);
            });
        }

        /* ── scheduled timers ──────────────────────────────────────────
           Arming is synchronous inside the flow's call stack (the engine
           only grants this mod permissions there). Firing happens inside
           the Scheduler callback - a call the engine made, so the mod
           still holds its permissions: r166 T-02 proved mail, toast and
           objective completion all work from inside one. */
        /* A whole, non-negative count - every offset box is one. */
        function intOr0(v) {
            return Math.max(0, Math.round(Number(v) || 0));
        }

        /* The unit boxes of whichever mode this Timer is in, read exactly the
           way the editor's own vocabulary reads them (src/schema/timer.ts).
           "daytime" pins a clock time, so it has no hours or minutes boxes. */
        function offsetParts(d) {
            if ((d.mode || "after") === "daytime") {
                return {
                    years: intOr0(d.offsetYears), months: intOr0(d.offsetMonths),
                    weeks: intOr0(d.offsetWeeks), days: intOr0(d.offsetDays),
                    hours: 0, minutes: 0
                };
            }
            return {
                years: intOr0(d.years), months: intOr0(d.months), weeks: intOr0(d.weeks),
                days: intOr0(d.days), hours: intOr0(d.hours), minutes: intOr0(d.minutes)
            };
        }

        /* Add an offset to a moment, in the order the editor promises: years
           and months move the calendar (keeping the day number, clamped to the
           target month's last day - 31 Jan + 1 month = 28 Feb, never 2 or 3
           March), then weeks and days, then hours and minutes as wall-clock
           time. The clamp happens once, on the calendar part alone, so
           "1 month + 1 day" from 31 Jan is 1 March. The Date constructor
           rolls any overflow, so four more hours past 23:00 is tomorrow. */
        function addOffset(base, p) {
            var y = base.getFullYear() + p.years;
            var m = base.getMonth() + p.months;
            var day = base.getDate();
            if (p.years || p.months) {
                var last = new Date(y, m + 1, 0).getDate();
                if (day > last) day = last;
            }
            day += p.weeks * 7 + p.days;
            return new Date(y, m, day, base.getHours() + p.hours, base.getMinutes() + p.minutes);
        }

        /* The in-game timestamp a Timer is due at (r173; relative units
           r176/r177). Returns null when an "at" date is incomplete (the
           analysis already warns; the flow fails open). "daytime" resolves
           against Time.date() at arm time - the local-time Date constructor
           makes the hour/minute the player's clock time with no offset
           maths. */
        function computeTimerFireAt(d) {
            var h = Math.max(0, Math.min(23, Math.round(Number(d.hour) || 0)));
            var mi = Math.max(0, Math.min(59, Math.round(Number(d.minute) || 0)));
            if (d.mode === "daytime") {
                /* "In 1 month 2 weeks 2 days, at 18:23": the calendar part is
                   resolved here, at arm time, against the player's own clock;
                   the clock is then pinned onto the day that lands on. */
                var later = addOffset(sdk.Time.date(), offsetParts(d));
                return new Date(later.getFullYear(), later.getMonth(), later.getDate(), h, mi).getTime();
            }
            var y = Math.round(Number(d.dateYear) || 0);
            var m = Math.round(Number(d.dateMonth) || 0);
            var day = Math.round(Number(d.dateDay) || 0);
            if (!(y > 0 && m > 0 && day > 0)) return null;
            /* "The clock shows h:mi": interpret the chosen wall time in the
               player machine's zone - the zone the in-game clock displays.
               Settled in game on 2026-09-18 (S-04): the taskbar clock read
               20:17 while Time.now rendered 20:15 local and 18:15 UTC, so
               local it is, and this correction stays. The probe was the raw
               QA harness's "qe24 clock"; the arm log keeps printing ISO next
               to the raw value for future spot checks. */
            var tz = new Date().getTimezoneOffset() * 60000;
            return Date.UTC(y, m - 1, day, h, mi) - tz;
        }

        /* The rule in the author's own units, for the arm log: a tester
           compares it with what the inspector promised. */
        function ruleText(d) {
            var mode = d.mode || "after";
            var pad = function (n) { return (n < 10 ? "0" : "") + n; };
            var clock = pad(Math.max(0, Math.min(23, Math.round(Number(d.hour) || 0)))) + ":" +
                pad(Math.max(0, Math.min(59, Math.round(Number(d.minute) || 0))));
            if (mode === "at") {
                return "on " + intOr0(d.dateYear) + "-" + intOr0(d.dateMonth) + "-" + intOr0(d.dateDay) + ", at " + clock + " in-game";
            }
            var keys = mode === "daytime"
                ? [["offsetYears", "y"], ["offsetMonths", "mo"], ["offsetWeeks", "w"], ["offsetDays", "d"]]
                : [["years", "y"], ["months", "mo"], ["weeks", "w"], ["days", "d"], ["hours", "h"], ["minutes", "m"]];
            var bits = [];
            for (var i = 0; i < keys.length; i++) {
                var n = intOr0(d[keys[i][0]]);
                if (n) bits.push(n + keys[i][1]);
            }
            var offset = bits.length ? bits.join(" ") : "(nothing set)";
            return mode === "daytime" ? "in " + offset + ", at " + clock + " in-game" : "after " + offset;
        }

        function armBeat(nodeId, d) {
            if (!sdk.Scheduler) {
                __QE.log("timer node " + nodeId + ": no Scheduler API - the timer will not fire");
                return;
            }
            /* Idempotent: the opening flow re-runs on a save reload, and a
               re-run must not double-arm the same timer. */
            var pending = (sdk.Scheduler.list ? sdk.Scheduler.list(BEAT_KIND) : []) || [];
            for (var i = 0; i < pending.length; i++) {
                var pp = pending[i].payload;
                if (pp && pp.questId === qd.id && pp.nodeId === nodeId) {
                    __QE.log("timer node " + nodeId + " already armed - not double-arming");
                    return;
                }
            }
            var payload = { questId: qd.id, nodeId: nodeId, attempts: 0 };
            var mode = d.mode || "after";
            var parts = offsetParts(d);
            var id;
            if (mode === "after" && !parts.years && !parts.months) {
                /* The SDK's own duration form: days, hours and minutes, which
                   the engine adds to the in-game clock itself. Preferred
                   wherever it fits - it is the path S-01/S-02 verified. */
                if (!sdk.Scheduler.schedule) {
                    __QE.log("timer node " + nodeId + ": no schedule API - the timer will not fire");
                    return;
                }
                id = sdk.Scheduler.schedule(BEAT_KIND, payload, {
                    days: parts.weeks * 7 + parts.days,
                    hours: parts.hours,
                    minutes: parts.minutes,
                });
                __QE.log("timer node " + nodeId + " armed " + ruleText(d) + " (job " + id + ")");
            } else {
                /* Everything else is an absolute moment: months and years have
                   no field in the duration form (r177), and the calendar modes
                   are resolved here, at arm time, against Time.date(). */
                if (!sdk.Scheduler.scheduleAt) {
                    __QE.log("timer node " + nodeId + ": no scheduleAt API in this game build - the timer will not fire");
                    return;
                }
                var fireAt = mode === "after"
                    ? addOffset(sdk.Time.date(), parts).getTime()
                    : computeTimerFireAt(d);
                if (fireAt === null) {
                    __QE.log("timer node " + nodeId + ": no full date set - the timer will not fire");
                    return;
                }
                id = sdk.Scheduler.scheduleAt(BEAT_KIND, payload, fireAt);
                /* The rule in words, then the ISO string next to the raw
                   value: S-04 compares both with the on-screen clock to settle
                   the display timezone, and the raw harness's "qe24 clock"
                   does the same for the current time. */
                __QE.log("timer node " + nodeId + " armed " + ruleText(d) + " -> in-game " + new Date(fireAt).toISOString() + " (fireAt " + fireAt + ", job " + id + ")");
            }
            beatJobs.push(id);
        }

        function fireBeat(nodeId) {
            var node = byId[nodeId];
            if (!node) {
                __QE.log("timer: node " + nodeId + " no longer exists - nothing to fire");
                return;
            }
            __QE.log("timer " + nodeId + " fired");
            /* The timer is a new entry point into the graph: a fresh
               payload, quest data still resolving through dataScope(). */
            flowOuts(nodeId).forEach(function (e) {
                runFlow(e.target, { payload: {}, vars: {} }, 0);
            });
        }

        function bindBeatFire(instance) {
            liveBeats[qd.id] = function (nodeId) {
                questRef = instance;
                fireBeat(nodeId);
            };
        }

        function cancelBeatJobs() {
            if (!beatJobs.length) return;
            beatJobs.forEach(function (id) {
                __QE.safe(function () { if (sdk.Scheduler && sdk.Scheduler.cancel) sdk.Scheduler.cancel(id); });
            });
            __QE.log("cancelled " + beatJobs.length + " pending timer(s)");
            beatJobs.length = 0;
        }

        function runFlowStep(nodeId, ctx, depth) {
            if (depth > 200) return undefined;
            var node = byId[nodeId];
            if (!node) return undefined;
            var scope = scopeOf(ctx);
            var next = function () {
                var edges = flowOuts(nodeId);
                if (node.type === "flow.random" && edges.length) {
                    var picked = edges[Math.floor(Math.random() * edges.length)];
                    edges = [picked];
                    /* "Store the result as" promised the pick would be readable
                       through {{data.name}} and nothing ever wrote it (audit,
                       r67). Save the socket's label, which is the thing an
                       author names their outputs after. */
                    if (node.data.storeAs && questRef && questRef.SetData) {
                        var pickedLabel = picked.sourceHandle || "";
                        __QE.safe(function () { questRef.SetData(node.data.storeAs, pickedLabel); });
                    }
                }
                if (node.type === "flow.branch") {
                    var yes = __QE.matchAll(node.data.conditions, node.data.source === "data" ? (questRef ? questRef.Data : {}) : (ctx && ctx.payload) || {}, scopeOf(ctx));
                    edges = edges.filter(function (e) { return e.sourceHandle === (yes ? "true" : "false"); });
                }
                /* Walk the wires SYNCHRONOUSLY for as long as we can.
                   The engine only treats this mod as "current" while it is
                   inside a call it made - OnStart, OnObjectivesStart, an event
                   handler. The moment one of those returns, the mod loses its
                   identity, and any SDK call made afterwards is attributed to
                   Mod "null" and refused its permissions. Chaining every node
                   through Promise.resolve().then(...) pushed ALL the real work
                   into a microtask that ran after OnStart had returned, which
                   is why the log read:

                       quest "..." started (1 entry point)
                       node world-network4 ... Mod "null" ... without "network"

                   ...with the failures arriving after the start line. Only a
                   node that genuinely has to wait (a Delay, a timed chat beat)
                   may go async, and past that point the quest has already lost
                   its permissions anyway - so waits are worth flagging rather
                   than hiding. */
                return __QE.seq(edges, function (e) {
                    return runFlow(e.target, ctx, depth + 1);
                }, function (e) {
                    __QE.log("flow after node " + nodeId + " stopped: " + (e && e.message ? e.message : e));
                });
            };
            var d = node.data;
            switch (node.type) {
                case "world.network": {
                    /* "random" is allocated once in CreateData() and lives in
                       Data.targetIp, so the SAME ip is used here as whatever
                       {{data.targetIp}} resolves to elsewhere (mail, notify,
                       other device fields, condition values, ...). */
                    var netIp = d.ipMode === "random"
                        ? ((questRef && questRef.Data && questRef.Data.targetIp) || (sdk.Network.randomIp ? sdk.Network.randomIp() : d.device.ip))
                        : d.device.ip;
                    /* Clear anything already at this address first.

                       A network the save already holds WINS over one created
                       at the same ip: the engine keeps the old one and the new
                       definition is ignored. r52 added teardown on
                       complete/abandon, which is right but not enough - a
                       player who simply stops playing, or replaces the mod
                       with a newer build, never triggers either. The engine's
                       own PruneOrphanQuests drops the stale QUEST record but
                       leaves the network standing.

                       QA saw the consequence three rounds running: a port
                       closed in the project still showing OPEN in game, and an
                       exploit failing against a machine whose guest account
                       had been added two builds earlier. Both were the old
                       network answering. Destroying first makes every run
                       start from the network the mod actually describes. */
                    /* Take back any domain this device tree claims before
                       creating it, or an older build's server keeps answering
                       for the name (r75). */
                    domainsOf(d.device).forEach(function (dom) { reclaimDomain(dom, netIp); });

                    var rootDef = mapDevice(Object.assign({}, d.device, { ip: netIp }));

                    /* A subnet must be ROOTED IN A ROUTER.

                       QA, r77: a single DEVICE with port 22 open, handed
                       straight to createSubnetNetwork as the top-level node.
                       The machine existed - ping answered, "Host is up
                       (0.234s latency)" - but nmap reported "No ports found",
                       so metasploit had nothing to attack and the quest died
                       at the same step it has died at for several rounds.

                       The reference mod is unanimous on this: all SEVEN of its
                       createSubnetNetwork calls pass a Router at the top with
                       everything else in children, and not one passes a bare
                       Device. Its scannable machines - the ones a player
                       actually nmaps and exploits, like MAREK - are always
                       CHILDREN of that router. SubnetNetworkDefinition does
                       type-check a bare Device, which is why this was never
                       caught, but the engine evidently only builds the port
                       tables for machines sitting behind a router.

                       So when an author's network is a lone machine (which is
                       the common case, and exactly what the Harbour template
                       is), give it the router it needs. The router takes its
                       own address and the author's machine KEEPS netIp, so
                       every {{data.targetIp}} reference - whois, the nmap
                       objective, the scp command, the conditions - still
                       points at the box the player is meant to break into. */
                    var createIp = netIp;
                    if (String(rootDef.type || "").toUpperCase() !== "ROUTER") {
                        var gatewayIp = (questRef && questRef.Data && questRef.Data.gatewayIp) ||
                            (sdk.Network.randomIp ? sdk.Network.randomIp() : "10.0.0.254");
                        createIp = gatewayIp;
                        rootDef = {
                            ip: gatewayIp,
                            type: "ROUTER",
                            name: "ROUTER",
                            accessable: true,
                            users: [],
                            ports: [],
                            children: [rootDef],
                        };
                    }

                    /* Destroying the ROUTER takes the whole subnet with it
                       ("Removes all child subnets and files"), so cleanup has
                       to name the address we actually created at. */
                    questCleanup.push({ kind: "network", ip: createIp, onComplete: d.destroyOnComplete === true });
                    return buildNetwork(createIp, rootDef, next);
                    /* Tear the network down again when the quest ends. The
                       editor has always offered this toggle; the compiler
                       ignored it, so nothing was ever destroyed. A network the
                       game has already saved at an IP wins over the one a
                       later version of the mod tries to create there - which
                       is why QA saw a port banner ("OpenSSH 7.2") that no
                       longer existed in the project, unchanged across three
                       re-exports, and an exploit that failed against a machine
                       whose users had since been added. */
                }
                case "world.wifi": {
                    var wifiIp = d.ipMode === "fixed" && d.ip
                        ? d.ip
                        : ((questRef && questRef.Data && questRef.Data.targetIp) || (sdk.Network.randomIp ? sdk.Network.randomIp() : "10.0.0.1"));
                    var wifiRoot = mapDevice({
                        ip: wifiIp,
                        type: "ROUTER",
                        model: d.model,
                        ports: d.ports || [],
                        users: d.users || [],
                        children: d.children || [],
                    });
                    /* SDK 0.24.0 declares a native wireless creator. Keep the
                       old router fallback because exported mods may run on an
                       older game build even though the editor now exposes the
                       Wi-Fi node for current HackHub builds. */
                    if (sdk.Network.createWifiNetwork) {
                        var wifiDef = {
                            ssid: d.ssid,
                            password: d.password,
                            signal: d.signal,
                            ip: wifiIp,
                            model: d.model,
                            users: wifiRoot.users || [],
                            ports: wifiRoot.ports || [],
                            children: wifiRoot.children || [],
                        };
                        if (d.bssid) wifiDef.bssid = d.bssid;
                        if (d.channel != null && d.channel !== "") {
                            var wifiChannel = Number(d.channel);
                            if (!isNaN(wifiChannel)) wifiDef.channel = wifiChannel;
                        }
                        if (d.wps != null) wifiDef.wps = !!d.wps;
                        var madeWifiIp = sdk.Network.createWifiNetwork(wifiDef);
                        questCleanup.push({ kind: "network", ip: madeWifiIp || wifiIp, onComplete: d.destroyOnComplete === true });
                        return next();
                    } else {
                        /* Same reason as world.network above. */
                        questCleanup.push({ kind: "network", ip: wifiIp, onComplete: d.destroyOnComplete === true });
                        return buildNetwork(wifiIp, wifiRoot, next);
                    }
                }
                case "world.domain": {
                    /* A domain the player can whois / nslookup their way to. */
                    var domIp = __QE.fill(d.ip || "", scope) || ((questRef && questRef.Data && questRef.Data.targetIp) || "");
                    if (sdk.Network && sdk.Network.registerDomain && d.domain && domIp) {
                        /* Same reclaim as world.network: a domain the save
                           still points at an old address would otherwise win
                           (r75). */
                        reclaimDomain(d.domain, domIp);
                        sdk.Network.registerDomain(d.domain, domIp, d.vulnerabilities || []);
                        if (d.removeOnComplete !== false) questCleanup.push({ kind: "domain", domain: d.domain });
                    }
                    return next();
                }
                case "world.firewall": {
                    var fwIp = __QE.fill(d.ip || "", scope);
                    if (sdk.Network && sdk.Network.addFirewallRule && fwIp && d.rule) {
                        var rule = {
                            allowed: !!d.rule.allowed,
                            port: Number(d.rule.port || 0),
                        };
                        if (d.rule.source) rule.source = __QE.fill(d.rule.source, scope);
                        if (d.rule.destination) rule.destination = __QE.fill(d.rule.destination, scope);
                        if (d.rule.locked != null) rule.locked = !!d.rule.locked;
                        sdk.Network.addFirewallRule(fwIp, rule);
                        if (d.removeOnComplete !== false) questCleanup.push({ kind: "firewall", ip: fwIp, port: rule.port });
                    }
                    return next();
                }
                case "world.port": {
                    var portIp = __QE.fill(d.ip || "", scope);
                    var portNo = Number((d.port && d.port.external) || 0);
                    if (sdk.Network && portIp && portNo) {
                        if (d.action === "open" && sdk.Network.openPort) sdk.Network.openPort(portIp, portNo);
                        if (d.action === "close" && sdk.Network.closePort) sdk.Network.closePort(portIp, portNo);
                        if (d.action === "add" && sdk.Network.addPort) {
                            var np = {
                                external: portNo,
                                internal: Number(d.port.internal || portNo),
                                active: d.port.active !== false,
                            };
                            if (d.port.service) np.service = d.port.service;
                            if (d.port.version) np.version = d.port.version;
                            sdk.Network.addPort(portIp, np);
                        }
                        if (d.action === "remove" && sdk.Network.removePort) sdk.Network.removePort(portIp, portNo);
                        /* Put it back the way it was found, if asked. */
                        if (d.restoreOnComplete) {
                            questCleanup.push({ kind: "port", ip: portIp, port: portNo, action: d.action });
                        }
                    }
                    return next();
                }
                case "world.database": {
                    if (sdk.Database && sdk.Database.create && d.host) {
                        /* Authors write plain values; the engine's cell shape
                           is { value, type }. Shape them here, so what ships
                           matches DatabaseCellDefinition either way. */
                        var tables = {};
                        (d.tables || []).forEach(function (t) {
                            if (!t.name) return;
                            tables[t.name] = (t.rows || []).map(function (row) {
                                var shaped = {};
                                Object.keys(row || {}).forEach(function (col) {
                                    var v = row[col];
                                    shaped[col] = { value: v, type: typeof v === "number" ? "number" : "string" };
                                });
                                return shaped;
                            });
                        });
                        var dbId = sdk.Database.create({
                            host: __QE.fill(d.host, scope),
                            user: d.user || "",
                            password: d.password || "",
                            tables: tables,
                        });
                        if (d.removeOnComplete !== false && dbId) questCleanup.push({ kind: "database", id: dbId });
                    }
                    return next();
                }
                case "world.toolResponse": {
                    /* Shell.addCommandData(command, input, data): the input is
                       the thing the player typed after the command, and the
                       data is a SHAPE the tool understands, not a block of
                       text. Passing the text as the input (which is what this
                       used to do) throws inside the engine and takes the rest
                       of the quest with it. */
                    if (sdk.Shell && sdk.Shell.addCommandData) {
                        var trInput = __qeCommandInput(d, scope);
                        /* Scripted tool answers live in the SAVE, the same as
                           networks do, and an older build's answer wins over a
                           new one for the same input. QA scanned a fresh random
                           address and whois still printed the address a
                           previous export had written - so the scan objective
                           could never match.

                           removeCommandData returns void, not a promise, so
                           clearing first is safe here: no race, no await, and
                           the quest keeps its permissions. */
                        if (sdk.Shell.removeCommandData) {
                            __QE.safe(function () { sdk.Shell.removeCommandData(d.command, trInput); });
                        }
                        sdk.Shell.addCommandData(d.command, trInput, __qeCommandData(d.command, __QE.fill(d.dataText || "", scope)));
                        if (d.removeOnComplete !== false) {
                            questCleanup.push({ kind: "commandData", command: d.command, input: trInput });
                        }
                    }
                    return next();
                }
                case "world.packData": {
                    /* Community data (r137): one SharedStorage write declared
                       by a tool pack. The pack's entry template travels IN the
                       node (snapshotted at authoring time), so this works even
                       where the pack itself is not loaded. Contract fields are
                       substituted first (JSON-safe: text becomes a string,
                       numbers and booleans land raw), then __QE.fill resolves
                       any story tokens ({{data.targetIp}}) left in the values,
                       and only then is the JSON parsed - so a malformed
                       template fails here, inside our own try, instead of
                       taking the quest down. The substitution itself is the
                       shared __QE.packFill helper (see the QE runtime). */
                    if (sdk.SharedStorage && sdk.SharedStorage.set && d.storageKey) {
                        __QE.safe(function () {
                            var entry = __QE.packFill(scope, d.fields, d.values, d.entry == null ? {} : d.entry);
                            if (d.merge === "overwrite") {
                                sdk.SharedStorage.set(d.storageKey, entry);
                            } else {
                                var cur = sdk.SharedStorage.get ? sdk.SharedStorage.get(d.storageKey) : null;
                                var list = Array.isArray(cur) ? cur.slice() : [];
                                if (d.mergeBy) {
                                    list = list.filter(function (x) {
                                        return !x || x[d.mergeBy] !== entry[d.mergeBy];
                                    });
                                }
                                list.push(entry);
                                sdk.SharedStorage.set(d.storageKey, list);
                            }
                            __QE.log("Community data set: " + d.storageKey);
                        });
                    }
                    return next();
                }
                case "pack.node": {
                    /* Community nodes (r138): a pack-authored node is a
                       declarative emitter - pure config snapshotted into the
                       node, interpreted here. Four emitters over one shared
                       template filler (__QE.packFill / __QE.packText), the
                       whole thing inside __QE.safe so a malformed template
                       can never take the quest down. */
                    __QE.safe(function () {
                        var flds = d.fields || [];
                        var vals = d.values || {};
                        if (d.emitter === "storage" && sdk.SharedStorage && sdk.SharedStorage.set && d.storageKey) {
                            var pkEntry = __QE.packFill(scope, flds, vals, d.entry == null ? {} : d.entry);
                            if (d.merge === "overwrite") {
                                sdk.SharedStorage.set(d.storageKey, pkEntry);
                            } else {
                                var pkCur = sdk.SharedStorage.get ? sdk.SharedStorage.get(d.storageKey) : null;
                                var pkList = Array.isArray(pkCur) ? pkCur.slice() : [];
                                if (d.mergeBy) {
                                    pkList = pkList.filter(function (x) {
                                        return !x || x[d.mergeBy] !== pkEntry[d.mergeBy];
                                    });
                                }
                                pkList.push(pkEntry);
                                sdk.SharedStorage.set(d.storageKey, pkList);
                            }
                            __QE.log("Community data set: " + d.storageKey);
                        }
                        if (d.emitter === "emit" && sdk.Events && sdk.Events.emit && d.eventName) {
                            sdk.Events.emit(
                                __QE.packText(scope, flds, vals, d.eventName),
                                __QE.packFill(scope, flds, vals, d.payload == null ? {} : d.payload),
                            );
                            __QE.log("Community event fired: " + d.eventName);
                        }
                        if (d.emitter === "commandData" && sdk.Shell && sdk.Shell.addCommandData && d.command) {
                            var pkCmd = __QE.packText(scope, flds, vals, d.command);
                            var pkInput = __QE.packText(scope, flds, vals, d.input || "");
                            /* Scripted answers live in the SAVE: clear the old
                               one first, exactly like world.toolResponse. */
                            if (sdk.Shell.removeCommandData) sdk.Shell.removeCommandData(pkCmd, pkInput);
                            sdk.Shell.addCommandData(pkCmd, pkInput, __QE.packFill(scope, flds, vals, d.data == null ? {} : d.data));
                            __QE.log("Community tool answer placed: " + d.command);
                        }
                        if (d.emitter === "sdk" && (d.steps || []).length) {
                            (d.steps || []).forEach(function (s) {
                                var parts = String(s.call || "").split(".");
                                var obj = sdk;
                                for (var i = 0; i < parts.length - 1 && obj; i++) obj = obj[parts[i]];
                                var method = obj ? obj[parts[parts.length - 1]] : null;
                                if (typeof method !== "function") {
                                    __QE.log("Community node: the SDK has no " + s.call + " - skipped");
                                    return;
                                }
                                var args = (s.args || []).map(function (a) {
                                    return typeof a === "string"
                                        ? __QE.packText(scope, flds, vals, a)
                                        : __QE.packFill(scope, flds, vals, a);
                                });
                                method.apply(obj, args);
                            });
                        }
                    });
                    return next();
                }
                case "comms.tweet": {
                    postTweets(node, scope);
                    return next();
                }
                case "comms.dialogue": {
                    /* Timed chat → play it here, message by message, so a
                       conversation can land on a Sequence beat. */
                    if (liveChat[node.id]) return sendChatNow(node, scope).then(next);
                    if (d.kind === "mail" && mailIndex[node.id] != null) {
                        sendQuestMail(node, scope);
                    }
                    if (d.kind === "phone") {
                        var branchName = d.phone && d.phone.branch ? d.phone.branch : "default";
                        var startIndex = d.phone && d.phone.startIndex ? d.phone.startIndex : 0;
                        var waitForEnd = !(d.phone && d.phone.continueMode === "immediate");
                        if (!questRef || typeof questRef.createDialog !== "function") {
                            __QE.log("phone dialogue \"" + branchName + "\" could not start; continuing flow");
                            return next();
                        }
                        if (waitForEnd) {
                            var continued = false;
                            var continueOnce = function () {
                                if (continued) return;
                                continued = true;
                                __QE.log("phone dialogue \"" + branchName + "\" ended; continuing flow");
                                return next();
                            };
                            var callbacks = installDialog(scope, continueOnce);
                            questRef.createDialog(branchName, startIndex);
                            if (!callbacks) {
                                __QE.log("phone dialogue \"" + branchName + "\" has no ending line; continuing flow now");
                                return next();
                            }
                            return undefined;
                        }
                        installDialog(scope, null);
                        questRef.createDialog(branchName, startIndex);
                    }
                    return next();
                }
                case "fx.completeQuest":
                    requestQuestEnd("complete");
                    return undefined;
                case "fx.retireQuest":
                    requestQuestEnd("retire");
                    return undefined;
                case "fx.unclaimQuest": {
                    var unclaimName = __QE.fill(d.questName || qd.name, scope);
                    requestQuestEnd("unclaim", unclaimName);
                    return undefined;
                }
                case "fx.notify": {
                    var notifyMsg = __QE.fill(d.message, scope);
                    if (sdk.UI) {
                        /* toast takes the tone as its second argument; it used
                           to be dropped, so every toast looked the same. */
                        if (d.variant === "toast" && sdk.UI.toast) sdk.UI.toast(notifyMsg, d.tone || "info");
                        else if (sdk.UI.notify) sdk.UI.notify(notifyMsg);
                    }
                    return next();
                }
                case "fx.prompt": {
                    var runPromptOut = function (handle, nextCtx) {
                        return __QE.seq(flowOuts(nodeId).filter(function (e) { return e.sourceHandle === handle; }), function (e) {
                            return runFlow(e.target, nextCtx, depth + 1);
                        }, function (e) {
                            __QE.log("flow after Ask player stopped: " + (e && e.message ? e.message : e));
                        });
                    };
                    var promptOptions = {};
                    var title = __QE.fill(d.title || "", scope).trim();
                    var label = __QE.fill(d.label || "", scope).trim();
                    var placeholder = __QE.fill(d.placeholder || "", scope);
                    var defaultValue = __QE.fill(d.defaultValue || "", scope);
                    if (title) promptOptions.title = title;
                    if (label) promptOptions.label = label;
                    if (placeholder) promptOptions.placeholder = placeholder;
                    if (defaultValue) promptOptions.defaultValue = defaultValue;
                    if (d.password) promptOptions.password = true;
                    if (sdk.UI && sdk.UI.prompt) {
                        return Promise.resolve(sdk.UI.prompt(promptOptions)).then(function (answer) {
                            if (answer === null || answer === undefined) return runPromptOut("cancel", ctx);
                            var text = String(answer);
                            var key = String(d.storeAs || "").trim();
                            if (key && questRef && questRef.SetData) questRef.SetData(key, text);
                            var nextCtx = {
                                payload: ctx && ctx.payload ? ctx.payload : {},
                                vars: Object.assign({}, (ctx && ctx.vars) || {}, { answer: text }),
                            };
                            var handle = __QE.matchPrompt(d, text, scopeOf(nextCtx)) ? "success" : "failure";
                            return runPromptOut(handle, nextCtx);
                        }, function (e) {
                            __QE.log("Ask player failed: " + (e && e.message ? e.message : e));
                            return runPromptOut("cancel", ctx);
                        });
                    }
                    __QE.log("Ask player skipped: UI.prompt is unavailable");
                    return runPromptOut("cancel", ctx);
                }
                case "fx.handbook": {
                    /* No permission needed: the SDK's permission list has no
                       handbook entry, and open() only reads. A blank article
                       does nothing — the export warns about it instead. */
                    if (sdk.Handbook && sdk.Handbook.open) {
                        var articleId = __QE.fill(d.articleId || "", scope).trim();
                        if (articleId) {
                            var articleCat = __QE.fill(d.category || "", scope).trim();
                            if (articleCat) sdk.Handbook.open(articleId, articleCat);
                            else sdk.Handbook.open(articleId);
                        }
                    }
                    return next();
                }
                case "fx.setData":
                    questRef.SetData(d.key, __QE.fill(d.value, scope));
                    return next();
                case "fx.claimQuest":
                    /* Quest.claim is a static on the Quest class, and the field
                       is questName - this used to pass an undefined "quest". */
                    if (sdk.Quest && sdk.Quest.claim && d.questName) sdk.Quest.claim(d.questName);
                    return next();
                case "fx.pay":
                case "fx.withdraw": {
                    if (sdk.Bank) {
                        var amount = d.amountMode === "percent"
                            ? Math.round(((sdk.Bank.getBalance ? sdk.Bank.getBalance() : 0) * Number(d.percent || 0)) / 100)
                            : Number(d.amount || 0);
                        if (amount > 0) {
                            var tx = { amount: amount, description: __QE.fill(d.description || "", scope) };
                            /* BankTransactionOptions.from is { IBAN, name }. The
                               editor has always asked for both and the compiler
                               dropped them, so every payment came from nobody
                               (audit, r67). The engine wants both halves or
                               neither. */
                            var fromName = __QE.fill(d.fromName || "", scope);
                            var fromIBAN = __QE.fill(d.fromIBAN || "", scope);
                            if (fromName || fromIBAN) tx.from = { IBAN: fromIBAN, name: fromName };
                            if (node.type === "fx.pay" && sdk.Bank.transaction) sdk.Bank.transaction(tx);
                            if (node.type === "fx.withdraw" && sdk.Bank.withdraw) sdk.Bank.withdraw(tx);
                        }
                    }
                    return next();
                }
                case "world.files":
                    /* Only the player's own machine can be written to from
                       here: Files.* resolves against the current session, and
                       at quest start there is no remote one. Files for a remote
                       device belong in that device's tree, where the engine
                       mounts them before anyone connects. */
                    if (d.target === "player" && sdk.Files && sdk.Files.createTree) {
                        return Promise.resolve(sdk.Files.createTree(d.parentPath || "~/", mapFiles(d.files)))
                            .then(next, next);
                    }
                    return next();
                case "fx.shell": {
                    /* The SDK calls it exec(); execute() never existed, so this
                       node quietly did nothing at all. */
                    var shellCmd = __QE.fill(d.command, scope);
                    if (sdk.Shell && sdk.Shell.exec) return Promise.resolve(sdk.Shell.exec(shellCmd)).then(next, next);
                    if (sdk.Shell && sdk.Shell.execute) sdk.Shell.execute(shellCmd);
                    return next();
                }
                case "flow.debug": {
                    /* A checkpoint the author dropped into the chain. Most of
                       the hard bugs in this project were invisible - the mod
                       ran, nothing errored, and nothing happened - so this
                       prints three things that were each expensive to learn
                       the hard way: that the flow reached here at all, what
                       the event really carried (field names included, since
                       the declared shape is not always the real one), and what
                       the quest has saved. */
                    var dbgLabel = __QE.fill(d.label || "", scope) || node.id;
                    var dbgParts = ["reached \"" + dbgLabel + "\""];
                    if (d.includePayload !== false) {
                        var pay = ctx && ctx.payload;
                        var hasPayload = pay != null && (typeof pay !== "object" || Object.keys(pay).length > 0);
                        dbgParts.push("event: " + (hasPayload ? __QE.describe(pay) : "(none - not reached from a trigger)"));
                    }
                    if (d.includeData !== false) {
                        /* Report what a {{token}} here would ACTUALLY resolve
                           against, which after the engine retires a completed
                           quest is the remembered copy rather than the live
                           one. Printing "undefined" while tokens still filled
                           correctly would send the next reader down the wrong
                           path. */
                        var live = questRef && questRef.Data;
                        dbgParts.push("saved: " + __QE.describe(live || lastKnownData || null) +
                            (live ? "" : " (quest data already retired; using the last known values)"));
                    }
                    __QE.log(dbgParts.join(" | "));
                    if (d.toast && sdk.UI && sdk.UI.toast) {
                        __QE.safe(function () { sdk.UI.toast("debug: " + dbgLabel, "info"); });
                    }
                    return next();
                }
                case "flow.delay":
                    return __QE.sleep(Math.max(0, Number(d.seconds || 0)) * 1000).then(next);
                case "flow.timer": {
                    var timerMode = d.mode || "after";
                    if (timerMode === "after") {
                        var schedTotal = (Number(d.years) || 0) + (Number(d.months) || 0) +
                            (Number(d.weeks) || 0) + (Number(d.days) || 0) +
                            (Number(d.hours) || 0) + (Number(d.minutes) || 0);
                        if (schedTotal <= 0) {
                            /* Fail-open like the rest of the codebase: a
                               timer with no time set fires immediately
                               instead of stranding the story (analysis
                               already warns about it). */
                            __QE.log("timer node " + nodeId + ": nothing scheduled - the timer fires immediately");
                            return next();
                        }
                    } else {
                        /* Date modes (r173): fail open when no full date is
                           set, or when the due time has already passed by
                           the time the story arrives. */
                        var dueAt = computeTimerFireAt(d);
                        if (dueAt === null) {
                            __QE.log("timer node " + nodeId + ": no full date set - the timer fires immediately");
                            return next();
                        }
                        if (sdk.Time && sdk.Time.now && dueAt <= sdk.Time.now()) {
                            __QE.log("timer node " + nodeId + ": due time already passed - the timer fires immediately");
                            return next();
                        }
                    }
                    armBeat(nodeId, d);
                    /* The flow ends here: the story continues down "Out"
                       when the in-game clock reaches the due time (r172). */
                    return Promise.resolve();
                }
                case "flow.sequence": {
                    /* Fire each output in author order, pausing the step's own
                       delay (milliseconds) before it. Steps own their sockets:
                       socket id is "step-" + step.id. */
                    var seqSteps = d.steps || [];
                    var seqOuts = flowOuts(nodeId);
                    return seqSteps.reduce(function (chain, step) {
                        return chain.then(function () {
                            var wait = Math.max(0, Number(step.delayMs || 0));
                            var handleId = "step-" + step.id;
                            var branch = seqOuts.filter(function (e) { return e.sourceHandle === handleId; });
                            var fire = function () {
                                return branch.reduce(function (p, e) {
                                    return p.then(function () { return runFlow(e.target, ctx, depth + 1); });
                                }, Promise.resolve());
                            };
                            return wait > 0 ? __QE.sleep(wait).then(fire) : fire();
                        });
                    }, Promise.resolve());
                }
                case "objective":
                    /* When the story flow reaches an objective, tick it off.
                       (Objectives with a trigger event complete via the SDK
                       declarative trigger instead.) */
                    if (d.name && questRef && questRef.completeObjective) questRef.completeObjective(d.name);
                    return next();
                case "trigger.event":
                case "entry.start":
                case "entry.load":
                case "entry.complete":
                case "entry.abandon":
                    return next();
                case "reply.input":
                    /* The player has to answer before the story continues: the
                       flow pauses here, and the terminal command this node
                       registers resumes it down "Correct" or "Wrong". */
                    return Promise.resolve();
                default:
                    return next();
            }
        }

        /* Editor file entries → the SDK's NetworkFileMap/FileDefinition: drop
           the editor's own id, and "locked" is the engine's "readonly". */
        function mapFiles(list) {
            return (list || []).map(function (f) {
                var o = { name: f.name };
                if (f.extension) o.extension = f.extension;
                if (f.data) o.data = f.data;
                if (f.isFolder) o.isFolder = true;
                if (f.locked) o.readonly = true;
                if (f.hidden) o.hidden = true;
                if (f.children && f.children.length) o.children = mapFiles(f.children);
                return o;
            });
        }

        /* What the player types after the command. Most tools are keyed by a
           single string (an ip or a domain); hydra/ssh/ftp are keyed by an
           object, per the SDK's CommandDataMap. */
        function __qeCommandInput(d, scope) {
            var input = __QE.fill(d.input || "", scope);
            var user = __QE.fill(d.inputUser || "", scope);
            var target = __QE.fill(d.inputTarget || "", scope) || input;
            if (d.command === "hydra") return { user: user, target: target };
            if (d.command === "ssh") return { host: target, key: user };
            if (d.command === "ftp") return { host: target, username: user, password: "" };
            return input;
        }

        /* Authors write the response as readable lines; the engine wants the
           shape its own tool returns. JSON is passed through untouched for
           anyone who wants exact control. */
        function __qeKeyValueLines(text) {
            var out = [];
            String(text).split("\n").forEach(function (line) {
                var at = line.indexOf(":");
                if (at <= 0) return;
                var key = line.slice(0, at).trim().toLowerCase();
                var value = line.slice(at + 1).trim();
                if (key && value) out.push([key, value]);
            });
            return out;
        }

        function __qeCommandData(command, text) {
            var trimmed = String(text || "").trim();
            if (!trimmed) return command === "ping" ? true : {};
            if (trimmed.charAt(0) === "{" || trimmed.charAt(0) === "[") {
                try { return JSON.parse(trimmed); } catch (e) { /* not JSON after all */ }
            }
            var pairs = __qeKeyValueLines(trimmed);
            var get = function (names) {
                for (var i = 0; i < pairs.length; i++) {
                    if (names.indexOf(pairs[i][0]) >= 0) return pairs[i][1];
                }
                return "";
            };
            if (command === "ping") return !/false|down|unreachable/i.test(trimmed);
            if (command === "nslookup" || command === "mxlookup") {
                return get(["ip", "address", "answer"]) || trimmed.split("\n")[0].trim();
            }
            if (command === "whois") {
                var whois = {};
                var domain = get(["domain", "domain name"]);
                var ip = get(["ip", "ip address", "address"]);
                var contact = get(["registrant", "contact", "owner", "organisation", "organization"]);
                var email = get(["email", "e-mail", "abuse"]);
                if (domain) whois.domain = domain;
                if (ip) whois.ip = ip;
                if (contact) whois.contact = contact;
                if (email) whois.email = email;
                whois.status = !/status:\s*(inactive|expired|false)/i.test(trimmed);
                return whois;
            }
            if (command === "geoip") {
                return {
                    country: get(["country"]),
                    city: get(["city"]),
                    latitude: get(["latitude", "lat"]),
                    longitude: get(["longitude", "lon", "lng"]),
                };
            }
            if (command === "lynx") {
                var lynx = { additional: [] };
                pairs.forEach(function (pair) {
                    var key = pair[0];
                    var value = pair[1];
                    if (key.indexOf("mail") >= 0) {
                        lynx.contact = lynx.contact || {};
                        lynx.contact.emails = (lynx.contact.emails || []).concat(value.split(/[,;]\s*/));
                    } else if (key.indexOf("phone") >= 0) {
                        lynx.contact = lynx.contact || {};
                        lynx.contact.phones = (lynx.contact.phones || []).concat(value.split(/[,;]\s*/));
                    } else if (key === "ip" || key === "ips") {
                        lynx.ips = (lynx.ips || []).concat(value.split(/[,;]\s*/));
                    } else if (key.indexOf("address") >= 0 || key === "location") {
                        lynx.address = (lynx.address || []).concat([value]);
                    } else if (key.indexOf("social") >= 0) {
                        lynx.socialMedia = (lynx.socialMedia || []).concat(value.split(/[,;]\s*/));
                    } else {
                        var record = {};
                        record[pair[0]] = value;
                        lynx.additional.push(record);
                    }
                });
                return lynx;
            }
            if (command === "nmap") {
                var ports = [];
                String(trimmed).split("\n").forEach(function (line) {
                    /* "22/tcp open ssh OpenSSH 7.2" and "22 open ssh" both.
                       The status has to be a whole word: a greedy match found
                       the "Open" inside "OpenSSH" and read the rest wrong. */
                    var m = /^\s*(\d{1,5})(?:\/\w+)?\s+(open|closed?|filtered|forwarded)\b\s*([a-z0-9_.-]*)\s*(.*)$/i.exec(line);
                    if (!m) return;
                    var status = m[2].toUpperCase();
                    if (status === "CLOSED") status = "CLOSE";
                    var port = { port: Number(m[1]), status: status, service: m[3] || "" };
                    if (m[4] && m[4].trim()) port.version = m[4].trim();
                    ports.push(port);
                });
                return ports;
            }
            if (command === "hydra") {
                return { credentials: { username: get(["username", "user"]), password: get(["password", "pass"]) } };
            }
            if (command === "ssh") {
                return { ip: get(["ip", "host"]), status: /close|refus|denied/i.test(trimmed) ? "CLOSE" : "OPEN" };
            }
            var generic = {};
            pairs.forEach(function (pair) { generic[pair[0]] = pair[1]; });
            return pairs.length ? generic : trimmed;
        }

        /* Build a device's user list the way the engine expects to find it.

           The SSH exploit does not log in as whoever happens to be listed. It
           looks for a guest account or a user who is ONLINE, and says so when
           it finds neither:

               [*] Attack failed.
               [*] No guest account or online user found.

           QA hit exactly that with a device that had a perfectly good named
           user on it. The working reference mod never hands over a bare array:
           every one of its 25 machines wraps its users in
           createDefaultUserSchema(users, { guest: true }), which is what adds
           the accounts the engine actually attacks.

           So we do the same, and mark the author's own users online unless they
           deliberately said otherwise - a machine nobody is logged into cannot
           be broken into through a login service, which is not a distinction
           the editor ever offered to make. */
        /* Create a network at an address.

           The engine writes networks into the SAVE, and one already there WINS
           over a new one created at the same address (r52/r55) - so a
           re-exported mod cannot replace its own machines unless the old one
           is destroyed first. But destroyNetwork returns a PROMISE, and the
           engine only grants a mod its permissions while it is inside a call
           the engine made (r45). Three rounds died between those two facts:

             r55  destroy, create next line   -> destroy resolved AFTER the
                  create and deleted the new network
             r56  await the destroy           -> the create then ran past an
                  await, outside the permission window: Mod "null"
             r71  do not destroy at all       -> the stale network wins, which
                  is what QA hit

           The addresses are cleared in OnModPackageLoaded instead. The SDK
           declares it "void | Promise<void>", so it may await, and it runs
           once when the mod loads - before any quest starts. By the time a
           quest's OnStart runs, the destroys have settled, and the create is a
           single synchronous call inside its own permission window. */
        function buildNetwork(ip, definition, next) {
            void ip;
            sdk.Network.createSubnetNetwork(definition);
            return next();
        }

        /* Every domain name anywhere in a device tree. A quest's server is
           usually the only one that carries a domain, but a router with
           children may carry several, and each one has to be reclaimed. */
        function domainsOf(dev, into) {
            var acc = into || [];
            if (!dev) return acc;
            if (dev.domainName) acc.push(dev.domainName);
            (dev.children || []).forEach(function (c) { domainsOf(c, acc); });
            return acc;
        }

        /* The ledger of what THIS mod has created in THIS save.

           SaveStorage is per-save and namespaced per-mod ("Each mod has its
           own isolated namespace within the save"), it travels with the save
           and it is deleted when the save is. That makes it the one place we
           can record ownership honestly: a domain listed here was registered
           by this mod, in this playthrough, and by nobody else.

           Without this, reclaiming was a guess. Zeis asked the right question
           in r75: the first cut looked up whoever owned a domain and destroyed
           them, which on a name collision would have deleted another mod's -
           or the base game's - network. A display bug is not worth that. */
        var LEDGER_KEY = "qe.ownedDomains";

        function ownedDomains() {
            var got = __QE.safe(function () {
                return sdk.SaveStorage && sdk.SaveStorage.get ? sdk.SaveStorage.get(LEDGER_KEY) : null;
            });
            return got && typeof got === "object" && typeof got.length === "number" ? got : [];
        }

        function rememberDomain(domain, ip) {
            if (!domain || !sdk.SaveStorage || !sdk.SaveStorage.set) return;
            __QE.safe(function () {
                var list = ownedDomains().filter(function (e) { return e && e.domain !== domain; });
                list.push({ domain: domain, ip: ip });
                sdk.SaveStorage.set(LEDGER_KEY, list);
                return "";
            });
        }

        /* What this mod recorded for a domain, or "" if it never claimed it. */
        function ownedIpFor(domain) {
            var list = ownedDomains();
            for (var i = 0; i < list.length; i++) {
                if (list[i] && list[i].domain === domain) return list[i].ip || "";
            }
            return "";
        }

        /* Take a domain back from whatever is currently answering for it.

           This is the r75 fault, and it is NOT the same bug as the
           same-address collision above. The addresses are allocated by the
           game per playthrough now (r73), so this mod can never collide with
           its own old IP - but the DOMAIN is still written by the author, so
           it is identical in every build. QA's save still held the network an
           older Harbour created at its hardcoded 203.0.113.47, still owning
           harbourline-logistics.com, and the game resolved whois against that
           subnet rather than the freshly created one. The log proved the new
           network and the new scripted whois answer were both registered
           correctly - they were simply not the ones being asked.

           The engine's own PruneOrphanQuests drops the stale QUEST record (QA
           saw it drop "TheHarbourManifest5") but leaves the network standing,
           so nothing else will ever clear it.

           Safety, and why this is not r55 again: r55 destroyed the address it
           was about to create at, and the promise resolved after the create
           and deleted the new network. Here we only ever destroy an address
           that is DIFFERENT from the one this run uses - if the domain already
           points at our own new ip there is nothing to do - so the destroy
           cannot race the create no matter when it settles. It is deliberately
           not awaited, for the same reason: an await would push the create
           outside the permission window (r56), and a promise the game has to
           await may never settle (r72). */
        function reclaimDomain(domain, keepIp) {
            if (!domain || !sdk.Network) return;
            var found = __QE.safe(function () {
                if (sdk.Network.getSubnetByDomain) return sdk.Network.getSubnetByDomain(domain);
                if (sdk.Network.resolveDomain) return sdk.Network.resolveDomain(domain);
                return "";
            });
            var oldIp = found && typeof found === "object" ? (found.ip || "") : "";
            if (!oldIp || oldIp === keepIp) {
                rememberDomain(domain, keepIp);
                return;
            }

            /* Only ever reclaim what this mod can PROVE it left behind.

               The ledger is the proof. If this save has no record of us
               registering this domain, the network answering for it belongs to
               somebody else - another mod, or the base game - and destroying
               it would be data loss in someone else's content. Leave it
               standing and say so: the author picked a name that is already
               taken, and that is a thing they need to know and can fix, not a
               thing we should resolve by deleting a stranger's server. */
            var mine = ownedIpFor(domain);
            if (mine !== oldIp) {
                __QE.log("domain \"" + domain + "\" is already registered to " + oldIp +
                    ", and this mod has no record of creating it - so it belongs to the base game " +
                    "or another mod. Leaving it alone: this quest's server at " + (keepIp || "its address") +
                    " will NOT answer to that name. Pick a domain name nothing else uses.");
                return;
            }

            __QE.log("domain \"" + domain + "\" was still pointing at " + oldIp +
                " (a network this mod created in an earlier run); reclaiming it for " +
                (keepIp || "this quest"));
            __QE.safe(function () {
                if (sdk.Network.removeDomain) sdk.Network.removeDomain(domain);
            });
            __QE.safe(function () {
                if (sdk.Network.destroyNetwork) {
                    var p = sdk.Network.destroyNetwork(oldIp);
                    /* Never hand this promise on, and never let a rejection
                       escape as an unhandled one. */
                    if (p && typeof p.then === "function") p.then(function () {}, function () {});
                }
                return "";
            });
            /* The claim moves to the new address. */
            rememberDomain(domain, keepIp);
        }

        function mapUsers(dev) {
            var made = (dev.users || []).map(function (u) {
                var o = sdk.Network.createUser
                    ? sdk.Network.createUser({ username: u.username, password: u.password })
                    : { username: u.username, password: u.password };
                if (u.firstName) o.firstName = u.firstName;
                if (u.lastName) o.lastName = u.lastName;
                /* A user's files mount in their home directory — this is the
                   only way to put a file on a remote machine before the
                   player ever connects to it. */
                if (u.files && u.files.length) o.files = mapFiles(u.files);
                o.online = u.online == null ? true : !!u.online;
                if (u.acceptReverseTCP != null) o.acceptReverseTCP = !!u.acceptReverseTCP;
                if (u.emailAddress) o.email = { address: u.emailAddress, password: u.emailPassword || "" };
                return o;
            });
            var kind = String(dev.type || "").toUpperCase();
            /* Only DEVICES get the default schema.

               The reference mod is precise about this: 25 createDefaultUserSchema
               calls across 26 Devices, and none at all on its 7 Routers, which
               carry a plain list of named accounts. r53 applied it everywhere,
               which put a guest account on the edge router - and the SSH
               exploit then logged in as the easiest account it could find:

                   [*] UID: uid=0(guest) gid=0(guest).
                   [*] Found shell.

               A guest shell is not the way in the quest intends. The named
               account with acceptReverseTCP is, and that is what the exploit
               reaches once guest is not sitting in front of it. Routers,
               splitters and firewalls keep exactly the accounts the author
               wrote. */
            if (kind !== "DEVICE") return made;
            /* ...but only when the author wants them.

               QA, r78: a quest whose story has exactly ONE account on the
               server ("It is a server, so the account you land in already owns
               the files") showed root, guest and admin in meterpreter, and the
               exploit dropped the player into guest - so they had to crack a
               password the quest never intended them to touch. That is the
               advanced template's job, not this one's.

               The schema default is on, so nothing that relied on the old
               behaviour changes; an author who wants the single-account server
               the story describes now turns it off. */
            if (dev.extraAccounts === false) return made;
            if (sdk.Network.createDefaultUserSchema) {
                return sdk.Network.createDefaultUserSchema(made, { guest: true });
            }
            return made;
        }

        function mapDevice(dev) {
            var out = {
                ip: dev.ip,
                type: dev.type,
                ports: (dev.ports || []).map(function (p) {
                    var o = { external: p.external, internal: p.internal, active: !!p.active, service: p.service };
                    /* "locked" was in the schema and the inspector but never
                       reached the engine. The reference mod is consistent
                       about it: a router's web port is locked, and the SSH
                       port the player is meant to exploit is explicitly
                       unlocked. Send whatever the author chose. */
                    if (p.locked != null) o.locked = !!p.locked;
                    if (p.version) o.version = p.version;
                    return o;
                }),
                users: mapUsers(dev),
            };
            /* The SDK's device definition is a discriminated union, and only
               some arms carry some fields: children belongs to Router and
               Splitter, rules to Firewall, model/accessable to Router. We used
               to attach all of them to everything, so a plain DEVICE went out
               with an empty children array and an empty rules array. Nemesis,
               which builds far larger networks than we do, never does that -
               its DEVICE objects carry only ip/type/name/users/ports/rootFiles
               /domain. Sending fields an arm does not declare is exactly the
               kind of thing this engine ignores silently, so each field is now
               attached only where it belongs. */
            var kind = String(dev.type || "").toUpperCase();
            if (kind === "ROUTER" || kind === "SPLITTER") {
                out.children = (dev.children || []).map(mapDevice);
            }
            if (kind === "FIREWALL") {
                out.rules = dev.rules || [];
            }
            if (dev.name) out.name = dev.name;
            if (dev.lanIp) out.lanIp = dev.lanIp;
            if (dev.isIpHidden != null) out.isIpHidden = !!dev.isIpHidden;
            /* model/accessable are Router-only in the union. */
            if (kind === "ROUTER") {
                if (dev.model) out.model = dev.model;
                if (dev.accessable != null) out.accessable = dev.accessable;
            }
            if (dev.rootFiles && dev.rootFiles.length) out.rootFiles = mapFiles(dev.rootFiles);
            if (dev.files && dev.files.length) out.rootFiles = (out.rootFiles || []).concat(mapFiles(dev.files));
            /* The engine takes a domain as { name, vulnerabilities }, not a
               bare string, and has no place for vulnerabilities outside it. */
            if (dev.domainName) {
                out.domain = { name: dev.domainName };
                if (dev.vulnerabilities && dev.vulnerabilities.length) out.domain.vulnerabilities = dev.vulnerabilities;
            }
            return out;
        }

        var QC = (function () {
            var cls = class extends sdk.Quest {
                constructor() {
                    super(...arguments);
                    questRef = this;
                    this.Name = qd.name;
                    /* Title and Description are read at REGISTRATION, so a
                       {{tr.…}} in either has to be resolved right here - later
                       is too late. Everything else keeps its token until it is
                       used, which is what lets {{data.…}} work at all. */
                    this.Title = __QE.fillTranslations(qd.title);
                    this.Description = __QE.fillTranslations(qd.description);
                    this.Group = qd.group;
                    /* Only assign when the author actually set rewards.
                       Assigning undefined still defines the property, and
                       Nemesis - the known-working mod - never assigns it at
                       all. Keep the emitted shape identical to a mod that
                       simply omits rewards (r84). */
                    if (qd.rewards) this.Rewards = qd.rewards;
                    if (qd.employer && Object.keys(qd.employer).length) this.Employer = qd.employer;
                    if (qd.icon) this.Icon = qd.icon;
                    /* Behaviour toggles from the quest settings. AutoStart is
                       the big one: without it the quest waits to be claimed
                       and none of the On… hooks ever run. */
                    if (qd.autoStart) this.AutoStart = true;
                    if (qd.autoComplete != null) this.AutoComplete = !!qd.autoComplete;
                    if (qd.abandonable != null) this.Abandonable = !!qd.abandonable;
                    /* Assign explicitly either way. Leaving it unset inherits
                       the engine's default; the editor setting should be the
                       only source of truth for whether the player sees this
                       manual finish button. */
                    this.HasCompleteButton = !!qd.hasCompleteButton;
                    if (qd.questsToComplete && qd.questsToComplete.length) this.QuestsToComplete = qd.questsToComplete;
                    if (qd.maxClaim != null) this.MaxClaim = qd.maxClaim;
                    if (qd.maxClaimPerDay != null) this.MaxClaimPerDay = qd.maxClaimPerDay;
                    if (qd.hackhubPost) {
                        var hp = { content: qd.hackhubPost.content };
                        if (qd.hackhubPost.media) hp.media = qd.hackhubPost.media;
                        if (qd.hackhubPost.authorName) hp.author = { name: qd.hackhubPost.authorName };
                        if (qd.hackhubPost.likes != null) hp.likes = qd.hackhubPost.likes;
                        if (qd.hackhubPost.comments && qd.hackhubPost.comments.length) {
                            hp.comments = qd.hackhubPost.comments.map(function (c) {
                                return { author: { name: c.authorName }, content: c.content };
                            });
                        }
                        this.HackhubPost = hp;
                    }
                    this.Objectives = Objectives;
                    if (Mails.length) this.Mails = Mails;
                    if (Object.keys(Dialog).length) this.Dialog = Dialog;
                    /* Only the chats that are NOT timed into the story: the
                       timed ones are played live when the flow reaches them. */
                    if (DeclaredKisscordChats.length) this.KisscordChats = DeclaredKisscordChats;
                    if (DeclaredWeeChatChats.length) this.WeeChatChats = DeclaredWeeChatChats;
                }
                CreateData() {
                    /* Required by the SDK (Quest.CreateData is abstract) even
                       when a quest has no data of its own to seed. Any
                       world.network/world.wifi node using ipMode: "random"
                       gets ONE ip allocated here, once, so it's stable
                       across reloads and shared by both the live device and
                       every {{data.targetIp}} reference. */
                    var seed = {};
                    if (needsTargetIp) seed.targetIp = sdk.Network.randomIp ? sdk.Network.randomIp() : "10.0.0.1";
                    /* The router a lone machine gets wrapped in (r77) needs an
                       address of its own, allocated once here so it survives a
                       reload - otherwise cleanup would look for a subnet at an
                       address nothing was ever created at. */
                    if (needsGateway) seed.gatewayIp = sdk.Network.randomIp ? sdk.Network.randomIp() : "10.0.0.254";
                    return seed;
                }
                OnStart() {
                    /* The engine may build this class more than once (metadata
                       pass, then the live quest). Whichever instance the engine
                       is actually calling is the one that can send mail and
                       complete objectives, so bind it here rather than trusting
                       whatever the last constructor saw. */
                    questRef = this;
                    bindBeatFire(this);
                    refillObjectives();
                    /* The story is starting (or a reload re-ran OnStart): make
                       sure the characters it needs exist. Idempotent, so a
                       second call adopts what the first one created. */
                    twotterQuestLive[qd.id] = true;
                    ensureDeclaredTwotterAccounts(qd.id);
                    var ctx = { payload: {}, vars: {} };
                    var starts = g.nodes.filter(function (n) { return n.type === "entry.start"; });
                    /* Logged unconditionally. A quest whose OnStart never runs
                       looks exactly like a quest whose OnStart ran and did
                       nothing, and telling those apart from a player's log file
                       is worth one line. */
                    __QE.log("quest \"" + qd.name + "\" started (" + starts.length +
                        " entry point" + (starts.length === 1 ? "" : "s") + ")");
                    starts.forEach(function (n) { runFlow(n.id, ctx, 0); });
                }
                OnObjectivesStart() {
                    /* The engine may build this class more than once (metadata
                       pass, then the live quest). Whichever instance the engine
                       is actually calling is the one that can send mail and
                       complete objectives, so bind it here rather than trusting
                       whatever the last constructor saw. */
                    questRef = this;
                    var self = this;
                    bindBeatFire(this);
                    __QE.log("quest \"" + qd.name + "\" objectives started");
                    /* OnObjectivesStart runs again after every reload, which is
                       exactly why the accounts are ensured here too: a save
                       loaded into a session where the account never got made
                       gets it made now. */
                    twotterQuestLive[qd.id] = true;
                    ensureDeclaredTwotterAccounts(qd.id);
                    var ctx = { payload: {}, vars: {} };
                    refillComms();
                    weechatServers.forEach(function (s) {
                        if (sdk.WeeChat && sdk.WeeChat.createServer) sdk.WeeChat.createServer(s.host, s.password);
                    });
                    g.nodes
                        .filter(function (n) { return n.type === "entry.load"; })
                        .forEach(function (n) { runFlow(n.id, ctx, 0); });
                    /* Objectives that a game event completes.

                       These used to rely on QuestObjectiveDefinition.trigger,
                       the SDK's declarative form. QA reading the contract mail
                       never ticked "Read the contract" off, which is the same
                       shape of fault as the mail bug: an API the declarations
                       describe but the build does not honour. The SDK's own
                       Quest example completes objectives the imperative way -

                           this.Events.on("Terminal.NmapScan", (data) => {
                               if (data.ip === this.Data.targetIp)
                                   this.completeObjective("scan");
                           });

                       - so that is what is emitted now: listen to the trigger's
                       event, check the author's conditions, call
                       completeObjective, then follow the "On complete" wire.
                       One listener does both jobs, so the tick and the story
                       beat after it can no longer disagree.

                       (An objective the story flow *reaches* is ticked off in
                       runFlow instead; this covers the ones the player
                       completes by playing.) */
                    objectiveNodes.forEach(function (n) {
                        var trig = g.edges
                            .filter(function (e) { return e.kind === "condition" && e.target === n.id; })
                            .map(function (e) { return byId[e.source]; })
                            .filter(function (s) { return s && s.type === "trigger.event"; })[0];
                        if (!trig) return;
                        var doneEdges = g.edges.filter(function (e) {
                            return e.source === n.id && e.kind === "flow" && e.sourceHandle === "done";
                        });
                        var fired = false;
                        /* Logged at registration, not just on completion: when
                           an objective never ticks we have to be able to tell
                           "the listener was never attached" from "it was
                           attached and the event never arrived". Round 40 cost
                           a full round for want of this line. */
                        /* Some things the player does raise one of SEVERAL
                           events depending on how they did it, and an author
                           picking one from a list cannot be expected to know
                           which. Breaking into a machine is the case QA hit
                           (r78): the objective listened for
                           Metasploit.Meterpreter.Connected, the exploit opened
                           a COMMAND SHELL instead ("Command shell session
                           opened"), no Meterpreter.Connected was ever raised,
                           and the objective never ticked - which, because the
                           whole chain is wired with unlocksAfter, left every
                           later objective "locked by unlocksAfter; queued as
                           pending" even though the player finished the quest.

                           Note the log is the proof it never arrived: an event
                           that fires but fails its conditions prints "fired but
                           did not match", and there is no such line.

                           So an access event listens for its siblings too. The
                           objective still completes once (fired guards it), and
                           a quest that names any of them behaves the same. */
                        var ACCESS_SIBLINGS = {
                            "Metasploit.Meterpreter.Connected": ["RemoteConnection.Established", "Terminal.SSH.Connected"],
                            "RemoteConnection.Established": ["Metasploit.Meterpreter.Connected", "Terminal.SSH.Connected"],
                            "Terminal.SSH.Connected": ["RemoteConnection.Established", "Metasploit.Meterpreter.Connected"],
                        };
                        var listenFor = [trig.data.event].concat(ACCESS_SIBLINGS[trig.data.event] || []);
                        __QE.log("objective \"" + n.data.name + "\" is listening for " + listenFor.join(", "));
                        var onEvent = function (data, evName) {
                            if (fired) return;
                            if (!__QE.matchAll(trig.data.conditions, data, dataScope())) {
                                /* The event arrived but the author's conditions
                                   said no. Without this line, a mistyped field
                                   name or a case mismatch is indistinguishable
                                   from the game never raising the event at all,
                                   and the log stays silent either way. Print
                                   what actually turned up so the author can see
                                   which field to match on. */
                                __QE.log("objective \"" + n.data.name + "\": " + (evName || trig.data.event) +
                                    " fired but did not match. Event carried: " + __QE.describe(data));
                                return;
                            }
                            fired = true;
                            /* Story beats FIRST, tick the objective LAST.

                               completeObjective can synchronously run engine
                               completion work before it returns. Running the
                               author's "done" wires first preserves the event
                               payload they expect and keeps closing mail,
                               payments and notifications in the same trusted
                               SDK call stack.

                               A Complete quest / Retire quest / Unclaim quest
                               node reached directly from this objective is
                               deferred until after completeObjective below, so
                               the visible objective ticks before the quest
                               entry is formally ended.

                               Deliberately NOT deferred to a timer: r45 - the
                               engine only grants this mod permissions inside a
                               call it made, and work moved to a later tick
                               loses that identity. */
                            objectiveDoneDepth++;
                            try {
                                doneEdges.forEach(function (e) {
                                    runFlow(e.target, { payload: data, vars: {} }, 0);
                                });
                            } finally {
                                objectiveDoneDepth--;
                            }
                            if (n.data.name) {
                                try {
                                    self.completeObjective(n.data.name);
                                    __QE.log("objective \"" + n.data.name + "\" completed by " + (evName || trig.data.event));
                                } catch (e) {
                                    __QE.log("could not complete objective \"" + n.data.name + "\": " +
                                        (e && e.message ? e.message : e));
                                }
                            }
                            objectivesDone++;
                            if (qd.hideObjectivesWhenDone && objectivesDone >= objectiveNodes.length) {
                                hideAllObjectives();
                            }
                            flushQuestEnd();
                        };
                        listenFor.forEach(function (evName) {
                            self.Events.on(evName, function (data) { onEvent(data, evName); });
                        });
                    });
                    g.nodes
                        .filter(function (n) {
                            if (n.type !== "trigger.event") return false;
                            return !g.edges.some(function (e) { return e.source === n.id && e.kind === "condition"; });
                        })
                        .forEach(function (n) {
                            __QE.log("trigger " + n.id + " is listening for " + n.data.event);
                            self.Events.on(n.data.event, function (data) {
                                if (!__QE.matchAll(n.data.conditions, data, dataScope())) {
                                    __QE.log("trigger " + n.id + ": " + n.data.event +
                                        " fired but did not match. Event carried: " + __QE.describe(data));
                                    return;
                                }
                                flowOuts(n.id).forEach(function (e) { runFlow(e.target, { payload: data, vars: {} }, 0); });
                            });
                        });
                }
                OnComplete() {
                    /* The engine may build this class more than once (metadata
                       pass, then the live quest). Whichever instance the engine
                       is actually calling is the one that can send mail and
                       complete objectives, so bind it here rather than trusting
                       whatever the last constructor saw. */
                    questRef = this;
                    var ctx = { payload: {}, vars: {} };
                    /* These traces stay useful for in-game QA: whichever line
                       appears last tells us which completion phase ran. */
                    __QE.log("OnComplete: starting");
                    runQuestCleanup("complete");
                    /* Pending scheduled beats belong to a story that is over
                       now: cancel them so they cannot fire later (r172). */
                    cancelBeatJobs();
                    delete liveBeats[qd.id];
                    __QE.log("OnComplete: cleanup done, removing weechat servers");
                    weechatServers.forEach(function (s) {
                        if (sdk.WeeChat && sdk.WeeChat.removeServer) sdk.WeeChat.removeServer(s.host, s.password);
                    });
                    __QE.log("OnComplete: running end-of-quest nodes");
                    g.nodes
                        .filter(function (n) { return n.type === "entry.complete"; })
                        .forEach(function (n) { runFlow(n.id, ctx, 0); });
                    __QE.log("OnComplete: finished, handing back to the game");
                }
                OnAbandon() {
                    /* The engine may build this class more than once (metadata
                       pass, then the live quest). Whichever instance the engine
                       is actually calling is the one that can send mail and
                       complete objectives, so bind it here rather than trusting
                       whatever the last constructor saw. */
                    questRef = this;
                    var ctx = { payload: {}, vars: {} };
                    /* These traces stay useful for in-game QA: whichever line
                       appears last tells us which abandon phase ran. */
                    __QE.log("OnAbandon: starting");
                    runQuestCleanup("abandon");
                    /* The player walked away: pending beats go with the
                       quest (r172). */
                    cancelBeatJobs();
                    delete liveBeats[qd.id];
                    __QE.log("OnAbandon: cleanup done, removing weechat servers");
                    weechatServers.forEach(function (s) {
                        if (sdk.WeeChat && sdk.WeeChat.removeServer) sdk.WeeChat.removeServer(s.host, s.password);
                    });
                    __QE.log("OnAbandon: running end-of-quest nodes");
                    g.nodes
                        .filter(function (n) { return n.type === "entry.abandon"; })
                        .forEach(function (n) { runFlow(n.id, ctx, 0); });
                    __QE.log("OnAbandon: finished, handing back to the game");
                }
            };
            return cls;
        })();

        sdk.RegisterQuest(QC);
        questRef = null;

        /* commands for manual-input moments and reply.input nodes */
        var inputMoments = [];
        g.nodes.forEach(function (n) {
            if (n.type === "comms.dialogue") {
                var msgs = n.data.kind === "kisscord" ? n.data.kisscord.messages : n.data.kind === "weechat" ? n.data.weechat.messages : [];
                (msgs || []).forEach(function (m, i) {
                    if (m.playerAction === "input" && m.input) {
                        inputMoments.push({ id: n.id + ":" + i, input: m.input, label: m.content || "Type your answer:" });
                    }
                });
            }
        });
        g.nodes.filter(function (n) { return n.type === "reply.input"; }).forEach(function (n) {
            inputMoments.push({ id: n.id, input: { expected: n.data.expected, matchMode: n.data.matchMode, caseSensitive: n.data.caseSensitive, failureText: n.data.failureMessage, wrongRoute: "wrong" }, label: n.data.prompt || "Your answer:", node: n });
        });

        inputMoments.forEach(function (moment) {
            /* The author's own command name wins. The editor asks for one and
               the compiler used to ignore it, so a quest that told the player
               to run "reply" registered "qe-something" instead (audit, r67).
               Fall back to a generated name only when the field is blank. */
            var authored = moment.node && moment.node.data.commandName
                ? String(moment.node.data.commandName).trim().replace(/\s+/g, "-").toLowerCase()
                : "";
            var cmdName = authored || ("qe-" + moment.id.replace(/[^a-z0-9]+/gi, "-").slice(0, 24).toLowerCase());
            var cls = class extends sdk.Command {
                constructor() {
                    super(...arguments);
                    this.CommandName = cmdName;
                    this.Description = moment.node ? moment.node.data.commandDescription || "Quest input" : "Answer to continue the conversation";
                }
                Run(tools) {
                    var input = moment.input;
                    return tools.prompt(input && input.expected && moment.node && moment.node.data.mask ? { label: moment.label, password: true } : moment.label).then(function (answer) {
                        if (__QE.matchInput(input, answer)) {
                            if (input.expected) {
                                tools.printSuccess(
                                    (moment.node && moment.node.data.successMessage) || "Correct.",
                                );
                            }
                            sdk.Events.emit("QE." + moment.id + ".ok", { answer: answer });
                            /* reply.input's sockets are "success" and "failure"
                               (registry: successOut / failureOut). This
                               followed "out", which that node does not have, so
                               a correct answer routed nowhere and everything
                               wired to Correct was dead (r70). A dialogue
                               moment has no node, so both are tried. */
                            if (moment.node) {
                                flowOutsFrom(moment.node.id, "success");
                                flowOutsFrom(moment.node.id, "out");
                            }
                        } else {
                            tools.printError(input.failureText || "That is not it.");
                            sdk.Events.emit("QE." + moment.id + ".wrong", { answer: answer });
                            if (moment.node && input.wrongRoute === "wrong") flowOutsFrom(moment.node.id, "failure");
                        }
                    });
                }
            };
            sdk.RegisterCommand(cls);
        });

        function flowOutsFrom(id, handle) {
            g.edges
                .filter(function (e) { return e.source === id && e.kind === "flow" && (!handle || e.sourceHandle === handle); })
                .forEach(function (e) { runFlow(e.target, { payload: {}, vars: {} }, 0); });
        }

    }

    /* ── websites ──────────────────────────────────────────────────────── */
    function registerWebsite(w, extraPages) {
        var pages = (w.pages || []).map(function (p) {
            /* The SDK's WebsitePageDefinition also takes a search-result
               description and extra search terms (r129). Emit them only when
               the author set them, so a page that does not use the fields
               produces exactly the same export as before. */
            var page = { path: p.path, title: p.title, html: p.content, seo: !!p.seo };
            if (p.description) page.description = p.description;
            if (p.search && p.search.length) page.search = p.search;
            return page;
        });
        (extraPages || []).forEach(function (p) { pages.push(p); });
        var cls = class extends sdk.Website {
            constructor() {
                super(...arguments);
                this.SiteName = w.name || w.host;
                this.Host = w.host;
                /* Icon is declared abstract on Website, so it is a member the
                   engine expects to find. We have no icon to give a generated
                   site, but every website in Nemesis sets it - including to ""
                   - and an absent abstract member is precisely the kind of
                   thing this build ignores without complaint. */
                this.Icon = w.icon || "";
                /* popular is declared on WebsiteDefinition (docs/03 Q12 —
                   purpose unverified; our self-test compares search ranking
                   with one site flagged). Emitted only when set, so sites
                   that don't use it compile exactly as before (r129 rule). */
                if (w.popular) this.Popular = true;
                this.Pages = pages;
            }
        };
        sdk.RegisterWebsite(cls);
    }

    /* ── pack extras (r203) ──────────────────────────────────────────────
       The things a pack puts outside its own quests: start-menu items, desktop
       widgets and right-click items, plus the click actions behind them.

       All four surfaces were verified in game before any of this was authored
       (r200/r201 probe). Two findings are baked into what follows:

       - Menu.addItem renders items in the bottom strip of the start menu, and
         the SDK's declared "section" field has NO visible effect - so nothing
         here passes one.
       - Desktop.addWidget's "transparent" defaults to TRUE in the SDK, which is
         why the first probe drew bare text with no background. The editor's own
         default is opaque, and the flag is always sent explicitly so the
         behaviour never depends on a default again.

       Every registration is guarded and every click is wrapped: a pack whose
       extras misbehave must not be able to stop its quests from loading. */

    function extraScope() { return __QE.tokenScope(); }

    function extraText(text) {
        return __QE.fill(text || "", extraScope());
    }

    function extraAction(action) {
        var a = action || {};
        if (a.kind === "claim") {
            return function () {
                if (!sdk.Quest || !sdk.Quest.claim) return;
                __QE.log("extras: claiming quest " + a.questId);
                __QE.safe(function () { sdk.Quest.claim(a.questId); });
            };
        }
        if (a.kind === "mail") {
            return function () {
                if (!sdk.Mail || !sdk.Mail.send) return;
                var mail = {
                    subject: extraText(a.mailSubject),
                    content: __QE.htmlToText(extraText(a.mailContent)),
                };
                if (a.mailFrom) mail.from = extraText(a.mailFrom);
                var to = __QE.safe(function () { return sdk.Mail.getPlayerEmail ? sdk.Mail.getPlayerEmail() : ""; });
                if (to) mail.to = to;
                __QE.log("extras: sending mail \"" + mail.subject + "\"");
                __QE.safe(function () { sdk.Mail.send(mail); });
            };
        }
        if (a.kind === "handbook") {
            return function () {
                if (!sdk.Handbook || !sdk.Handbook.open) return;
                __QE.log("extras: opening handbook article " + a.handbookId);
                __QE.safe(function () {
                    if (a.handbookCategory) sdk.Handbook.open(a.handbookId, a.handbookCategory);
                    else sdk.Handbook.open(a.handbookId);
                });
            };
        }
        /* notify, and the safe default for anything unrecognised: say something
           rather than doing nothing at all. */
        return function () {
            var text = extraText(a.text) || "This item does nothing yet.";
            if (sdk.UI && sdk.UI.notify) __QE.safe(function () { sdk.UI.notify(text); });
            else __QE.log("extras: notify \"" + text + "\"");
        };
    }

    function registerExtras() {
        var extras = PROJECT.extras;
        if (!extras) return;
        (extras.menuItems || []).forEach(function (item) {
            if (!sdk.Menu || !sdk.Menu.addItem) return;
            __QE.safe(function () {
                /* The LABEL is read when the item is registered, exactly like
                   a quest's Title - so a {{tr.…}} in it has to be resolved here.
                   The action's own text waits until the click, which is later
                   and may legitimately differ. */
                var def = { id: item.id, label: __QE.fillTranslations(item.label), onClick: extraAction(item.action) };
                if (item.icon) def.icon = item.icon;
                sdk.Menu.addItem(def);
            });
        });
        (extras.widgets || []).forEach(function (w) {
            if (!sdk.Desktop || !sdk.Desktop.addWidget) return;
            __QE.safe(function () {
                /* Spread the record the compiler built, then add the two fields
                   the SDK wants in another shape (the position pair and an
                   always-explicit transparency flag). */
                var def = {};
                for (var k in w) def[k] = w[k];
                def.position = { x: w.x, y: w.y };
                def.transparent = !!w.transparent;
                sdk.Desktop.addWidget(def);
            });
        });
        (extras.contextItems || []).forEach(function (item) {
            if (!sdk.ContextMenu || !sdk.ContextMenu.register) return;
            __QE.safe(function () {
                var def = { id: item.id, label: __QE.fillTranslations(item.label), target: item.target, onClick: extraAction(item.action) };
                if (item.icon) def.icon = item.icon;
                sdk.ContextMenu.register(def);
            });
        });
    }

    /* Translations go in FIRST, before any quest is registered: a quest's Title
       is read at registration (the SDK says so), so a translated title has to
       already exist by the time registerQuest runs. */
    (function registerTranslations() {
        var t = PROJECT.translations;
        if (!t || !sdk.Localization || !sdk.Localization.register) return;
        Object.keys(t.strings || {}).forEach(function (language) {
            __QE.safe(function () { sdk.Localization.register(language, t.strings[language]); });
        });
    })();

    (PROJECT.quests || []).forEach(registerQuest);

    /* Websites are registered after the quests, so anything a quest adds to a
       site has already been collected. */
    (PROJECT.websites || []).forEach(function (w) {
        registerWebsite(w, []);
    });
    /* Pack extras are registered after the websites, for the same reason the
       websites come after the quests: everything that wants to add to them has
       already run. */
    registerExtras();

    /* The mod package entry point. Every piece of content is registered by the
       quest, website and command classes above, so this class has almost
       nothing to do - except say, in the game's own log, that it loaded.

       That line is not decoration. Two QA rounds were spent on mail that never
       arrived, and the thing that finally identified the fault was a game log
       in which every other installed mod printed a load banner and ours
       printed nothing at all. A mod that announces itself turns "the mail is
       broken" into "the mod never ran", which is a different bug entirely. */
    /* ── "did this mod load in this session?" (r193) ─────────────────────
       A cross-mod marker, and the reason it exists: the QA harness has to start
       quests that live in a DIFFERENT mod (this pack), and the game can leave a
       mod disabled - it remembers the disabled flag by mod, and it survives both
       a version change and the mod being removed from disk. Nothing in SDK 0.24
       lets one mod ask about another: ModInfo is a type with no reader, and
       Quest.claim() returns void, so a quest that was never registered fails
       SILENTLY. On 2026-09-19 exactly that cost a tester a session: qe24 run tw1
       printed "Claimed", nothing appeared, and the log had no line for this pack
       at all.

       SharedVariables is session-scoped and shared by every mod, so it answers
       the one question the harness needs: did this pack actually load? Set on
       load, removed on unload, both guarded - an older game build without the
       API keeps working, and nothing here is required for the story. */
    var LOADED_MARKER = "qe.export.loaded";

    function markThisModLoaded() {
        try {
            if (sdk.SharedVariables && typeof sdk.SharedVariables.set === "function") {
                sdk.SharedVariables.set(LOADED_MARKER, PROJECT.mod.version + " (" + __QE_BUILD + ")");
            }
        } catch (e) {
            __QE.log("could not leave the loaded marker (harmless): " + e);
        }
    }

    function clearThisModLoaded() {
        try {
            if (sdk.SharedVariables && typeof sdk.SharedVariables.remove === "function") {
                sdk.SharedVariables.remove(LOADED_MARKER);
            }
        } catch (e) {
            /* Nothing to do: the session is ending anyway. */
        }
    }

    var Mod = class extends sdk.Bootstrap {
        /* The cleanup moment the SDK offers, and its own example for it is a mod
           disabled by the user - verified in game 2026-09-19: disabling this mod
           in the Mods list makes the game unload the package after the restart,
           this hook runs, and the accounts are gone from the save afterwards.
           Synchronous, and it returns nothing - the game awaits whatever this
           hook returns, and r72 is the story of what happens when that promise
           never settles.

           A plain quit does not call it, and a mod deleted from disk while the
           game is closed can never call it. See removeAllTwotterAccounts. */
        OnModPackageUnloaded() {
            try {
                __QE.log("unloading: removing the Twotter accounts this mod declared");
                removeAllTwotterAccounts();
            } catch (e) {
                __QE.log("unloading: Twotter cleanup failed (continuing): " + e);
            }
            clearThisModLoaded();
        }
        OnModPackageLoaded() {
            __QE.log(PROJECT.mod.name + " v" + PROJECT.mod.version +
                " loaded (editor build " + __QE_BUILD + ").");
            markThisModLoaded();

            /* Nothing else happens here, and nothing is returned.

               r72 cleared stale networks from this hook and returned the
               promise. The game AWAITS whatever OnModPackageLoaded returns, so
               when destroyNetwork was called on an address with no network and
               did not settle, the loader waited forever: the game never
               reached its main menu. That is far worse than the bug it was
               fixing - one broken quest against an unusable game.

               The stale-network problem is gone for a better reason now:
               addresses are allocated by the game per playthrough (r73), so a
               mod can no longer collide with one an earlier build of itself
               left in the save. There is nothing to clear.

               Standing rule: never hand the game a promise it has to await
               unless we can prove it settles, and we cannot prove that about
               any SDK call. */
        }
    };
    sdk.RegisterModPackage(Mod);
    return Mod;
}

var __QE_MOD;
module.exports = Object.defineProperty({ __esModule: true }, "default", {
    get: function () { return __QE_MOD; },
    enumerable: true,
});
__QE_MOD = __qeRegisterProject(sdk, PROJECT);
