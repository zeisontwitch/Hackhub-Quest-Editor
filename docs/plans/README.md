# Working notes

One file per investigation, written before the fix rather than after it. They
are kept because the *evidence* is the valuable part: what was measured, which
theories were tested and discarded, and why the eventual answer was the answer.

Not current documentation. For how the editor works today, see
[`../06-how-it-works-today.md`](../06-how-it-works-today.md).

| Round | Investigation | What it established |
|---|---|---|
| r225 | **VF-1: the old emission stays absent — the rename is the cure; saga closed** | v3 (pre-rename, r217) absent alone on a fresh save, the same day v6 (renamed, r222) rendered alone. Within editor exports the A/B is decisive: anonymous-class exports never surface posts; per-quest-named classes do (r221+). The HF-11 harness footnote (same inferred name, rendered) goes to the developers with the full falsification matrix — 11+ candidate causes eliminated in one day. Exports older than r221 need re-exporting; H-12 (editor avatar+comment visuals) is the only open row, optional. |
| r224 | **Correction: v6 rendered ALONE — the fork collapses to one run** | Zeis confirmed no canary was installed beside v6, killing the co-installation theory before it was tested. VF-1 is now: the OLD v3 export ALONE, fresh save. Renders → the r215–r218 failures were a stuck profile feed pipeline (the held HandleQuestHackhubPosts job) that the harness flood cleared — an engine note, editor exonerated. Absent → the r221 rename is the cure (with an HF-11 footnote). |
| r223 | **v6 RENDERED — the editor's feed posts work; VF-1 splits the last two explanations** | Zeis's v6 (bare post, r222 build) is the first editor export to surface since 1.0.38; author doubly cleared (\"Zeis\" and \"Zeissss\" both rendered). The emitted delta between silent r218 and working r222 is exactly the r221 rename — but v6 also ran beside a hand mod for the first time, and HF-11 (the cls twin) rendered. VF-1 (old v3 zip + canary together) decides: co-installation trigger vs the rename as cure. |
| r222 | **The author test: every silent export is authored \"Zeis\"** | HF-11 rendered — class names are innocent too; every hand-authored mod renders and the suppression is in the editor export itself. The last untested correlation: the manifest author string ("Zeis" on all five silent exports; nobody who ever rendered). Canary 1.0.2 flips exactly that string (HC2); plus v4, a fresh post authored in the r221 editor, as the end-to-end retest. Archaeology: the 1.0.38→1.0.39 runtime diff (52 lines, flow-runner only) confirms the once-render needs no special explanation. |
| r221 | **The class identity theory: every editor quest was class \"cls\"** | The r220 grid rendered all eleven posts (canary included) — shapes, quest fields, manifest all innocent; accept-reveals measured the employer fallback and confirmed hidden-until-accept posters are standard. The last structural difference: editor-compiled quests are anonymous classes assigned to `cls` (shared identity across every quest/export) while hand mods name theirs — one claimed `cls` (2026-09-21's feed accept) would retire every editor post since. r221 renames the emitted class to the quest's name (guard falsified) and ships the HF-11/HF-12 decisive pair; probes are abandonable now. |
| r220 | **The grid rendered: shapes innocent, the mod is the variable** | All five r219 harness posts rendered in one feed — the file-avatar contract drew (HF-3), name-without-avatar draws a broken icon, the employer fallback measured nothing (wrong shape, redone). r220 isolates the two surviving deltas: the editor's ALWAYS-ASSIGNED quest fields (HF-6…HF-10, the r84 anti-pattern vs the feed) and the manifest (canary `qe24-feedcanary`, editor-identical permissions, HC1). Harness 1.0.28; scaffold tests + falsified guards; editor blurbs updated with the broken-icon finding; §21 amended, §22 filed. |
| r219 | **The harness feed probe: five post variants, one look** | H-10's bare post failed too — every editor-export shape is now eliminated, including the only one that ever rendered (the r211 probe from the QA harness mod, whose five permissions vs the editor's two is the sharpest untested variable). r219 adds `qe24 feed` to the harness (1.0.27): HF-1 bare / HF-2 named poster / HF-3 avatar file / HF-4 likes+named comments / HF-5 employer fallback, under version-stamped names (claim memory mints each name once), run-clear extended; scaffold tests drive the grid and both guards were falsified. Player avatar returned → H-07 closed game-side. |
| r218 | **v3 failed flawless: the author block is the surviving suspect** | The r217 retest ran every check green (fresh name+id, named commenter, extracted assets, contract-clean shapes) and the post still never surfaced. The matrix: the only ever-render was a bare content-only post (1.0.38); every post with an author block failed across three fresh names and two fresh saves. Claim memory explains repeats, not v3. r218 files §21 (mod posts stopped surfacing + the held HandleQuestHackhubPosts job), puts the bare-shape replication in the checklist as H-10 (H-11 isolates the author block), and folds the evidence into the feed-post notice. Player card = game-side (429ing auth gateway, breaks on clean saves). |
| r217 | **The clean retest that still failed: the blank comment author** | The r216 export shipped the extracted asset and zero data-URIs, and the post still never surfaced. The log's two suspects (API v1 compat, held `Queue.HandleQuestHackhubPosts` job) are in every session on file including vanilla. Ground truth from the SDK types: feed posts appear only while the quest "hasn't been claimed yet" (per-profile claim memory, documented — answers the §18 mystery half), quests have no `id` (the game knows `Name`; our hidden `q-blank` never reaches it), and a comment author **requires a name** — both failing quests emitted `author: {}` for a blank commenter, the one rendered post had no comments. r217 omits the author when blank (avatar cannot ride without a name), withdraws the blank-commenter advice, surfaces the internal id with a confirmed reset (store migrates active-quest/viewport), files §19 (API v2) + §20 (blank comment persona). |
| r216 | **The playtest-as-a-player round: quest images were data-URIs** | Zeis's editor-authored export never reached the feed and broke his player avatar: the compiler shipped quest-level images (employer/post/comment avatars, quest icon) as inline data-URIs the game cannot load — only mod icon/cover were ever extracted. r216 extracts them to `assets/q<n>-<slot>.png` (verified against his uploaded project). Also from his pass: the feed-post notice is info-level (was error — red on a correct setup), Journal group + comments blurbs, the section inside Behaviour, employer-vs-poster written down and measured (H-06), XP honestly annotated (SDK-declared, never seen in game). |
| r215 | **Hackhub quest posting** — implemented as planned (likes + comments in, removal → §18) | ([plan](r215-hackhub-posting-plan.md)) The start route exists end-to-end except in the UI: quest-level `hackhubPost` is rich in the schema (content, media, author name/avatar, likes, comments), the runtime maps it but **drops every avatar**, and no editor surface writes it. Evidence: the post rendered once with a game-generated persona (blank fields = game persona, proven), then never again — profile-level claim memory is the theory to verify with a fresh quest name. Shape: a **quest-settings section** (the field is registration data, like autoStart), dice name (`fullName`), image-upload avatar, blank = game-generated; chaining stays on the deterministic claim node (no runtime post API). Folded in: the **mail node's dead To field** (graph path never reads it) — the last real mail item. |
| r214 | **The mail-authoring playtest: all green — round closed** | (results: [`../reference/sdk-0.24-qa/QE24-TestResults-MailAuthoring.md`](../reference/sdk-0.24-qa/QE24-TestResults-MailAuthoring.md)) W-01…W-04 green in game: the Reply button path, the `to` = From reply recipe (objective ticked via Mail.Sent), withdraw-at-complete (`Mail.remove -> true`, exactly one item) and persistence after reload. W-05 closed on prior evidence. Feed-post wrinkle recorded. QA folder back to NOTHING OPEN. |
| r213 | **W-01/W-02 green in game; the done-wire bug and the probe's missing ending** | Zeis's run proved the reply recipe on an editor-authored quest (objective ticked via Mail.Sent, to=From). W-03 was untestable: the probe had no ending (hasCompleteButton defaults off — docs/04), and adding a Complete-quest node exposed runtime bug #2 — flow arrival followed a trigger-objective's **done** wire, completing the quest at start. r213 stops flow at trigger objectives; the listener owns tick + done. Verified end-to-end on the compiled artifact; guard falsified. Probe splice bug fixed (stale-write). Export 1.0.40. |
| r212 | **The playtest probe exposed a flow-runner bug — trigger objectives pre-ticked** | Found by Zeis's first W-01 attempt: the probe's objective completed itself on accept. Root cause in `runtimeSource.ts`'s flow runner: `case "objective"` ticked every objective flow reached, trigger or not, so `mail → objective` wiring (the natural shape, and Bad Attachment's) pre-empted the event trigger. Fixed with an `objectivesWithTriggers` guard; falsified by revert; regression test replays the probe shape against the compiled artifact. Every trigger-objective template carried the same latent bug. Export 1.0.39. |
| r211 | **Mail authoring** — implemented as planned | ([plan](r211-mail-authoring-plan.md)) Turn the M-answers into editor features: replyable mails go out **direct** (`Mail.send({ replyable: true })` — M-04 disproved the quest-path assumption), capturing the returned id; a `withdrawOnQuestEnd` flag rides the existing `questCleanup` drain (ids make removal exact — M-02/M-03/M-07); the compile warning that repeated the disproved claim is rewritten around the real rule — **replies are matchable only by `to` = your from address** (M-05/M-06) — plus a new warning for replyable-without-from; the `Mail.Sent` catalogue entry and node help gain the reply recipe. Editor already authors reply triggers (`to`-contains conditions), so no new trigger machinery. For review: flag default (off), keep the quest-path throw-fallback, export swap cadence. |
| r210 | **The mail rows ran; the retired probes' surfaces are swept** | ([round record](r210-mail-verdicts-and-probe-retirement.md)) All ten M-rows answered (transcript: [`../reference/sdk-0.24-qa/QE24-TestResults-Mail.md`](../../reference/sdk-0.24-qa/QE24-TestResults-Mail.md)). Three findings went to the developers: **no `repliedTo`** on replies (docs/03 §16 — match a reply by `to` = the original's `from`), **no `subject` in `getInbox()`** (docs/03 §17 — the sweep now matches probe from-addresses), and **unload-hook refusals** (`Mod "null"` for every gated call, docs/03 §14 amended — quest-end cleanup works, unload cleanup is impossible on 1.3.1). M-09 closed the README's `moment` roadmap row (the warning fires on a clean save: the game's own tweets). Probe fixture fixes: one tickable objective; from-based sweeps; raw-inbox-entry evidence line in the audit. **Retired the r199–r205 probes**: harness 1.0.25 excises the clickprobe and extras verbs, `qe24 extras cleanup` sweeps all 14 QE24 ids, the QA export 1.0.36 ships no extras/translations, the widget file is gone. Five mutations falsified; 1,786 tests green. |
| r209 | **Mail cleanup / replyable mail QA** | ([plan](r209-mail-qa-plan.md)) SDK 0.24 added `Mail.remove(id)` and `replyable` on `MailDefinition` — but the promised `repliedTo` field is absent from the declared `Mail.Sent` payload, and the editor runtime still routes replyable mail through `Quest.sendMail` on a stale no-flag assumption. **Probe built (harness 1.0.24): `qe24 mail send/audit/remove/watch/cleanup/unload/bounce` + the `QESdk024MailQa` quest**, rows **M-01…M-10** in one batched session — id shape, remove timing (including a queued, never-read mail), both replyable paths, the raw reply payload, quest-end and unload cleanup (always-armed, subject-gated sweep — a flag cannot live across the restart the hook needs), the residual `moment` look (M-09), and `Mail.sendBounce` (M-10, added on Zeis's approval). Nine guards falsified by revert; editor stamp-only (export 1.0.35, manual stamps swept). Authoring is the next round, evidence-gated. |
| r199 | **The cheap wins — pack extras and localization — plan for review** | ([plan](r199-cheap-wins-plan.md)) Stage A: a harness probe for `Menu.addItem`, `Desktop.addWidget`, `ContextMenu.register` and `Localization` (no prior art in this repo — handbook, transcriptions and shipped mods are all silent), with four readings that each decide a design. Stage B: a **Pack extras** dialog reusing the website builder's page editor for widget HTML, and **localization** as a Languages table plus a `{{tr.…}}` token through the existing filler. One decision for Zeis: what a click does. **Both stages built (r203).** Stage A's readings (r200/r201) became facts in the build: no `section` control, widgets defaulted to opaque and always sent explicitly. Stage B shipped the dialog, the emitter and the runtime halves — including the trap the plan flagged, a Title read at registration, which turned out to apply to menu and right-click labels too. **Open: rows T-23…T-27 in game against export 1.0.28.** |
| r198 | **What the SDK offers and the editor does not — coverage audit** | ([plan](r198-sdk-coverage-audit.md)) Every exported SDK 0.24 namespace matched against what the editor can emit (40 node types, the compiled runtime, the project schema) — a namespace counts as exposed only when the runtime actually calls it for an authored project. Not exposed: **phone apps** (`PhoneApp`/`RegisterPhoneApp`), menu entries / desktop widgets / right-click items, localization, themes, guided tours, per-pack settings, dynamic HTTP endpoints and interception, HTTP history/cookies, reading a bank balance, file delete/move/rename, and Kisscord/Twotter extras. Recommends phone apps first (needs a harness probe), menu/desktop and localization as the cheap wins, and `Variables`/`Time.scale`/`Scheduler.list` as declared non-goals. The phone-proxy roadmap row was dropped the same round: the reporter meant an NPC-to-NPC call staged as eavesdropping, which the Dialogue node's call mode already authors. |
| r185 | **Twotter comes back — plan for review (revision 2)** | ([plan](r185-twotter-return.md)) The probe's evidence (r179) says the feature can return on the API path: a `bio: undefined` record no longer crashes search, `createUser` fills complete records, `removeUser` deletes accounts including quest-declared ones, and posts work. Zeis's answers are folded in: accounts at **mod level** (handle, display name, bio, avatar/banner, verified, counts, per-account "remove when the quest ends" toggle) with the removal rule **only what we created, and only when no other live quest declares it**; the node — named **"Twotter"** — now posts a **list of tweets**, each with its own time (**when the story arrives** or **earlier** by amount + unit), so a profile can be found carrying a month of history the way @alinamack's does in the Journalist's Sister line; a **migration** maps pre-r31 drafts (tweets, times, counts) instead of dropping them; a strict **fence set** (never emit the declarative `TwotterAccounts`/`Tweets` fields; assert a blank bio still creates `bio: ""`); QA rows **P-01** then T-08…T-15 plus a `qe24 twotter audit` command; the **whimsy** pass now per Zeis — the canvas card stays a normal card, the click-to-edit **mock profile** lives in the Twotter panel, and the node previews the timeline. **Both probes are green** (2026-09-18): `postTweet` keeps the `sendedAt` we send, in all three spellings tried, and a profile sorts by time with the **newest at the top** (`B, C, A`) — so the preview mirrors the game and the author's list stays chronological. Results: [`../reference/sdk-0.24-qa/P-01-BACKDATE.md`](../reference/sdk-0.24-qa/P-01-BACKDATE.md). **Stage 1 is built and the in-game round has run** (2026-09-18): **T-08/T-09/T-10/T-12/T-13/T-14 pass**; the moment.js line in the log is the game's own content, not ours; **T-11 failed** (an always-unchecked objective hid the Complete button, and the abandon left the account behind) and the run exposed three runtime bugs — a no-op quest-start hook, "live" counting unstarted quests, and a posting guard that survived a re-claim — all fixed in r186 with falsified fences. Tweet pictures do not survive the posting API at all, so the editor now says so. The 2026-09-19 pass closed **T-11b (completion)** and **T-12b** and settled the picture question — **SDK 0.24 has no picture field on a tweet**, so the control is hidden (r187) and a feature request is filed — and **stage 2 shipped in r188**: the panel became a click-to-edit mock profile that also lists what the account has posted across every quest, the node inspector previews the profile **newest-first** over its timeline, and one CSS shimmer marks the arrival row (dropped under `prefers-reduced-motion`). The 2026-09-19 second pass closed **T-08b** and **E-01** and produced one honest limitation: deleting the mod from disk leaves the accounts it created in the save (the SDK documents it; the unload hook cannot run for a mod that is gone), so the row became **T-15c** — disable it in the game's Mods list while running — and question 11 was filed. **The round closed on 2026-09-19** — every row green or resolved (the abandon half of T-11b last: `removeUser(qe-tw-account) -> true (the last quest that needs it ended)`, audit `not on this save`); export **1.0.25**, harness **1.0.19**. |
| r184 | The QA ledger closes | Timer row S-11 green (`qe24 schedule 120` → `NEXT EVENT` showed 1 h 55 m, ticking down), S-10 shelved by Zeis's decision (not a pass), and the r183 row-fold fix confirmed visually. Export stamp only. |
| r183 | Inspector rows fold instead of clipping | The third fixture pass closed **S-12** and **S-15** (both migration values show: `Wait` + hours 2, and `A coming day` with the clock at 18:23 and the card reading `in 2w at 18:23`) and produced a real editor bug: the Timer's four-box "In" row needs ~28rem of panel and the docked inspector is 340px, so the boxes ran off the right edge. The row is now a `@container` and folds to 2×2 below the width its captions need, opening back up when the panel is dragged wider — widening the default was rejected because the panel's floor is still 340 and a floating drawer can be 280. jsdom cannot prove pixels, so the tests assert the shape of the fix and the visual check is a screenshot pass. |
| r182 | Fixtures and an honest log: the S-12/S-15 blocker was an editor bug | The second Timer run closed **S-03** (the cancel evidence was in the log he pasted: armed → fired → re-armed → `OnAbandon` → cancelled, nothing fired afterwards) and revealed two defects of ours. **The editor bug:** both migration fixtures opened as "No quest selected" + an empty canvas + the first-run template hint, because neither file carries `editor.activeQuestId` and no load path picked one — indistinguishable from a broken file, and not fixture-specific. `ProjectSchema` now points the editor at the first quest that ships, so file load, import, autosave, templates and the export generator all agree. **The log:** the cancel line said "cancelled 2 pending timer(s)" when one had already fired; fired jobs are dropped as they fire. S-11 answered in part (the clock panel showed the game's own job, not the mod's, so the panel does not put mod jobs ahead of it) and has a definitive recipe. |
| r181 | Quiet QA: the notification storm, and the rows the pasted `qe24 timers` output already answered | Zeis's Timer run closed ten of the fifteen rows (the reload paste shows identical job ids and `fireAt` before and after save/quit/reload; the mixed row resolved to 3 Nov 18:23 and Wait-1-month to 18 Oct 19:27) — but the run was unusable as a *checklist* because five QA quests auto-started at load and four of them toasted. Every QA quest is now claimed on demand (`qe24 run`), debug nodes no longer toast, and two guards keep it that way. |
| r180 | The Twotter verdict, and the Timer rows made runnable | The probe came back green on the read path (build 25388883) and the checklist Zeis could not find now exists: `TIMER-ROWS.md` plus harness 1.0.10's `qe24 timers`, which reads a pending job's resolved moment instead of waiting out the month. Two new auto-start QA quests and two legacy fixtures make every S-row runnable; the `AccountCreated` finding is recorded for the implementation round. |
| r179 | The Twotter probe | sdk-0.24 declares `updateUser`/`removeUser` and 1.3.0 claims the crash is fixed, so the raw harness gets one open probe (`qe24 twotter`, rows T-01–T-07) that plants the exact r31 bad-record shape, tests repair-on-load, and checks the quest-declared write path. Eleven tests drive the probe against a stub SDK. |
| r178 | QA folder closed, S-04 answered | The in-game clock displays the machine's local time (20:17 on screen vs 20:15 local / 18:15 UTC), so the `at` correction stays. The QA folder becomes tooling: one STATUS ledger, no procedures, harness guide no longer lists finished checks. Twotter's r31 crash cause is fixed in 1.3.0, reopening the feature. |
| r177 | Every unit, both ways | Both relative rows take one box per unit: a coming day counts years/months/weeks/days from now and pins a clock, Wait takes every unit (calendar included, via `scheduleAt` — the engine's duration form has no month field). The clamp is applied once, on the calendar part, before weeks and days. |
| r176 | Timer inspector: the digital clock, rows, and relative units | The Timer resolves “in N days / weeks / months / years, at HH:MM” inside the game at arm time; the editor says the rule, warns about impossible dates and clamps short months. Rows and the clock are layout kinds, transparent to the manual extractor and to every walker. |
| r175 | Refreshed QA export + the r174 leftovers | The installable QA export is regenerated at build r175 (mod 1.0.3, both quests) and guarded byte-for-byte against the compiler; the manual's conditional-field sentence names the value it waits for (G16); the stale freeze hints are gone; and `qe24 clock` in raw harness 1.0.7 settles the Timer `at` timezone question (S-04) without a date edit. |
| r171 | Inspector polish + phone proxies | Field warning tooltips need an opaque surface; the inspector handle should face the canvas; pinned SDK/docs have phone dialogs and phone apps but no declared phone-proxy call API yet. |
| r170 | Ask player prompt node | `UI.prompt` becomes a small Effects node: one-line question, optional masked input/default/example text, optional save-to-data, Submitted/Correct/Wrong/Cancelled routing, and handbook coverage. |
| r75 | Stale IPs in the save | Mod-created networks persist after uninstall, and an older build's network wins over a new one at the same address. Led to game-allocated addresses everywhere. |
| r94 | Selection gestures | React Flow clears the selection at the start of every box drag with no modifier check; ctrl+drag-to-deselect does not exist upstream at all. |
| r98 | Alignment, first pass | Snapping each corner after centring pushes differently-sized cards back off the shared line. |
| r99 | Alignment, second pass | The real defect: alignment was being handed `size: undefined` for every node, so centring silently became corner alignment. |
| r102 | Node search | Blender's model — open at the pointer, type immediately, no separate search mode. Also two collisions found by reading our own code. |
| r120 | Remote file seeding | The `world.files` node pointed at a device silently did nothing; it now folds into the device tree at build time. |
| r119 | Handbook gap analysis | What the in-game handbook settles (phishing, Suspicion, port forwarding, log paths), where the editor cannot yet express what it teaches, and three r117 guesses it contradicts. |
| r117 | Template rebuild plan | Nine entries across three tiers, covering the two website styles, the phishing flow, and a multi-tool expert chain. Also the mechanical exploitability test, which corrected two claims the r116 audit got wrong. |
| r116 | Template audit | Three templates ask the player to break in and were never given the account setup that allows it; `investigation` also ships two nodes that compile to nothing. Plus a re-read of what each template is *for*. |
| r107 | Wire physics | Why a damped spring on one scalar rather than a Verlet rope, and the ten failure modes the design has to survive. |
| r123 | Editor UX audit | Field-level warnings are the missing feature (node analysis and compile warnings exist, nothing at field level); "no jargon" means no *mod-coding* jargon — the game's own vocabulary stays. |
| r124 | Actionable hookup warnings | Every node issue gets a `nextStep` ("which nodes to put where"), rendered in the inspector header and canvas tooltips; empty-IP and seed-files placement checks move earlier. |
| r125 | Zeis's UX-check fixes | 14 items end to end — event names, port presets, firewall addresses, tag picker, table editor, working Open-handbook, numbered sequences; the firewall rule was a list field over single-object data. |
| r126 | Template audit | Four templates wired endings to `entry.complete`, which never fires with the defaults; Cold Storage's database had no tables; Help Desk's client confirmed an unsent file; ssh taught without `-h`. All fixed and pinned (ledger deferred per Zeis). |
